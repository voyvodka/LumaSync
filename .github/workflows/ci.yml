name: CI

on:
  push:
    branches:
      - main
      - 'release/**'
  pull_request:
    branches:
      - main

# Pin the macOS deployment target at the workflow level so it is a real OS
# environment variable inherited by every rustc/swiftc/ld invocation in every
# step. This is the outermost belt in a belt-and-suspenders guard against the
# 1.5.2 launch crash (issue #115): linking with a deployment target < 12.0
# makes libswift_Concurrency back-deploy to an `@rpath` that only exists on the
# build machine, so dyld aborts at launch on users' machines. Tauri's bundler
# would otherwise inject the 10.13 default; a process-level env var beats both
# that and any Cargo `[env]` precedence quirk. Harmless on Linux/Windows.
env:
  MACOSX_DEPLOYMENT_TARGET: "12.3"

# Cancel superseded runs when a branch is force-pushed or pushed multiple
# times in quick succession. Saves ~15-20 minutes of duplicate Rust matrix
# work per redundant push.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    name: Build and Check (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    # Windows needs ~30 min on a cold cache (vendored openssl + aws-lc-rs), and the
    # cache save that follows was being cut off mid-write at the old 30. Raised
    # again for the launch smoke test, which relinks the app under a second cfg.
    timeout-minutes: 55
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Select Xcode 26 (macOS 26 SDK for screencapturekit 8 / apple-metal)
        if: runner.os == 'macOS'
        run: |
          # screencapturekit 8 pulls apple-metal 0.8.8, whose Swift bridge
          # references Metal 4 sampler APIs guarded by `if #available(macOS
          # 26.0, *)`. Swift still needs those symbols defined at COMPILE time,
          # i.e. the macOS 26 SDK. The macos-15 runner ships Xcode 26.x but
          # defaults to Xcode 16.4 (no macOS 26 SDK), so select the newest
          # installed Xcode explicitly. The availability guard keeps the built
          # binary runnable on older macOS.
          latest=$(ls -d /Applications/Xcode_26*.app 2>/dev/null | sort -V | tail -1)
          if [ -z "$latest" ]; then latest=$(ls -d /Applications/Xcode_*.app | sort -V | tail -1); fi
          echo "Selecting $latest"
          sudo xcode-select -s "$latest/Contents/Developer"
          xcodebuild -version
          echo "macOS SDK: $(xcrun --sdk macosx --show-sdk-version)"

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          # v1.6: busted to force a cold rebuild of the upgraded Rust deps
          # (screencapturekit 8 / keyring 4 / tauri 2.11.3) under Xcode 26.
          prefix-key: v1.6-rust-cache
          shared-key: ${{ matrix.os }}

      - name: Install Linux deps for Tauri + xcap (X11 capture)
        if: runner.os == 'Linux'
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # xvfb + dbus-x11 are for the launch smoke test, not the build: the app
          # needs a display to open its window and a session bus for the tray.
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            pkg-config \
            libudev-dev \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libxcb1-dev \
            libxcb-randr0-dev \
            libxrandr-dev \
            libpipewire-0.3-dev \
            libdbus-1-dev \
            libgbm-dev \
            xvfb \
            dbus-x11

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: TypeScript type check and shell contracts (Linux only)
        if: runner.os == 'Linux'
        run: pnpm check:all

      - name: Run frontend tests (Linux only)
        if: runner.os == 'Linux'
        run: pnpm vitest run

      - name: Rust fmt check
        working-directory: src-tauri
        run: cargo fmt --all -- --check

      - name: Rust clippy (deny warnings)
        working-directory: src-tauri
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Rust check (all targets, all features)
        working-directory: src-tauri
        run: cargo check --all-targets --all-features

      - name: Rust tests
        working-directory: src-tauri
        # --test-threads=1 is required: lighting_mode tests share a global
        # ACTIVE_AMBILIGHT_WORKERS AtomicUsize; parallel test execution causes
        # spurious failures when other tests' workers bleed into the count.
        run: cargo test --all-features -- --test-threads=1

      - name: Verify macOS Swift runtime linkage (guard against #115 regression)
        if: runner.os == 'macOS'
        working-directory: src-tauri
        # Build a native-arch binary and assert the link directives that
        # prevent the 1.5.2 launch crash: (a) LC_BUILD_VERSION minos >= 12.0,
        # and (b) no load command references @rpath/libswift_Concurrency. This
        # is the cheap CI mirror of the release.yml guard — it catches a
        # deployment-target regression on every push, before a tag is cut.
        run: |
          cargo build --bin lumasync
          BIN="target/debug/lumasync"
          bash ../scripts/verify/macos-swift-runtime.sh "$BIN"

      - name: Build app for the launch smoke test
        if: runner.os != 'Windows'
        # Not `cargo build`: that emits a dev-cfg binary to the same path which
        # loads the Vite dev URL, so it comes up blank and proves nothing.
        # `--no-bundle` skips packaging, which launch does not exercise.
        run: pnpm tauri build --debug --no-bundle

      - name: Launch smoke test (Linux)
        if: runner.os == 'Linux'
        env:
          # Xvfb has no GPU, and WebKit's accelerated compositor and DMA-BUF
          # renderer both abort without one, taking the webview down with them.
          WEBKIT_DISABLE_COMPOSITING_MODE: "1"
          WEBKIT_DISABLE_DMABUF_RENDERER: "1"
        # dbus-run-session gives libayatana-appindicator a session bus to register
        # the tray against instead of leaving it to GDBus autolaunch.
        run: xvfb-run -a dbus-run-session -- node scripts/verify/launch-smoke.mjs

      - name: Launch smoke test (macOS)
        # The runner provides a real desktop session, so the app launches as-is.
        if: runner.os == 'macOS'
        run: node scripts/verify/launch-smoke.mjs

      # Windows has no launch smoke step — see #181.

      - name: Rust audit (Linux only)
        if: runner.os == 'Linux'
        working-directory: src-tauri
        run: |
          cargo install --locked cargo-audit
          cargo audit
