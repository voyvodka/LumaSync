# Ratchet, not a target — it may only SHRINK. Read by untyped-mock-ratchet.mjs.
#
# The number of bare `vi.fn()` test doubles (comments excluded) under src/,
# mock/ and e2e/. Was 665 before the typed IPC layer typed the boundary ones.
377
