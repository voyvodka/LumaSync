# Codes declared in src/shared/contracts/** with no Rust producer.
#
# Ratchet, not a suppression list. A listed code that gains a producer (or is
# removed) FAILS the verifier until the line is deleted, and a new undeclared
# producer that is not listed here fails immediately.
#
# The file holds two kinds of entry. MISSING PRODUCER lines are defects and may
# only SHRINK. "Legitimate" lines are frontend-minted codes that are declared and
# never on the wire; adding one is allowed, and is the only allowed growth.
#
# Blank lines and `#` comments ignored. One code per line, sorted.

# Legitimate: minted by the frontend, never on the wire. `displayTargetState.ts`
# reports "no display to target, so no command was issued".
OVERLAY_NO_DISPLAY

# Legitimate, same category: `healthCheck.ts` synthesises a failed step when the
# health-check bridge was never injected, so no command ran. Added 2026-08-13
# when typing HealthStepResult.code turned it from a raw literal into a
# declaration — it was always frontend-minted, just undeclared.
HEALTH_CHECK_NOT_AVAILABLE

# Legitimate, same category: `useHueOnboardingCore.ts` holds it while it re-asks
# a bridge that answered HUE_PAIRING_LINK_BUTTON_NOT_PRESSED (#337). Was listed
# as a MISSING PRODUCER until the frontend started minting it.
HUE_PAIRING_PENDING_LINK_BUTTON

# No producer, but EN+TR strings exist under device.health.codes.*. Rust never
# sees the selected profile; the pickers compare the advertised one themselves.
SERIAL_HEALTH_FIRMWARE_MISMATCH

# No producer and no consumer. Its sibling HUE_CHANNEL_POSITIONS_UPDATED is real.
HUE_CHANNEL_POSITIONS_FAILED
