# Raw hex colour literals in src/ (comments, tests and the token declarations
# excluded), counted by design-tokens.mjs, which fails when the count rises.
# Ratchet: the number may only go down. What remains is mostly deliberate —
# SVG and canvas art, zone identity data, LED test-pattern swatches.
133
