/** Display name used in window titles, tray tooltip, and about surfaces. */
export const APP_NAME = "LumaSync";
/** Build-time app version, injected from `package.json` via Vite's `define`. */
export const APP_VERSION = __APP_VERSION__;
