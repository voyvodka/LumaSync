/**
 * Device Section
 *
 * Phase 1 baseline – LED device connection placeholder.
 * Full device connection setup is implemented in Phase 2.
 */

export function DeviceSection() {
  return (
    <div className="settings-section">
      <h2 className="settings-section__title">Device</h2>
      <p className="settings-section__description">
        LED device configuration will be available after Phase 2 (Connection Setup).
      </p>
    </div>
  );
}
