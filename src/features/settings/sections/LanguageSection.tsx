/**
 * Language Section
 *
 * Phase 1 slot – Navigation entry present, content wired in Plan 02 (i18n baseline).
 * Renders a placeholder so the sidebar IA is complete.
 */

export function LanguageSection() {
  return (
    <div className="settings-section">
      <h2 className="settings-section__title">Language</h2>
      <p className="settings-section__description">
        Language selection will be available after the i18n baseline is wired (Plan 02).
      </p>
    </div>
  );
}
