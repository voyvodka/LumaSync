/**
 * ZoneListPanel — Wave 0 test stubs for ROOM-03
 *
 * These stubs will be converted to real tests in Phase 19 Plan 03
 * when ZoneListPanel component is created.
 *
 * @vitest-environment node
 */
import { describe, it } from "vitest";

describe("ZoneListPanel", () => {
  it.todo("renders empty panel with prompt text when no zones");
  it.todo("renders zone rows with color chip, name, and light count");
  it.todo("adds new zone with default name on + Zone click");
  it.todo("allows inline zone name editing");
  it.todo("deletes zone on x button click");
  it.todo("highlights active zone row");
  it.todo("toggles channel assignment when Hue dot clicked in assign mode");
  it.todo("unassigned channels render at reduced opacity in assign mode");
});
