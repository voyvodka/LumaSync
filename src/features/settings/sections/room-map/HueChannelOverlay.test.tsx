/**
 * HueChannelOverlay — Wave 0 test stubs
 *
 * These tests will be implemented in Phase 17 Plan 03 when HueChannelOverlay
 * component is created. Stubs ensure requirement IDs are tracked.
 */
import { describe, it } from "vitest";

// Component import — will exist after Plan 03
// import { HueChannelOverlay } from "./HueChannelOverlay";

describe("HueChannelOverlay", () => {
  it.todo("ROOM-04: Hue channel dots render at correct positions on the room map canvas");
});
