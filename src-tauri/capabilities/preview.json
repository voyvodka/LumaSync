{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "preview",
  "description": "v1.6 LED Preview surfaces — the click-through digital-twin overlay (event listener) and the interactive control popup. Both load index.html and route by window label in main.tsx; the popup invokes preview commands and reads the store, and both bridge console output through tauri-plugin-log.",
  "windows": ["led-control-popup", "led-twin-overlay-*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-position",
    "core:window:allow-set-size",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-unminimize",
    "core:window:allow-close",
    "core:window:allow-center",
    "store:default",
    "log:default"
  ]
}
