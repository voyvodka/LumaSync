{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "preview-overlay",
  "description": "v1.6 digital-twin overlay. Click-through and passive: it listens for frame events, reads the shell store, and bridges its console into the log. No window API at all. `store:allow-set` stays because the shared shell-store read writes a migrated snapshot back. Policy: docs/architecture/ui-and-shell.md, \"Capabilities\".",
  "windows": ["led-twin-overlay-*"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "store:allow-load",
    "store:allow-get",
    "store:allow-set",
    "log:allow-log"
  ]
}
