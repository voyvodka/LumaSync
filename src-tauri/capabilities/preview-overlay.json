{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "preview-overlay",
  "description": "v1.6 digital-twin overlay. Click-through and passive: it listens for frame events, reads shell state, and bridges its console into the log. No window API, and no app command but the shell-state read. Policy: docs/architecture/ui-and-shell.md, \"Capabilities\".",
  "windows": [
    "led-twin-overlay-*"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "log:allow-log",
    "allow-get-shell-state"
  ]
}
