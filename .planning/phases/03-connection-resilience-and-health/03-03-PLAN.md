---
phase: 03-connection-resilience-and-health
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/features/device/deviceStatusCard.ts
  - src/features/device/deviceStatusCardMapping.test.ts
  - src/features/settings/sections/DeviceSection.tsx
  - src/locales/en/common.json
  - src/locales/tr/common.json
autonomous: true
requirements: [CONN-03, CONN-04]
gap_closure: true
must_haves:
  truths:
    - User can run health check and read top-level PASS/FAIL plus each step outcome in Device panel.
    - Health-check FAIL feedback remains visible with actionable guidance until state changes.
    - Reconnecting and health-check status precedence still prevents stale status cards from hiding active state.
  artifacts:
    - path: src/features/device/deviceStatusCard.ts
      provides: Status-card model includes deterministic step-outcome view data without collapsing to first failed step only.
    - path: src/features/device/deviceStatusCardMapping.test.ts
      provides: Regression coverage for full step outcome mapping and precedence persistence.
    - path: src/features/settings/sections/DeviceSection.tsx
      provides: Device panel renders per-step health outcomes from latest health-check result.
    - path: src/locales/en/common.json
      provides: English keys for health-step labels and pass/fail outcome text.
    - path: src/locales/tr/common.json
      provides: Turkish keys with one-to-one parity for health-step labels and pass/fail outcome text.
  key_links:
    - from: src/features/settings/sections/DeviceSection.tsx
      to: src/features/device/deviceStatusCard.ts
      via: mapped status model including health step outcomes
      pattern: "buildDeviceStatusCard"
    - from: src/features/settings/sections/DeviceSection.tsx
      to: src/features/device/useDeviceConnection.ts
      via: latestHealthCheck.steps consumed from hook state
      pattern: "latestHealthCheck"
    - from: src/features/settings/sections/DeviceSection.tsx
      to: src/locales/*/common.json
      via: i18n keys for step labels and pass/fail values
      pattern: "device\.healthCheck\.steps"
---

<objective>
Phase 3 verification gapini kapat: Device panelde health-check sonucunu yalnizca ozet degil, adim-adim okunabilir hale getir.

Purpose: 03-VERIFICATION.md'deki tek acik gapi (step-level outcomes gorunurlugu) kapatarak CONN-04 kullanici sozunu ve status geri-bildirim netligini tamamlamak.
Output: Step-level status model genisletmesi, mapper regression testleri, Device panel step listesi, EN/TR parity metinleri.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-connection-resilience-and-health/03-CONTEXT.md
@.planning/phases/03-connection-resilience-and-health/03-VERIFICATION.md
@.planning/phases/03-connection-resilience-and-health/03-02-SUMMARY.md
@src/features/device/deviceStatusCard.ts
@src/features/settings/sections/DeviceSection.tsx

<interfaces>
From src/features/device/deviceConnectionApi.ts:
```typescript
export interface HealthStepResult {
  step: DeviceHealthStep;
  pass: boolean;
  code: string;
  message: string;
  details: string | null;
}

export interface HealthCheckResult {
  pass: boolean;
  steps: HealthStepResult[];
  checkedAtUnixMs: number;
}
```

From src/features/device/deviceStatusCard.ts:
```typescript
export interface DeviceStatusCardModel {
  variant: "success" | "error" | "info";
  code: string;
  titleKey: string;
  bodyKey: string;
  details?: string;
}

export function buildDeviceStatusCard(input: DeviceStatusCardInput): DeviceStatusCardModel;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend status mapping to expose full health step outcomes</name>
  <files>src/features/device/deviceStatusCard.ts, src/features/device/deviceStatusCardMapping.test.ts</files>
  <behavior>
    - Test 1: Health-check FAIL mapping preserves all step outcomes in deterministic order (PORT_VISIBLE, PORT_SUPPORTED, CONNECT_AND_VERIFY), not only first failed step detail.
    - Test 2: Health-check PASS mapping still exposes full step outcomes and keeps success summary.
    - Test 3: Reconnecting and in-progress precedence remains unchanged so active operation visibility is not regressed by step rendering changes.
  </behavior>
  <action>Update the status-card model and mapper output so health-check results provide a render-ready step outcome structure (step identifier + pass/fail + message/details) while preserving current top-level title/body behavior. Add RED tests first in `deviceStatusCardMapping.test.ts` to codify the verification gap: per-step visibility must be deterministic and complete. Keep current user-decision tone and do not introduce auto-dismiss behavior.</action>
  <verify>
    <automated>yarn vitest run src/features/device/deviceStatusCardMapping.test.ts</automated>
  </verify>
  <done>Mapper returns complete step outcome data for PASS/FAIL health results, and regression tests prove no precedence breakage.</done>
</task>

<task type="auto">
  <name>Task 2: Render health-check step outcomes in Device panel with EN/TR parity copy</name>
  <files>src/features/settings/sections/DeviceSection.tsx, src/locales/en/common.json, src/locales/tr/common.json</files>
  <action>Render a visible per-step outcomes block in Device panel status area when `latestHealthCheck` exists, using mapper-provided step data and deterministic translations for step labels and pass/fail text. Keep inline status card as primary surface, keep reconnecting/health-fail visibility persistent until state changes, and keep technical details secondary under user-facing text. Add or update locale keys in both EN/TR files with exact parity for health step names and outcome labels.</action>
  <verify>
    <automated>yarn vitest run src/features/device/deviceStatusCardMapping.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Device panel shows PASS/FAIL summary plus three step outcomes after health check run, with parity i18n keys and compile-safe UI wiring.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/device/deviceStatusCardMapping.test.ts`
- `yarn tsc --noEmit`
</verification>

<success_criteria>
- 03-VERIFICATION gap "step-by-step outcomes not rendered" is closed by code-level evidence in Device panel.
- Health-check output is readable as top-level summary + deterministic per-step outcomes in both EN and TR.
- Existing reconnecting and health-fail visibility behavior remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/03-connection-resilience-and-health/03-03-SUMMARY.md`
</output>
