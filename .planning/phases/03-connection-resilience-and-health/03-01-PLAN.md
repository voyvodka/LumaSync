---
phase: 03-connection-resilience-and-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/contracts/device.ts
  - src/features/device/deviceConnectionApi.ts
  - src/features/device/useDeviceConnection.ts
  - src/features/device/recoveryFlow.test.ts
  - src/features/device/healthCheckFlow.test.ts
  - src-tauri/src/commands/device_connection.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [CONN-03, CONN-04]
must_haves:
  truths:
    - User can unplug/replug and the app starts bounded auto-recovery without restart.
    - User can trigger an explicit health check and receive pass/fail with step-level outcomes.
    - Manual connect/select action cancels prior auto-recovery and takes operation ownership.
  artifacts:
    - path: src/features/device/useDeviceConnection.ts
      provides: Single-operation controller gate with recovery + health-check orchestration.
    - path: src/shared/contracts/device.ts
      provides: Shared status/code/operation contracts for reconnecting and health-check flows.
    - path: src-tauri/src/commands/device_connection.rs
      provides: Backend command support for health-check verification primitives.
    - path: src/features/device/recoveryFlow.test.ts
      provides: Deterministic recovery behavior coverage.
    - path: src/features/device/healthCheckFlow.test.ts
      provides: Deterministic health-check step aggregation coverage.
  key_links:
    - from: src/features/device/useDeviceConnection.ts
      to: src/features/device/deviceConnectionApi.ts
      via: invoke wrappers for list/connect/status and health-check operation calls
      pattern: "(listSerialPorts|connectSerialPort|getSerialConnectionStatus)"
    - from: src/features/device/useDeviceConnection.ts
      to: src/shared/contracts/device.ts
      via: shared operation/status constants
      pattern: "DEVICE_(STATUS|COMMANDS)"
    - from: src-tauri/src/lib.rs
      to: src-tauri/src/commands/device_connection.rs
      via: tauri::generate_handler command registration
      pattern: "generate_handler!"
---

<objective>
Kopma-sonrasi toparlanma ve setup health-check davranisini controller + backend contract seviyesinde deterministic ve test-guvenceli hale getir.

Purpose: CONN-03 ve CONN-04 davranislarini UI'dan bagimsiz olarak dogru operasyon kurallariyla (bounded retry, manual override, single active operation) kilitlemek.
Output: Yeni resilience/health contracts, RED->GREEN test kapsamasi, backend command ve frontend controller orkestrasyonu.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-connection-resilience-and-health/03-CONTEXT.md
@.planning/phases/03-connection-resilience-and-health/03-RESEARCH.md
@.planning/phases/02-usb-connection-setup/02-02-SUMMARY.md
@.planning/phases/02-usb-connection-setup/02-03-SUMMARY.md

<interfaces>
From src/shared/contracts/device.ts:
```typescript
export const DEVICE_COMMANDS = {
  LIST_PORTS: "list_serial_ports",
  CONNECT_PORT: "connect_serial_port",
  DISCONNECT_PORT: "disconnect_serial_port",
  GET_CONNECTION_STATUS: "get_serial_connection_status",
} as const;

export const DEVICE_STATUS = {
  IDLE: "idle",
  SCANNING: "scanning",
  READY: "ready",
  CONNECTING: "connecting",
  CONNECTED: "connected",
  DISCONNECTED: "disconnected",
  ERROR: "error",
} as const;
```

From src/features/device/useDeviceConnection.ts:
```typescript
export interface DeviceConnectionController {
  initialize: () => Promise<void>;
  refreshPorts: () => Promise<void>;
  selectPort: (portName: string | null) => void;
  connectSelectedPort: () => Promise<void>;
}
```

From src/features/device/deviceConnectionApi.ts:
```typescript
export interface SerialConnectionStatus {
  portName: string | null;
  connected: boolean;
  status: CommandStatus;
  updatedAtUnixMs: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define resilience/health contracts and lock behaviors with RED tests</name>
  <files>src/shared/contracts/device.ts, src/features/device/deviceConnectionApi.ts, src/features/device/recoveryFlow.test.ts, src/features/device/healthCheckFlow.test.ts</files>
  <behavior>
    - Test 1: Last successful port unplug/replug pattern triggers fast reconnect attempt, then bounded short retries, then manual-required terminal state.
    - Test 2: Manual select/connect cancels in-flight auto-recovery for the old port and prevents stale status overwrite.
    - Test 3: Health check executes exactly 3 ordered steps (visibility, support eligibility, connect+verify) and returns top-level pass/fail + per-step outcomes.
    - Test 4: Recovery and health check cannot run concurrently; second operation is rejected with deterministic info/error status.
  </behavior>
  <action>Expand shared contracts first (new operation states/codes and health-check result types) and extend API DTOs accordingly. Write failing tests in `recoveryFlow.test.ts` and `healthCheckFlow.test.ts` that encode locked CONTEXT decisions exactly: auto-recovery on remembered-port return, staged bounded retries, visible reconnecting state, manual-priority cancellation, explicit user-triggered health check, and actionable fail semantics. Do not implement UI text or component layout in this task.</action>
  <verify>
    <automated>yarn vitest run src/features/device/recoveryFlow.test.ts src/features/device/healthCheckFlow.test.ts</automated>
  </verify>
  <done>Contracts compile, both test files exist, and tests fail for expected missing implementation reasons (RED baseline established).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement controller and backend orchestration for recovery and health check</name>
  <files>src/features/device/useDeviceConnection.ts, src-tauri/src/commands/device_connection.rs, src-tauri/src/lib.rs, src/features/device/deviceConnectionApi.ts</files>
  <behavior>
    - Test 1: Controller transitions into reconnecting, applies bounded retry timing, then lands on manual-required status when recovery window expires.
    - Test 2: User manual action during reconnecting cancels retry timers/tokens and owns subsequent status transitions.
    - Test 3: Health check result model always returns PASS/FAIL summary plus step-level statuses and contextual guidance codes.
  </behavior>
  <action>Implement single-operation gate inside `createDeviceConnectionController` so manual connect, auto-recovery, and health-check are mutually exclusive; keep port list interactive while reconnecting. Use short bounded retry constants (Claude discretion) and keep them injectable for tests. Extend Rust command layer only as needed for deterministic health-check verification primitives (port visibility/support/connect+immediate-status), register new command(s) in `lib.rs`, and wire invoke adapters in `deviceConnectionApi.ts`. Keep user-facing status first; technical details remain secondary payload. No event-stream architecture and no unbounded retries.</action>
  <verify>
    <automated>yarn vitest run src/features/device/recoveryFlow.test.ts src/features/device/healthCheckFlow.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Recovery + health-check tests pass, TypeScript compiles, and controller enforces manual-priority cancellation with bounded recovery semantics.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/device/recoveryFlow.test.ts src/features/device/healthCheckFlow.test.ts`
- `yarn tsc --noEmit`
</verification>

<success_criteria>
- Unplug/replug path enters explicit reconnecting state and attempts bounded auto-recovery without app restart.
- Auto-recovery timeout ends with clear manual-next-step status instead of silent failure.
- Health check returns deterministic 3-step results with top-level PASS/FAIL and actionable failure codes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-connection-resilience-and-health/03-01-SUMMARY.md`
</output>
