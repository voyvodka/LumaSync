---
phase: 03-connection-resilience-and-health
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/features/device/deviceStatusCardMapping.test.ts
  - src/features/device/deviceStatusCard.ts
  - src/features/settings/sections/DeviceSection.tsx
  - src/locales/en/common.json
  - src/locales/tr/common.json
autonomous: true
requirements: [CONN-03, CONN-04]
must_haves:
  truths:
    - User can see reconnecting state and understand what to do if auto-recovery fails.
    - User can run health check from Device panel and read pass/fail with per-step outcomes.
    - Reconnecting and health-fail feedback remain visible until state changes.
  artifacts:
    - path: src/features/settings/sections/DeviceSection.tsx
      provides: Device panel actions for manual connect, refresh, and explicit Run Health Check.
    - path: src/features/device/deviceStatusCard.ts
      provides: Deterministic mapping from controller/health states to status card content model.
    - path: src/features/device/deviceStatusCardMapping.test.ts
      provides: Behavioral coverage for reconnecting + health-fail persistence and messaging precedence.
    - path: src/locales/en/common.json
      provides: English reconnect/health-check copy.
    - path: src/locales/tr/common.json
      provides: Turkish reconnect/health-check copy with key parity.
  key_links:
    - from: src/features/settings/sections/DeviceSection.tsx
      to: src/features/device/useDeviceConnection.ts
      via: hook state/actions (`runHealthCheck`, operation flags, status payload)
      pattern: "useDeviceConnection\(\)"
    - from: src/features/settings/sections/DeviceSection.tsx
      to: src/features/device/deviceStatusCard.ts
      via: mapped status card view model
      pattern: "buildDeviceStatusCard"
    - from: src/features/settings/sections/DeviceSection.tsx
      to: src/locales/*/common.json
      via: i18n keys for reconnecting and health-check statuses
      pattern: "device\.status|device\.healthCheck"
---

<objective>
Resilience ve health-check sonuclarini Device panelde net, sakin ve aksiyon-odakli sekilde gorunur yap.

Purpose: CONN-03/CONN-04'un kullaniciya gorunen parcasini tamamlayip reconnecting + health-fail durumlarini kalici ve anlasilir geri bildirimle sunmak.
Output: Status-card mapping utility + testleri, Device panel health-check tetikleme UI'si, EN/TR parity metinleri.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-connection-resilience-and-health/03-CONTEXT.md
@.planning/phases/03-connection-resilience-and-health/03-RESEARCH.md
@.planning/phases/03-connection-resilience-and-health/03-01-SUMMARY.md
@src/features/settings/sections/DeviceSection.tsx
@src/locales/en/common.json
@src/locales/tr/common.json

<interfaces>
Expected from Plan 03-01 (`useDeviceConnection`):
```typescript
type HealthStep = "PORT_VISIBLE" | "PORT_SUPPORTED" | "CONNECT_AND_VERIFY";
type HealthStepResult = { step: HealthStep; pass: boolean; code: string; message: string; details?: string };
type HealthCheckResult = { pass: boolean; steps: HealthStepResult[]; checkedAtUnixMs: number };

interface UseDeviceConnectionResult {
  status: string;
  statusCard: { variant: "success" | "error" | "info"; code: string; message: string; details?: string } | null;
  isScanning: boolean;
  isConnecting: boolean;
  isReconnecting?: boolean;
  isHealthChecking?: boolean;
  runHealthCheck?: () => Promise<void>;
  latestHealthCheck?: HealthCheckResult | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build status-card mapping contracts for reconnecting and health-check outcomes</name>
  <files>src/features/device/deviceStatusCardMapping.test.ts, src/features/device/deviceStatusCard.ts</files>
  <behavior>
    - Test 1: Reconnecting state maps to info variant with user-first message and manual fallback guidance.
    - Test 2: Health-check FAIL maps to persistent error state with top-level fail summary and step-aware next action.
    - Test 3: When multiple statuses exist, mapping precedence keeps active operation visibility (reconnecting/health-check) over stale older cards.
  </behavior>
  <action>Create a pure mapping module (`deviceStatusCard.ts`) that receives controller state + latest health-check result and returns title/body/detail keys plus visual variant. Write RED tests first in `deviceStatusCardMapping.test.ts` for reconnecting persistence, fail persistence, and precedence rules. Keep mapping free of React concerns so it remains unit-testable and reusable.</action>
  <verify>
    <automated>yarn vitest run src/features/device/deviceStatusCardMapping.test.ts</automated>
  </verify>
  <done>Mapping utility exists, tests are green, and status precedence logic is deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Device panel health-check trigger and EN/TR copy parity</name>
  <files>src/features/settings/sections/DeviceSection.tsx, src/locales/en/common.json, src/locales/tr/common.json</files>
  <action>Update DeviceSection to expose explicit `Run Health Check` action in the existing action area (user decision), bind disable rules to single-operation gate (`isScanning`/`isConnecting`/`isReconnecting`/`isHealthChecking`), and render status card using mapping utility from Task 1. Keep port list interactive during recovery, and do not auto-dismiss reconnecting/health-fail feedback. Add EN/TR keys for reconnecting, health check in-progress, health PASS/FAIL summary, and actionable fail guidance with exact key parity between locale files.</action>
  <verify>
    <automated>yarn vitest run src/features/device/deviceStatusCardMapping.test.ts && yarn tsc --noEmit</automated>
  </verify>
  <done>Device panel can trigger health check, status messaging follows calm action-oriented tone, and EN/TR locale files contain parity keys for all new states.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/device/deviceStatusCardMapping.test.ts`
- `yarn tsc --noEmit`
</verification>

<success_criteria>
- Device panel includes explicit health-check trigger and user can run it during setup.
- Reconnecting and health-fail statuses remain visible until state changes, with actionable guidance.
- EN/TR translation keys for new resilience/health states remain one-to-one parity.
</success_criteria>

<output>
After completion, create `.planning/phases/03-connection-resilience-and-health/03-02-SUMMARY.md`
</output>
