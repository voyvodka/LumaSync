---
phase: 06-runtime-quality-controls
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands/runtime_quality.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [QUAL-01, QUAL-02]
must_haves:
  truths:
    - "Kullanici ani renk degisimlerinde tek-frame sert sicrama yerine yumusatilmis gecis gorur (QUAL-01)."
    - "Runtime, yuk arttiginda sabit 16ms donguye saplanmak yerine gozlenen maliyete gore gonderim araligini adapte eder (QUAL-02)."
    - "Capture patlamasi oldugunda backlog birikmez; sadece en guncel frame gonderim icin bekletilir (QUAL-02)."
  artifacts:
    - path: "src-tauri/src/commands/runtime_quality.rs"
      provides: "Smoothing + adaptive pacing + coalescing icin saf kalite kontrol katmani"
      exports: ["RuntimeQualityConfig", "RuntimeQualityController", "RuntimeFrameSlot"]
    - path: "src-tauri/src/lib.rs"
      provides: "Yeni runtime_quality modulunun tauri command katmani ile ayni namespace altinda derlenmesi"
      contains: "pub mod runtime_quality"
  key_links:
    - from: "src-tauri/src/commands/runtime_quality.rs"
      to: "src-tauri/src/commands/lighting_mode.rs"
      via: "ambilight worker loop quality kontrol entegrasyonu"
      pattern: "RuntimeQualityController|RuntimeFrameSlot"
    - from: "src-tauri/src/commands/runtime_quality.rs"
      to: "06-VALIDATION.md"
      via: "QUAL-01/QUAL-02 test map komutlari"
      pattern: "smoothes_step_changes|adapts_interval_under_pressure|coalesces_to_latest_frame"
---

<objective>
Phase 6 icin gerekli runtime kalite kontrol temelini olusturmak: smoothing, adaptif pacing ve latest-frame coalescing kurallarini ayri bir saf Rust modulunde test-backed hale getirmek.

Purpose: QUAL-01 ve QUAL-02 davranislarini UI'dan bagimsiz, deterministik ve birim testle dogrulanabilir bir cekirdekte kilitlemek.
Output: Yeni `runtime_quality.rs` modulunde kalite kontrol API'si + Wave 0 testleri + `lib.rs` modul kaydi.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-runtime-quality-controls/06-RESEARCH.md
@.planning/phases/06-runtime-quality-controls/06-VALIDATION.md
@.planning/phases/05-core-lighting-modes/05-05-SUMMARY.md
@src-tauri/src/commands/lighting_mode.rs
@src-tauri/src/commands/led_output.rs

<interfaces>
From src-tauri/src/commands/lighting_mode.rs:
```rust
fn start_ambilight_worker(
    output_bridge: LedOutputBridge,
    port_name: String,
    brightness: f32,
    mut frame_source: Box<dyn AmbilightFrameSource>,
) -> Result<LightingWorkerRuntime, String>;
```

From src-tauri/src/commands/ambilight_capture.rs:
```rust
pub struct SampledLedFrame {
    pub colors: Vec<[u8; 3]>,
}

pub fn sample_led_frame(
    frame: &CapturedFrame,
    calibration: &SamplingCalibration,
) -> Result<SampledLedFrame, AmbilightCaptureError>;
```

From src-tauri/src/commands/led_output.rs:
```rust
pub fn send_ambilight_frame_to_port(
    bridge: &LedOutputBridge,
    port_name: &str,
    frame: &[[u8; 3]],
    brightness: f32,
) -> Result<(), LedOutputError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runtime quality kontratlarini ve Wave 0 test iskeletini olustur</name>
  <files>src-tauri/src/commands/runtime_quality.rs</files>
  <behavior>
    - Test 1: `smoothes_step_changes` ani adim degisimde cikisin ara degerlerle yumusadigini dogrular.
    - Test 2: `resets_on_led_count_change` LED sayisi degisince onceki buffer'in deterministik resetlendigini dogrular.
    - Test 3: `adapts_interval_under_pressure` gozlenen maliyet artinca interval'in buyudugunu dogrular.
    - Test 4: `coalesces_to_latest_frame` burst altinda yalniz son frame'in korunup queue birikmedigini dogrular.
  </behavior>
  <action>`runtime_quality.rs` dosyasini olustur ve yalnizca kalite kontroluna odakli saf API kontratlarini tanimla: `RuntimeQualityConfig`, `RuntimeQualityController`, `RuntimeFrameSlot`, `RuntimeTimingSample`. Task sonunda test isimleri `06-VALIDATION.md` ile birebir uyumlu olsun; rastgele test adi veya kapsami disina tasma yapma.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml runtime_quality::tests:: -- --nocapture</automated>
  </verify>
  <done>Kalite kontrol API'si ve QUAL-01/QUAL-02 Wave 0 test isimleri dosyada mevcuttur ve test suite compile edilir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoothing ve adaptif pacing/coalescing davranisini uygula</name>
  <files>src-tauri/src/commands/runtime_quality.rs</files>
  <behavior>
    - Test 1: EMA/lerp tabanli smoothing her LED icin bir onceki frame'i kullanir ve tek adimda hedefe ziplamaz.
    - Test 2: Basinc (capture+send maliyeti) arttiysa adaptive interval taban intervalden buyuk hesaplanir ve tanimli min/max araliginda clamp edilir.
    - Test 3: Frame slot ard arda push aldiginda en son frame disindakileri atar.
  </behavior>
  <action>Task 1'de yazilan testleri green edecek sekilde `RuntimeQualityController` implement et: per-LED smoothing (`alpha` clamp 0..1), EWMA tabanli maliyet gozlemi, `Instant` farkina dayali `should_send_now` kapisi, latest-frame coalescing slot. Her hesap deterministic olmali; thread sleep'e bagli rassal davranis ekleme.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml runtime_quality::tests:: -- --nocapture</automated>
  </verify>
  <done>QUAL-01 ve QUAL-02 davranislari pure logic seviyesinde test-backed ve tekrar edilebilir hale gelir.</done>
</task>

<task type="auto">
  <name>Task 3: runtime_quality modulunu backend derleme zincirine kaydet</name>
  <files>src-tauri/src/lib.rs</files>
  <action>`src-tauri/src/lib.rs` icindeki `mod commands` bloguna `pub mod runtime_quality;` ekle. Baska command registration davranisina dokunma; bu taskin amaci yeni modulu Phase 6 sonrasi planlarin kullanabilecegi sekilde derlenebilir hale getirmek.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml runtime_quality::tests:: -- --nocapture</automated>
  </verify>
  <done>Yeni kalite modulu proje icinde resmi komut/modul agacina dahil olur ve test hedefi kesintisiz calisir.</done>
</task>

</tasks>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml runtime_quality::tests:: -- --nocapture`
- `06-VALIDATION.md` icindeki QUAL-01/QUAL-02 test adlariyla birebir eslesme
</verification>

<success_criteria>
- `runtime_quality.rs` QUAL-01 ve QUAL-02 icin pure logic kalite kontrol katmanini sunar.
- Wave 0 olarak isaretlenen testler artik dosya-ustunde mevcut ve otomasyonla kosulabilir durumdadir.
- Sonraki planlar `lighting_mode` entegrasyonunu bu kontratla dogrudan yapabilir.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-quality-controls/06-01-SUMMARY.md`
</output>
