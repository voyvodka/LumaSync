---
phase: 06-runtime-quality-controls
plan: "02"
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src-tauri/src/commands/lighting_mode.rs
  - src-tauri/src/commands/led_output.rs
  - src-tauri/src/commands/runtime_quality.rs
  - .planning/phases/06-runtime-quality-controls/06-VALIDATION.md
autonomous: true
requirements: [QUAL-01, QUAL-02]
must_haves:
  truths:
    - "Kullanici Ambilight acikken ani sahne degisimlerinde sert flicker yerine yumusak gecis gorur (QUAL-01)."
    - "Kullanici normal masaustu kullaniminda runtime'in sabit FPS zorlamasi yerine yuk durumuna gore dengeli gonderim yaptigini hisseder (QUAL-02)."
    - "Ambilight capture hizi artip azalsa da runtime backlog biriktirmeden stabil cikis korur (QUAL-02)."
  artifacts:
    - path: "src-tauri/src/commands/lighting_mode.rs"
      provides: "Ambilight worker icinde quality controller entegrasyonu ve adaptif send gate"
      exports: ["set_lighting_mode", "stop_lighting", "get_lighting_mode_status"]
    - path: "src-tauri/src/commands/led_output.rs"
      provides: "Ambilight hot-path icin tekrarli port-open maliyetini azaltan gonderim yolu"
      contains: "send_packet_to_port"
    - path: ".planning/phases/06-runtime-quality-controls/06-VALIDATION.md"
      provides: "Nyquist/Wave 0 durumunun plan sonucuna gore guncel dogrulama durumu"
  key_links:
    - from: "src-tauri/src/commands/lighting_mode.rs"
      to: "src-tauri/src/commands/runtime_quality.rs"
      via: "capture->smooth->coalesce->adaptive-send pipeline"
      pattern: "RuntimeQualityController|should_send_now|RuntimeFrameSlot"
    - from: "src-tauri/src/commands/lighting_mode.rs"
      to: "src-tauri/src/commands/led_output.rs"
      via: "ambilight frame packet gonderim hot path"
      pattern: "send_ambilight_frame_to_port|send_packet_to_port"
    - from: ".planning/phases/06-runtime-quality-controls/06-VALIDATION.md"
      to: "src-tauri/src/commands/runtime_quality.rs"
      via: "QUAL-01/02 otomatik test coverage kaydi"
      pattern: "runtime_quality::tests::"
---

<objective>
Phase 6 kalite kontrol cekirdegini canli Ambilight worker akisina baglayip sabit 16ms donguyu adaptive gating/coalescing ile degistirmek ve runtime dogrulama durumunu Nyquist uyumlu hale getirmek.

Purpose: QUAL-01 ve QUAL-02 gereksinimlerini gercek runtime davranisina tasiyarak perceptual yumusaklik + sistem yuk dengelemesini ayni anda saglamak.
Output: `lighting_mode.rs` icinde quality-aware worker loop, `led_output.rs` hot-path iyilestirmesi ve guncel `06-VALIDATION.md`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-runtime-quality-controls/06-RESEARCH.md
@.planning/phases/06-runtime-quality-controls/06-VALIDATION.md
@.planning/phases/06-runtime-quality-controls/06-01-SUMMARY.md
@.planning/phases/05-core-lighting-modes/05-06-SUMMARY.md
@src-tauri/src/commands/runtime_quality.rs
@src-tauri/src/commands/lighting_mode.rs
@src-tauri/src/commands/led_output.rs

<interfaces>
From src-tauri/src/commands/runtime_quality.rs (Plan 06-01 output):
```rust
pub struct RuntimeQualityController { /* stateful smoothing + pacing */ }
pub struct RuntimeFrameSlot { /* latest-frame coalescing */ }

impl RuntimeQualityController {
    pub fn smooth(&mut self, next: &[[u8; 3]]) -> Vec<[u8; 3]>;
    pub fn observe_timing(&mut self, capture_ms: f32, send_ms: f32);
    pub fn current_send_interval(&self) -> std::time::Duration;
    pub fn should_send_now(&self, now: std::time::Instant) -> bool;
}
```

From src-tauri/src/commands/lighting_mode.rs:
```rust
fn start_ambilight_worker(
    output_bridge: LedOutputBridge,
    port_name: String,
    brightness: f32,
    frame_source: Box<dyn AmbilightFrameSource>,
) -> Result<LightingWorkerRuntime, String>;
```

From src-tauri/src/commands/led_output.rs:
```rust
pub fn send_ambilight_frame_to_port(
    bridge: &LedOutputBridge,
    port_name: &str,
    frame: &[[u8; 3]],
    brightness: f32,
) -> Result<(), LedOutputError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ambilight worker loop'u quality controller ile yeniden kablola</name>
  <files>src-tauri/src/commands/lighting_mode.rs, src-tauri/src/commands/runtime_quality.rs</files>
  <behavior>
    - Test 1: Worker capture ettigi frame'i once smoothing'den gecirip send gate acildiginda gonderir.
    - Test 2: `should_send_now` false iken frame queue buyumez; yalniz latest frame tutulur.
    - Test 3: Islem maliyeti arttiginda bir sonraki gonderim araligi adaptif olarak buyur.
  </behavior>
  <action>`lighting_mode.rs` icindeki `start_ambilight_worker` ve dongu akisina `RuntimeQualityController` + `RuntimeFrameSlot` ekle: fixed `thread::sleep(16)` yerine `Instant` tabanli gate kullan, capture/sampling sonucu frame'leri latest-slot'ta coalesce et, gate acildiginda en guncel frame'i gonder. Mevcut transaction semantigini (stop_previous -> start_next) bozma ve `AMBILIGHT_MODE_START_FAILED` kod semantigini koru.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml lighting_mode::tests:: -- --nocapture</automated>
  </verify>
  <done>Ambilight runtime fixed-rate donguden quality-aware adaptive loop'a gecmis olur ve QUAL-01/QUAL-02 davranislari worker seviyesinde testle korunur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LED output hot-path maliyetini azalt ve send jitter'i dusur</name>
  <files>src-tauri/src/commands/led_output.rs, src-tauri/src/commands/lighting_mode.rs</files>
  <behavior>
    - Test 1: Ambilight ard arda frame gonderiminde gereksiz tekrarli port-open maliyeti azaltilmis yoldan ilerler.
    - Test 2: Solid mode davranisi ve packet encoding kurallari regress etmez.
    - Test 3: Port/send hatalari mevcut kodlu reason formatini korur.
  </behavior>
  <action>`led_output.rs` icinde Ambilight hot-path icin yeniden kullanilabilir gonderim stratejisi ekle (session/reuse odakli), `lighting_mode.rs` worker'ini bu yola bagla. `apply_solid_payload_to_port` davranisini degistirme; MODE-02 stabilitesi korunmali. Hata kodlarini (`LED_OUTPUT_*`) yeni patika icinde de ayni formatta yansit.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml led_output::tests:: -- --nocapture && cargo test --manifest-path src-tauri/Cargo.toml lighting_mode::tests:: -- --nocapture</automated>
  </verify>
  <done>Ambilight send hot-path daha dusuk ac/kapat maliyetiyle calisir, kodlu hata semantigi korunur, solid mode regress etmez.</done>
</task>

<task type="auto">
  <name>Task 3: Validation durumunu Wave 0 kapanisina gore guncelle</name>
  <files>.planning/phases/06-runtime-quality-controls/06-VALIDATION.md</files>
  <action>Plan 06-01 ve bu plan sonrasi `06-VALIDATION.md` dosyasinda Wave 0 ve Nyquist alanlarini gercek test durumuna gore guncelle: mevcut test dosyalari olusturulduysa `File Exists` ve checklist satirlarini tamamla, `nyquist_compliant` ve `wave_0_complete` degerlerini uygun sekilde true yap. Komutlari watch-mode olmadan net ve kisa tut.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml runtime_quality::tests:: -- --nocapture && cargo test --manifest-path src-tauri/Cargo.toml lighting_mode::tests:: -- --nocapture</automated>
  </verify>
  <done>Phase 06 validation kontrati, uygulanan kod ve test reality'siyle uyumlu ve Nyquist checklisti kapatilmis durumdadir.</done>
</task>

</tasks>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml runtime_quality::tests:: -- --nocapture`
- `cargo test --manifest-path src-tauri/Cargo.toml lighting_mode::tests:: -- --nocapture`
- `cargo test --manifest-path src-tauri/Cargo.toml led_output::tests:: -- --nocapture`
- `06-VALIDATION.md` frontmatter alanlari (`nyquist_compliant`, `wave_0_complete`) ve task map satirlari kodla tutarli
</verification>

<success_criteria>
- Ambilight worker loop sabit-rate yaklasimdan quality-aware adaptive yaklasima gecer.
- Burst frame durumunda backlog birikimi yerine latest-frame coalescing korunur.
- Runtime kalite testleri + lighting/output regression testleri birlikte green kalir.
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-quality-controls/06-02-SUMMARY.md`
</output>
