---
phase: 01-app-shell-and-baseline-defaults
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/App.tsx
  - src/features/settings/SettingsLayout.tsx
  - src/features/settings/sections/GeneralSection.tsx
  - src/features/settings/sections/StartupTraySection.tsx
  - src/features/settings/sections/AboutLogsSection.tsx
  - src/features/settings/sections/DeviceSection.tsx
  - src/features/persistence/shellStore.ts
  - docs/manual/phase-01-tray-checklist.md
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "User can open a full settings window with sidebar + content layout from tray"
    - "Closing and reopening settings restores safe window state and last visited section"
    - "First close-to-tray action is explained once with a persisted educational hint"
  artifacts:
    - path: "src/features/settings/SettingsLayout.tsx"
      provides: "Phase 1 settings scaffold with required baseline sections"
    - path: "src/features/persistence/shellStore.ts"
      provides: "Persisted shell state for window geometry, section, startup preference, and hint flag"
    - path: "docs/manual/phase-01-tray-checklist.md"
      provides: "Repeatable manual tray UX checklist for Phase 1 validation"
  key_links:
    - from: "src/app/App.tsx"
      to: "src/features/settings/SettingsLayout.tsx"
      via: "Settings shell mount and section routing"
      pattern: "SettingsLayout"
    - from: "src/features/shell/windowLifecycle.ts"
      to: "src/features/persistence/shellStore.ts"
      via: "Load/save shell state and one-time hint"
      pattern: "loadShellState|saveShellState|trayHintSeen"
---

<objective>
Build the Phase 1 settings shell scaffold and persistence layer on top of the tray/runtime contracts from Plan 01.

Purpose: Complete UX-01 interaction polish (settings structure + state restore) while keeping lifecycle wiring and UI state concerns in separate execution scopes.
Output: Sidebar settings shell sections, persisted shell state handling, and manual tray validation checklist.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-CONTEXT.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-RESEARCH.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement settings shell layout and Phase 1 baseline sections</name>
  <files>src/app/App.tsx, src/features/settings/SettingsLayout.tsx, src/features/settings/sections/GeneralSection.tsx, src/features/settings/sections/StartupTraySection.tsx, src/features/settings/sections/AboutLogsSection.tsx, src/features/settings/sections/DeviceSection.tsx</files>
  <action>Create the sidebar + content settings layout with required Phase 1 sections (General, Startup/Tray, Language slot in navigation, About/Logs, Device) and wire it into app shell bootstrap. Keep interaction no-reload SPA style and preserve quiet feedback defaults (no noisy notifications by default). Do not implement deferred features such as auto-update or active LED shutdown-on-quit logic.</action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Settings opens with the required sidebar information architecture and section rendering baseline ready for Language integration in the next plan.</done>
</task>

<task type="auto">
  <name>Task 2: Add shell state persistence, monitor-bounds guard, and tray smoke checklist</name>
  <files>src/features/persistence/shellStore.ts, src/app/App.tsx, docs/manual/phase-01-tray-checklist.md</files>
  <action>Implement shell state persistence for last visited section, window size/position, startup preference, and one-time close-to-tray educational hint flag. On reopen, restore state and apply monitor-bounds validation that recenters if saved coordinates are off-screen. Write a deterministic manual checklist covering launch, close-to-tray, reopen, and single-instance focus behavior for UX-01 verification.</action>
  <verify>
    <automated>node scripts/verify/phase01-shell-contracts.mjs</automated>
  </verify>
  <done>Shell state reliably restores with safe positioning and the manual checklist exists to validate tray behavior end-to-end.</done>
</task>

</tasks>

<verification>
- Run `node scripts/verify/phase01-shell-contracts.mjs`.
- Run `yarn tsc --noEmit`.
- Follow `docs/manual/phase-01-tray-checklist.md` while running `yarn tauri dev` to confirm UX-01 flows.
</verification>

<success_criteria>
- UX-01 settings surface is available with required Phase 1 sections and reliable reopen behavior.
- Shell persistence restores safe state and avoids off-screen window regressions.
- Manual validation artifact exists and is repeatable for tray lifecycle smoke checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-baseline-defaults/01-03-SUMMARY.md`
</output>
