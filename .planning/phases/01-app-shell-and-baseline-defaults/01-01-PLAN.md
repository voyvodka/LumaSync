---
phase: 01-app-shell-and-baseline-defaults
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
  - src/features/tray/trayController.ts
  - src/features/shell/windowLifecycle.ts
  - src/shared/contracts/shell.ts
  - scripts/verify/phase01-shell-contracts.mjs
autonomous: true
requirements: [UX-01]
must_haves:
  truths:
    - "User can launch the app and keep it running in system tray"
    - "User can reopen/focus settings from tray menu at any time"
    - "Second launch focuses the running instance instead of starting a duplicate"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Single-instance, tray menu wiring, and show/focus lifecycle hooks"
    - path: "src/features/tray/trayController.ts"
      provides: "Frontend bridge for Open Settings and Quit tray actions"
    - path: "src/shared/contracts/shell.ts"
      provides: "Canonical tray menu IDs and shell state contract exports"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src/features/tray/trayController.ts"
      via: "Open Settings and Quit tray actions"
      pattern: "open-settings|quit"
    - from: "src/features/shell/windowLifecycle.ts"
      to: "src/features/persistence/shellStore.ts"
      via: "Persist/restore window size, position, and last section"
      pattern: "loadShellState|saveShellState"
---

<objective>
Build the Phase 1 tray-first runtime shell baseline so lifecycle and tray menu behavior are reliable from first run.

Purpose: Deliver UX-01 shell fundamentals first, then let a follow-up plan layer settings scaffold and persistence without overloading one execution context.
Output: Working tray/menu/single-instance shell plus canonical shell contracts and structure verifier.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-CONTEXT.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shell contracts and automated structure verifier</name>
  <files>src/shared/contracts/shell.ts, scripts/verify/phase01-shell-contracts.mjs, package.json</files>
  <action>Create exported contracts for tray menu IDs, settings sidebar section IDs (General, Startup/Tray, Language, About/Logs, Device), and persisted shell state fields (window geometry, last section, one-time tray hint flag, startup preference). Add a Node verifier script that fails if required exports or section IDs are missing, so later tasks have a deterministic automated gate instead of manual file inspection.</action>
  <verify>
    <automated>node scripts/verify/phase01-shell-contracts.mjs</automated>
  </verify>
  <done>Contract file is the single source of truth for shell IDs/state shape and verifier exits 0 only when all required shell contracts exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tray lifecycle, single-instance, and close-to-tray behavior</name>
  <files>src-tauri/src/lib.rs, src-tauri/capabilities/default.json, src/features/tray/trayController.ts, src/features/shell/windowLifecycle.ts</files>
  <action>Wire Tauri v2 plugins/patterns from research: register single-instance first, build tray menu entries (Open Settings, Status Indicator, Startup Toggle, Quit), route every open/focus path through one show-and-focus function, and intercept close requests to hide window to tray. Keep quit immediate and safe (no deferred LED shutdown workflow in this phase). Ensure startup toggle uses official autostart permission entries in capabilities.</action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>Second launch focuses existing instance, close action hides to tray, and tray menu actions compile with no type/build errors.</done>
</task>

</tasks>

<verification>
- Run `node scripts/verify/phase01-shell-contracts.mjs`.
- Run `cargo check --manifest-path src-tauri/Cargo.toml`.
- Run `cargo check --manifest-path src-tauri/Cargo.toml && yarn tsc --noEmit`.
</verification>

<success_criteria>
- UX-01 shell behavior is implemented with tray-first lifecycle, single-instance focus, and reliable settings reopen path.
- Plan scope stays focused on shell/runtime lifecycle so follow-up plan can implement settings scaffold/persistence without context degradation.
- All automated verification commands pass without introducing deferred/out-of-scope capabilities.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-baseline-defaults/01-01-SUMMARY.md`
</output>
