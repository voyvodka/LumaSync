---
phase: 01-app-shell-and-baseline-defaults
plan: 04
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/tauri.conf.json
  - src/features/tray/trayController.ts
  - src/features/settings/sections/StartupTraySection.tsx
  - docs/manual/phase-01-tray-checklist.md
autonomous: true
gap_closure: true
requirements: [UX-01]
must_haves:
  truths:
    - "Uygulama acildiginda tek bir tray icon gorunur"
    - "Startup toggle uygulama ici ve tray menu arasinda cift yonlu senkron kalir"
    - "macOS'ta fullscreen modunda pencere kapatilip tray'e alindiginda siyah ekran artefakti olusmaz"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Tek tray stratejisi, startup checkmark senkronu, fullscreen-aware close-to-tray akisi"
    - path: "src-tauri/tauri.conf.json"
      provides: "Config/runtime tray cakismasini onleyen tek tray kaynagi"
    - path: "src/features/tray/trayController.ts"
      provides: "Frontend->tray checkmark senkron kancasi"
    - path: "src/features/settings/sections/StartupTraySection.tsx"
      provides: "Ilk yuklemede ve UI toggle'da tray checkmark ile autostart state hizalama"
  key_links:
    - from: "src/features/settings/sections/StartupTraySection.tsx"
      to: "src/features/tray/trayController.ts"
      via: "toggle/init sonrasi tray checkmark sync"
      pattern: "sync.*startup|set.*checked"
    - from: "src/features/tray/trayController.ts"
      to: "src-tauri/src/lib.rs"
      via: "invoke veya event ile tray CheckMenuItem checked degeri guncelleme"
      pattern: "invoke|emit"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/tauri.conf.json"
      via: "TrayIconBuilder tek kaynak, app.trayIcon pasif"
      pattern: "TrayIconBuilder|trayIcon"
---

<objective>
Close all diagnosed Phase 01 UAT gaps so tray lifecycle behavior is deterministic on macOS and startup state never desynchronizes between UI and tray.

Purpose: Restore UX-01 runtime reliability by removing duplicate tray creation, enforcing bidirectional startup-toggle sync, and making close-to-tray fullscreen-aware.
Output: Single-tray runtime, synchronized startup toggle behavior, and fullscreen-safe close interception with updated manual verification steps.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-UAT.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-01-SUMMARY.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-03-SUMMARY.md

<interfaces>
From src/features/tray/trayController.ts:
```ts
export async function toggleStartup(): Promise<boolean>;
export async function getStartupEnabled(): Promise<boolean>;
export async function listenStartupToggle(onToggle: (newState: boolean) => void): Promise<UnlistenFn>;
```

From src/features/settings/sections/StartupTraySection.tsx:
```tsx
const [startupEnabled, setStartupEnabled] = useState(false);
const enabled = await getStartupEnabled();
const newState = await toggleStartup();
```

From src-tauri/src/lib.rs:
```rust
fn build_tray_menu<R: Runtime>(app: &AppHandle<R>) -> tauri::Result<Menu<R>>;
// startup-toggle currently forwards event: app.emit("tray:startup-toggle-clicked", ())
// close interception currently always: prevent_close + hide
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate tray source and enforce single tray strategy</name>
  <files>src-tauri/tauri.conf.json, src-tauri/src/lib.rs</files>
  <action>Keep runtime TrayIconBuilder as the only tray creator and remove `app.trayIcon` configuration from `tauri.conf.json` so macOS no longer renders two icons. Preserve existing tray menu IDs and behavior (Open Settings, Status Indicator, Startup Toggle, Quit) and keep single-instance + open/focus flow unchanged.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const c=JSON.parse(fs.readFileSync('src-tauri/tauri.conf.json','utf8')); if (c.app && Object.prototype.hasOwnProperty.call(c.app,'trayIcon')) { process.exit(1); }"</automated>
  </verify>
  <done>`tauri.conf.json` no longer defines `app.trayIcon`, and tray creation is sourced only from Rust runtime builder.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bidirectional startup-toggle sync between UI and tray checkmark</name>
  <files>src-tauri/src/lib.rs, src/features/tray/trayController.ts, src/features/settings/sections/StartupTraySection.tsx</files>
  <action>Add a bridge from frontend to Rust that explicitly sets tray `startup-toggle` CheckMenuItem checked state after every autostart state change. Initialize tray checkmark on app load from `isEnabled()` (via existing frontend init path), and after UI-originated toggle update both React state and tray checkmark from the same resulting boolean. Do not rely on `checked=false` defaults or implicit tray toggling behavior.</action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Tray checkmark and in-app toggle always reflect the same autostart boolean on startup, tray-click, and UI-click paths.</done>
</task>

<task type="auto">
  <name>Task 3: Make close-to-tray fullscreen-aware on macOS and refresh manual checklist</name>
  <files>src-tauri/src/lib.rs, docs/manual/phase-01-tray-checklist.md</files>
  <action>Update close interception in Rust: on macOS, if the main window is fullscreen when close is requested, first exit fullscreen (`set_fullscreen(false)`), then continue hide-to-tray flow and emit `shell:close-to-tray`. Keep non-macOS behavior unchanged. Extend the manual checklist with an explicit fullscreen-close regression step so this scenario is always re-tested.</action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>Fullscreen close no longer leaves black-screen artifacts on macOS, and checklist includes this regression case.</done>
</task>

</tasks>

<verification>
- Run `node -e "const fs=require('node:fs'); const c=JSON.parse(fs.readFileSync('src-tauri/tauri.conf.json','utf8')); if (c.app && Object.prototype.hasOwnProperty.call(c.app,'trayIcon')) { process.exit(1); }"`.
- Run `yarn tsc --noEmit`.
- Run `cargo check --manifest-path src-tauri/Cargo.toml`.
- Manual: run `yarn tauri dev` and execute updated fullscreen-close + startup-sync checklist entries.
</verification>

<success_criteria>
- UAT gap "duplicate tray icon" is closed: only one tray icon appears at app launch.
- UAT gap "startup desync" is closed: tray and UI toggles remain in sync in both directions.
- UAT gap "fullscreen black screen" is closed: macOS fullscreen close transitions cleanly to tray without artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-baseline-defaults/01-04-SUMMARY.md`
</output>
