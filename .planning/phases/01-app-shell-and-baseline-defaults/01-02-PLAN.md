---
phase: 01-app-shell-and-baseline-defaults
plan: 02
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - .planning/phases/01-app-shell-and-baseline-defaults/01-I18N-ALIGNMENT.md
  - src/features/i18n/languagePolicy.ts
  - src/features/i18n/i18n.ts
  - src/features/i18n/default-language.test.ts
  - src/features/settings/sections/LanguageSection.tsx
  - src/locales/en/common.json
  - src/locales/tr/common.json
  - src/app/providers.tsx
  - vitest.config.ts
autonomous: true
requirements: [I18N-02]
must_haves:
  truths:
    - "User sees English on first launch with no saved language"
    - "User can switch language immediately from Settings > Language"
    - "Missing locale keys fall back to English"
  artifacts:
    - path: "src/features/i18n/languagePolicy.ts"
      provides: "Deterministic first-launch language selection policy"
    - path: "src/features/settings/sections/LanguageSection.tsx"
      provides: "Runtime language selector UI in required settings location"
    - path: "src/features/i18n/default-language.test.ts"
      provides: "Automated proof for first-launch default and persisted override"
  key_links:
    - from: "src/features/settings/sections/LanguageSection.tsx"
      to: "src/features/i18n/i18n.ts"
      via: "Immediate changeLanguage invocation"
      pattern: "changeLanguage"
    - from: "src/features/i18n/languagePolicy.ts"
      to: "src/features/persistence/shellStore.ts"
      via: "Read/write persisted language key"
      pattern: "language"
---

<objective>
Implement Phase 1 localization baseline with a deterministic first-launch language behavior and explicit requirement-alignment handling.

Purpose: Fulfill I18N-02 now while preserving a clean switch-point for the user's preferred system-locale default in a later requirement update.
Output: Runtime i18n wiring, Language settings section, translation resources, and automated tests for first-launch language behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-CONTEXT.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-RESEARCH.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create explicit Phase 1 language policy and alignment note</name>
  <files>.planning/phases/01-app-shell-and-baseline-defaults/01-I18N-ALIGNMENT.md, src/features/i18n/languagePolicy.ts</files>
  <behavior>
    - Test 1: No saved language on first launch resolves to `en` to satisfy I18N-02.
    - Test 2: If a saved language exists, policy returns the saved language.
    - Test 3: Policy includes a clear switch-point to system-locale default once requirements are revised.
  </behavior>
  <action>Implement a dedicated `resolveInitialLanguage()` policy that enforces English-first now (requirements authority) while documenting the conflict with context preference (`system locale`) in `01-I18N-ALIGNMENT.md`. The alignment note must map source-of-truth precedence: REQUIREMENTS/ROADMAP acceptance criteria win for Phase 1, with future change path documented for later phase/requirement update.</action>
  <verify>
    <automated>yarn vitest run src/features/i18n/default-language.test.ts -t "first-launch"</automated>
  </verify>
  <done>Language policy is deterministic, conflict handling is explicitly documented, and tests prove I18N-02-compliant first-launch behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire i18next runtime and Settings > Language immediate apply flow</name>
  <files>src/features/i18n/i18n.ts, src/features/settings/sections/LanguageSection.tsx, src/locales/en/common.json, src/locales/tr/common.json, src/app/providers.tsx</files>
  <action>Initialize i18next with English fallback, register EN/TR resources, and mount provider in app bootstrap. Build Language section under settings sidebar location (from Plan 01 contract) with immediate apply behavior (`changeLanguage` without restart). Persist selection in store and ensure missing key fallback resolves to English. Do not introduce full localization scope beyond the baseline strings needed for shell sections.</action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>User can change language from Settings > Language instantly, and untranslated keys display English fallback safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add i18n verification harness for first-launch and persisted override</name>
  <files>vitest.config.ts, src/features/i18n/default-language.test.ts</files>
  <behavior>
    - Test 1: Fresh state -> `resolveInitialLanguage()` returns `en`.
    - Test 2: Persisted `tr` -> `resolveInitialLanguage()` returns `tr`.
    - Test 3: Unknown locale key falls back to `en` resources.
  </behavior>
  <action>Create Vitest config (if absent) and implement focused tests for the language policy and fallback behavior. Keep tests fast (<60s) and deterministic by mocking persistence reads instead of invoking full Tauri runtime.</action>
  <verify>
    <automated>yarn vitest run src/features/i18n/default-language.test.ts</automated>
  </verify>
  <done>Automated tests exist and pass for first-launch English default, persisted override, and fallback logic.</done>
</task>

</tasks>

<verification>
- Run `yarn vitest run src/features/i18n/default-language.test.ts`.
- Run `yarn tsc --noEmit`.
- Confirm `01-I18N-ALIGNMENT.md` explicitly records conflict resolution and future migration path.
</verification>

<success_criteria>
- I18N-02 is satisfied by deterministic English default on first launch.
- Context preference conflict is explicitly handled and documented with a future switch strategy.
- Language UI is functional in the required settings location and fallback behavior is verified automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-baseline-defaults/01-02-SUMMARY.md`
</output>
