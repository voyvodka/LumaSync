---
phase: 02-usb-connection-setup
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/commands/device_connection.rs
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
autonomous: true
requirements: [CONN-01, CONN-02]
must_haves:
  truths:
    - "Uygulama desteklenen USB serial cihazlari metadata ile listeleyebilir."
    - "Kullanici secili herhangi bir port icin explicit connect denemesi yapabilir."
    - "Baglanti denemesi sonucu kodlu ve insan-okunur durum olarak frontend'e donebilir."
  artifacts:
    - path: "src-tauri/src/commands/device_connection.rs"
      provides: "Serial listeleme ve explicit connect command'lari"
      exports: ["list_serial_ports", "connect_serial_port", "get_serial_connection_status"]
    - path: "src-tauri/Cargo.toml"
      provides: "serialport crate bagimliligi"
      contains: "serialport"
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri command registration"
      contains: "generate_handler"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/commands/device_connection.rs"
      via: "tauri::generate_handler registration"
      pattern: "list_serial_ports|connect_serial_port|get_serial_connection_status"
    - from: "src-tauri/src/commands/device_connection.rs"
      to: "serialport::available_ports"
      via: "port enumeration"
      pattern: "available_ports"
---

<objective>
Rust/Tauri tarafinda Device panelinin kullanacagi serial listeleme ve baglanti command omurgasini kur.

Purpose: Frontend baglanti akisi runtime IO detaylarini bilmeden command API ile ilerleyebilsin.
Output: Command modulu, dependency/capability guncellemeleri, invoke edilebilir backend surface.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-usb-connection-setup/02-CONTEXT.md
@.planning/phases/02-usb-connection-setup/02-RESEARCH.md
@src-tauri/src/lib.rs

<interfaces>
From src-tauri/src/lib.rs:
```rust
#[tauri::command]
fn set_tray_startup_checked(checked: bool) -> Result<(), String>;

// existing command registration style
.invoke_handler(tauri::generate_handler![set_tray_startup_checked])
```

From .planning/phases/02-usb-connection-setup/02-RESEARCH.md (standard stack):
```text
- Use serialport crate with available_ports() + SerialPortType::UsbPort metadata
- Keep connect explicit (no auto-connect on selection)
- Return human-readable errors plus short technical code/details
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Device connection command modulunu olustur</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/commands/device_connection.rs</files>
  <action>`serialport` crate bagimliligini ekle. `device_connection.rs` icinde `list_serial_ports`, `connect_serial_port`, `get_serial_connection_status` command'larini yaz. Listeleme sonucunda USB metadata'dan supported karari uret (VID/PID allowlist ile), unsupported portlari da don. Connect komutu secilen portu explicit acmayi dener ve basari/hata kodu + mesaji dondurur; auto-retry ve unplug/replug recovery logic ekleme (Phase 3 kapsaminda).</action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>Rust command API, Device panelinin ihtiyac duydugu list/connect/status davranislarini tek modulde saglar.</done>
</task>

<task type="auto">
  <name>Task 2: Command registration ve capability izinlerini bagla</name>
  <files>src-tauri/src/lib.rs, src-tauri/capabilities/default.json</files>
  <action>`lib.rs` icinde yeni command modulunu include et ve `tauri::generate_handler!` listesine device command'larini ekle. Mevcut tray davranisina dokunma. Capability dosyasini, frontendin yeni invoke command'larini cagirmasini engellemeyecek sekilde guncelle; sadece gerekli izinleri ekle, genis wildcard izin vermekten kacin.</action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>Frontend, Tauri invoke ile list/connect/status command'larini runtime'da cagirabilecek hale gelir.</done>
</task>

</tasks>

<verification>
`cargo check --manifest-path src-tauri/Cargo.toml` sonucu temiz olmali ve command kayitlari compile-time gorunmelidir.
</verification>

<success_criteria>
- Rust katmani supported/other ayrimini uretebilir ve explicit connect attempt donebilir.
- Device paneli icin gerekli backend komutlari invoke'e acik, yetkilendirme engeli olmadan kullanilabilir olur.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usb-connection-setup/02-02-SUMMARY.md`
</output>
