---
phase: 02-usb-connection-setup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/contracts/shell.ts
  - src/shared/contracts/device.ts
  - src/features/device/types.ts
  - src/features/device/portSelection.ts
  - src/features/device/portClassification.test.ts
  - src/features/device/manualConnectFlow.test.ts
  - src/features/device/selectionMemory.test.ts
autonomous: true
requirements: [CONN-01, CONN-02]
must_haves:
  truths:
    - "Supported ve other serial portlari deterministik olarak ayrilir ve supported grup once gelir."
    - "Ilk acilista ilk supported port secilebilir; returning usage'da son basarili port once secilir."
    - "Son port kaliciligi yalnizca basarili baglanti sonrasinda guncellenir."
  artifacts:
    - path: "src/features/device/portSelection.ts"
      provides: "Port gruplama/siralama/secim kurallari"
      exports: ["groupAndSortPorts", "resolveInitialSelection", "shouldPersistLastSuccessfulPort"]
    - path: "src/shared/contracts/device.ts"
      provides: "Cihaz baglanti command/event/store key kontratlari"
      exports: ["DEVICE_COMMANDS", "DEVICE_STATUS", "SUPPORTED_CONTROLLER_IDS"]
    - path: "src/features/device/selectionMemory.test.ts"
      provides: "Success-only persistence politikasinin test kaniti"
      contains: "persist only on successful connect"
  key_links:
    - from: "src/features/device/portSelection.ts"
      to: "src/features/device/portClassification.test.ts"
      via: "unit test behavior assertions"
      pattern: "groups supported ports first"
    - from: "src/shared/contracts/shell.ts"
      to: "src/features/device/selectionMemory.test.ts"
      via: "lastSuccessfulPort alan sozlesmesi"
      pattern: "lastSuccessfulPort"
---

<objective>
Device connection akisinin kontratlarini ve deterministik secim kurallarini test destekli sekilde netlestir.

Purpose: Phase 2 UI ve Rust command katmanlari ayni kurallara gore calissin; implementer tahmin yapmasin.
Output: Device contracts, port selection logic ve Wave-0 test kapsami.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-usb-connection-setup/02-CONTEXT.md
@.planning/phases/02-usb-connection-setup/02-RESEARCH.md
@src/shared/contracts/shell.ts

<interfaces>
From src/shared/contracts/shell.ts:
```ts
export interface ShellState {
  windowWidth: number;
  windowHeight: number;
  windowX: number | null;
  windowY: number | null;
  lastSection: SectionId;
  trayHintShown: boolean;
  startupEnabled: boolean;
  language?: string;
}
```

From .planning/phases/02-usb-connection-setup/02-CONTEXT.md (locked decisions):
```text
- Two-group list: supported first, other serial ports second
- Connect is explicit button action (no auto-connect on selection)
- Remembered port persistence only after successful connection
- If remembered port missing after refresh: clear selection + informative status
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Device kontratlarini ve saf secim kurallarini tanimla</name>
  <files>src/shared/contracts/shell.ts, src/shared/contracts/device.ts, src/features/device/types.ts, src/features/device/portSelection.ts</files>
  <behavior>
    - Test 1: Supported ve unsupported portlar ayristirilir; supported grup daima once listelenir.
    - Test 2: Returning usage'da `lastSuccessfulPort` listede varsa secim ona doner.
    - Test 3: `lastSuccessfulPort` yoksa ilk supported secilir; supported da yoksa secim bos kalir.
  </behavior>
  <action>`src/shared/contracts/device.ts` olusturup command adlari, status enum/degerleri, support allowlist (`SUPPORTED_CONTROLLER_IDS`) ve olasi hata kodlarini tek kaynaga topla. `src/shared/contracts/shell.ts` icine `lastSuccessfulPort?: string` ekle (breaking degil, optional). `src/features/device/types.ts` ile Rust DTO'larinin frontend karsiliklarini tanimla. `src/features/device/portSelection.ts` icinde gruplama/siralama/baslangic secimi/success-only persistence karar fonksiyonlarini pure function olarak uygula; UI veya invoke bagimliligi ekleme.</action>
  <verify>
    <automated>yarn vitest run src/features/device/portClassification.test.ts -t "groups supported ports first"</automated>
  </verify>
  <done>Device domain kurallari typed ve tekrar kullanilabilir fonksiyonlarla tek yerde toplanmis olur; shell state remembered-port icin hazirdir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 test dosyalarini olustur ve kurallari kanitla</name>
  <files>src/features/device/portClassification.test.ts, src/features/device/manualConnectFlow.test.ts, src/features/device/selectionMemory.test.ts</files>
  <behavior>
    - Test 1: "groups supported ports first" davranisi gecer.
    - Test 2: "manual fallback connect" davranisinda unsupported secim engellenmez ama connect explicit aksiyonla tetiklenir.
    - Test 3: "persist only on successful connect" sadece basari durumunda true doner.
  </behavior>
  <action>Validation dosyasindaki Wave-0 bosluklarini kapat: uc test dosyasinda once davranislari net adlandir, sonra `portSelection.ts` export'lari ile assertion yaz. Race condition icin refresh sonrasi secim-validasyon senaryosu da ekle. Testleri bu plan kapsaminda yesil duruma getir; flaky async patternlerden kacin.</action>
  <verify>
    <automated>yarn vitest run src/features/device/manualConnectFlow.test.ts src/features/device/selectionMemory.test.ts</automated>
  </verify>
  <done>02-VALIDATION.md icindeki uc MISSING test dosyasi mevcut olur ve device secim/memory kurallarini otomatik dogrular.</done>
</task>

</tasks>

<verification>
`yarn vitest run src/features/device/portClassification.test.ts src/features/device/manualConnectFlow.test.ts src/features/device/selectionMemory.test.ts` komutu gecmeli ve test adlari Phase 2 kararlarini dogrudan yansitmalidir.
</verification>

<success_criteria>
- Device secim ve persistence politikalari kod + test seviyesinde deterministik hale gelir.
- Downstream UI/backend planlari ayni kontratlari import ederek ilerler, magic string veya local kural tekrarina gerek kalmaz.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usb-connection-setup/02-01-SUMMARY.md`
</output>
