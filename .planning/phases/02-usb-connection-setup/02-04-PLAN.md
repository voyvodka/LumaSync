---
phase: 02-usb-connection-setup
plan: "04"
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/features/device/useDeviceConnection.ts
  - src/features/device/manualConnectFlow.test.ts
  - src/features/settings/sections/DeviceSection.tsx
  - src/locales/en/common.json
  - src/locales/tr/common.json
autonomous: true
requirements: [CONN-01, CONN-02]
gap_closure: true
must_haves:
  truths:
    - "Kullanici Refresh butonuna hover yaptiginda metin okunakliligini kaybetmez."
    - "Kullanici Refresh'e hizli tekrar tikladiginda tarama spamlenmez ve uygulama bunu net sekilde bildirir."
    - "Kullanici kisit araligi bittiginde yeni taramayi ayni Device panelinden tekrar baslatabilir."
  artifacts:
    - path: "src/features/device/useDeviceConnection.ts"
      provides: "Refresh aksiyonu icin minimum tekrar araligi ve durum uretimi"
      exports: ["createDeviceConnectionController", "useDeviceConnection"]
    - path: "src/features/device/manualConnectFlow.test.ts"
      provides: "Refresh spam korumasi ve yeniden izin penceresi davranis testleri"
    - path: "src/features/settings/sections/DeviceSection.tsx"
      provides: "Refresh button kontrastli hover ve scanning/rate-limit geri bildirimi"
      min_lines: 200
  key_links:
    - from: "src/features/settings/sections/DeviceSection.tsx"
      to: "src/features/device/useDeviceConnection.ts"
      via: "refreshPorts handler + isScanning/statusCard state"
      pattern: "refreshPorts|isScanning|statusCard"
    - from: "src/features/device/useDeviceConnection.ts"
      to: "src/features/device/deviceConnectionApi.ts"
      via: "listSerialPorts command cagrisi"
      pattern: "listSerialPorts"
---

<objective>
Phase 2 dogrulamasinda kalan iki runtime UX boslugunu kapat: Refresh hover kontrasti ve refresh spam tiklama kontrolu.

Purpose: Device panelinde scan aksiyonu her kosulda okunur, sakin ve kontrollu kalsin; kullaniciya scanning ve yeniden deneme durumu acikca gorunsun.
Output: Refresh rate-limit korumasi + buna ait testler + kontrastli ve durum-farkinda Refresh UI.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-usb-connection-setup/02-CONTEXT.md
@.planning/phases/02-usb-connection-setup/02-VERIFICATION.md
@src/features/device/useDeviceConnection.ts
@src/features/settings/sections/DeviceSection.tsx

<interfaces>
From src/features/device/useDeviceConnection.ts:
```ts
export interface UseDeviceConnectionResult extends DeviceConnectionControllerState {
  groupedPorts: ReturnType<typeof groupAndSortPorts>;
  refreshPorts: () => Promise<void>;
  selectPort: (portName: string | null) => void;
  connectSelectedPort: () => Promise<void>;
  connectButtonLabel: "connect" | "reconnect" | "connected";
}
```

From src/features/settings/sections/DeviceSection.tsx:
```ts
const {
  status,
  isScanning,
  statusCard,
  refreshPorts,
} = useDeviceConnection();
```

From .planning/phases/02-usb-connection-setup/02-CONTEXT.md (locked decisions):
```text
- Refresh keeps list in place with inline scanning state (no full-screen swap)
- During active refresh/scanning, Connect is disabled
- Failure/info states should include clear next-step guidance
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refresh spam korumasi ve durum geri bildirimi state makinesine ekle</name>
  <files>src/features/device/useDeviceConnection.ts, src/features/device/manualConnectFlow.test.ts</files>
  <behavior>
    - Test 1: Arka arkaya hizli iki `refreshPorts()` cagrisi, minimum aralik dolmadan ikinci kez `listSerialPorts` cagirmaz.
    - Test 2: Kisit nedeniyle bloklanan refresh denemesinde state kullaniciya bilgi veren bir status uretir (scanning disi net bilgi).
    - Test 3: Minimum aralik dolduktan sonra refresh tekrar calisir ve listeleme cagrisi yeniden yapilir.
  </behavior>
  <action>`createDeviceConnectionController` icine refresh icin minimum tekrar araligi (100-300ms bandinda, varsayilan 250ms) ekle; policy user karari geregi refresh spamini engellerken normal tekrar denemeyi bloke etmesin. Bu guard initial scan davranisini bozmasin, sadece user-triggered refresh cagrilarina uygulansin. Bloklanan denemede mevcut liste korunmaya devam etsin ve status surface icin net bir info sinyali uret (kod + mesaj/detay). `manualConnectFlow.test.ts` dosyasina fake-timer tabanli testler ekleyerek red-green sekilde bu uc davranisi kilitle.</action>
  <verify>
    <automated>yarn vitest run src/features/device/manualConnectFlow.test.ts -t "refresh"</automated>
  </verify>
  <done>Refresh aksiyonu hizli spam tiklamada ikinci listeleme cagrisi uretmez; state, kullanicinin neden bekledigini acik sekilde anlatir ve aralik bitince yeni refresh kabul edilir.</done>
</task>

<task type="auto">
  <name>Task 2: DeviceSection Refresh buton kontrastini ve rate-limit gorunurlugunu duzelt</name>
  <files>src/features/settings/sections/DeviceSection.tsx, src/locales/en/common.json, src/locales/tr/common.json</files>
  <action>Refresh butonunun hover/disabled/loading class kombinasyonunu, metin kontrasti her durumda korunacak sekilde guncelle (ozellikle hover state'te metin kaybolmamali). Buton varyanti scanning ve cooldown/rate-limit durumlarinda gorsel olarak tutarli kalsin. Hook'tan gelen refresh-limit bilgi durumunu Device panelinde net copy ile goster; scanning metniyle cakismayan tek bir durum anlatimi kullan. Yeni metin key'lerini EN/TR locale dosyalarina birebir parity ile ekle.</action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Refresh butonu hover'da okunur kalir ve hizli tekrar deneme kisitlandiginda kullanici panelde ne oldugunu acikca gorur.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/device/manualConnectFlow.test.ts`
- `yarn tsc --noEmit`
</verification>

<success_criteria>
- Refresh butonu hover durumunda metin/zemin kontrasti bozulmaz.
- Refresh aksiyonu minimum tekrar araligi ile korunur; ardisik tiklamalar yeni scan request spam'i uretmez.
- Kullanici scanning ile refresh-kisit durumunu ayirt edebilir ve ne zaman tekrar deneyebilecegini panelden anlar.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usb-connection-setup/02-04-SUMMARY.md`
</output>
