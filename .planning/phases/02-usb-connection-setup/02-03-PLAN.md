---
phase: 02-usb-connection-setup
plan: "03"
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/features/device/deviceConnectionApi.ts
  - src/features/device/useDeviceConnection.ts
  - src/features/settings/sections/DeviceSection.tsx
  - src/locales/en/common.json
  - src/locales/tr/common.json
autonomous: true
requirements: [CONN-01, CONN-02]
must_haves:
  truths:
    - "Kullanici Device panelinde supported ve other serial port gruplarini gorur."
    - "Kullanici auto-detect kacirdiginda unsupported dahil bir portu secip Connect ile manual deneme yapar."
    - "Kullanici liste yenilendiginde secim kaybi/port kaybi durumunu net status kartindan anlar ve restart olmadan tekrar deneyebilir."
  artifacts:
    - path: "src/features/device/useDeviceConnection.ts"
      provides: "scan/select/connect/status state machine"
      exports: ["useDeviceConnection"]
    - path: "src/features/settings/sections/DeviceSection.tsx"
      provides: "Iki-gruplu liste + manual fallback UI + status card"
      min_lines: 120
    - path: "src/features/device/deviceConnectionApi.ts"
      provides: "invoke wrappers for list/connect/status"
      exports: ["listSerialPorts", "connectSerialPort", "getSerialConnectionStatus"]
  key_links:
    - from: "src/features/settings/sections/DeviceSection.tsx"
      to: "src/features/device/useDeviceConnection.ts"
      via: "hook state + handlers"
      pattern: "useDeviceConnection"
    - from: "src/features/device/useDeviceConnection.ts"
      to: "src/features/device/deviceConnectionApi.ts"
      via: "invoke wrappers"
      pattern: "listSerialPorts|connectSerialPort"
---

<objective>
Device panelini, Phase 2 kararlarina birebir uyan tarama-secim-manuel baglanti akisiyla calisir hale getir.

Purpose: Kullanici destekli cihazi hizli bulsun, auto-detect kacirsa da manuel fallback ile app restart etmeden baglanabilsin.
Output: Device hook + API adapter + tamamlanmis DeviceSection UI + EN/TR metinleri.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-usb-connection-setup/02-CONTEXT.md
@.planning/phases/02-usb-connection-setup/02-RESEARCH.md
@src/features/settings/sections/DeviceSection.tsx
@src/features/persistence/shellStore.ts

<interfaces>
From src/features/persistence/shellStore.ts:
```ts
export const shellStore = {
  async load(): Promise<ShellState>;
  async save(partial: Partial<ShellState>): Promise<void>;
};
```

From .planning/phases/02-usb-connection-setup/02-CONTEXT.md (locked decisions):
```text
- Manual selection UI always visible
- Connect enabled only when a port is selected
- During refresh/scanning, Connect is disabled
- If selected port disappears after refresh, clear selection + informational warning
- If last successful port reappears, auto-reselect
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frontend command adapter ve baglanti state hook'unu uygula</name>
  <files>src/features/device/deviceConnectionApi.ts, src/features/device/useDeviceConnection.ts</files>
  <behavior>
    - Test 1: Device panel acilisinda auto-scan tetiklenir ve liste korunarak inline scanning state guncellenir.
    - Test 2: `lastSuccessfulPort` listede varsa secim buna donecek sekilde initial selection cozulur.
    - Test 3: Connect denemesi sadece explicit handler ile tetiklenir; secim degisimi connect cagirmaz.
  </behavior>
  <action>Plan 01 kontratlarini ve Plan 02 Rust command isimlerini kullanarak invoke wrapper dosyasini olustur. `useDeviceConnection` hook'unda `idle/scanning/ready/connecting/connected/error` state gecislerini tek merkezde yonet. Refresh sirasinda Connect disabled kuralini uygula. Successful connect sonrasinda `shellStore.save({ lastSuccessfulPort })` yaz; failed attempt'te yazma. Port kaybi ve remembered-port geri gelme durumlarini hook icinde ele al.</action>
  <verify>
    <automated>yarn vitest run src/features/device/manualConnectFlow.test.ts src/features/device/selectionMemory.test.ts</automated>
  </verify>
  <done>Device paneline bagimsiz testlenebilir bir hook+API katmani olusur ve port memory kararlari kodda garanti altina alinir.</done>
</task>

<task type="auto">
  <name>Task 2: DeviceSection UI'yi iki-grup liste + status card akisiyla tamamla</name>
  <files>src/features/settings/sections/DeviceSection.tsx, src/locales/en/common.json, src/locales/tr/common.json</files>
  <action>Placeholder DeviceSection'i `useDeviceConnection` ile yeniden yaz. Ekranda Supported controllers ve Other serial ports gruplarini ayri basliklarla goster; satirda cihaz adi, port kimligi, support status badge'i bulunsun. Manual selection alani her zaman gorunur kalsin. Connect butonu secim yokken veya scanning sirasinda disabled olsun; connected port seciliyken label/state connection-aware olsun. Inline status card'da success/failure/missing-port durumlarini sakin dilde ve aksiyon onerileriyle ver. EN/TR locale dosyalarina yeni key'leri parity ile ekle.</action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>Kullanici Device panelinden scan-refresh-select-connect akisini tamamlarken tum locked UX kararlarini ekranda gorur.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/device/portClassification.test.ts src/features/device/manualConnectFlow.test.ts src/features/device/selectionMemory.test.ts`
- `yarn tsc --noEmit`
</verification>

<success_criteria>
- CONN-01: Supported controller'lar Device panelinde auto-detect ile ustte gorunur.
- CONN-02: Auto-detect kacirdiginda user unsupported dahil port secip Connect ile manuel baglanti denemesi yapar.
- Son basarili port tekrar gorunurse otomatik preselect edilir; kayip port durumunda secim temizlenir ve net durum mesaji sunulur.
</success_criteria>

<output>
After completion, create `.planning/phases/02-usb-connection-setup/02-03-SUMMARY.md`
</output>
