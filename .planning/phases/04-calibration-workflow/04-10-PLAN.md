---
phase: 04-calibration-workflow
plan: "10"
type: execute
wave: 5
depends_on: [04-02, 04-03, 04-08]
files_modified:
  - src/shared/contracts/display.ts
  - src/features/calibration/state/displayTargetState.ts
  - src/features/calibration/state/displayTargetState.test.ts
  - src/features/calibration/ui/CalibrationOverlay.tsx
  - src/features/calibration/calibrationApi.ts
  - src/features/device/useDeviceConnection.ts
  - src-tauri/src/commands/calibration.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [CAL-04]
must_haves:
  truths:
    - "Kullanici test pattern acmadan once hedef ekrani mini kartlardan secebilir."
    - "Kullanici test pattern acikken hedef ekran degistirirse eski ekrandaki overlay kapanir, yeni ekranda tek aktif overlay acilir."
    - "OS-level overlay acilamazsa test pattern fallback preview'e dusmeden bloke edilir."
  artifacts:
    - path: "src/features/calibration/state/displayTargetState.ts"
      provides: "tek-aktif overlay ve open-fail blokaj state orkestrasyonu"
      exports: ["createDisplayTargetState"]
    - path: "src/shared/contracts/display.ts"
      provides: "display kimligi ve overlay command kontratlari"
    - path: "src/features/calibration/ui/CalibrationOverlay.tsx"
      provides: "display mini-kart secici ve blocked-state UX"
  key_links:
    - from: "src/features/calibration/ui/CalibrationOverlay.tsx"
      to: "src/features/calibration/state/displayTargetState.ts"
      via: "display seciminde switchActiveDisplay akisi"
      pattern: "switchActiveDisplay"
    - from: "src/features/calibration/calibrationApi.ts"
      to: "src-tauri/src/commands/calibration.rs"
      via: "open/close/list display overlay command eslesmesi"
      pattern: "open_display_overlay|close_display_overlay|list_displays"
---

<objective>
Display lifecycle kapsamini (hedef ekran secimi + tek aktif overlay + open-fail blokaji) mode/control planindan ayirip daha kucuk bir vertical slice olarak tamamla.

Purpose: 04-03 kapsam yogunlugunu dusurmek ve checker'in scope_sanity uyarisini kapatmak.
Output: Display target state katmani, display command wrapper/handler'lari ve overlay mini-kart secici.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-CONTEXT.md
@.planning/phases/04-calibration-workflow/04-03-PLAN.md
@src/features/calibration/ui/CalibrationOverlay.tsx
@src-tauri/src/commands/calibration.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: displayTargetState ve display command bridge'i TDD ile kur</name>
  <files>src/shared/contracts/display.ts, src/features/calibration/state/displayTargetState.ts, src/features/calibration/state/displayTargetState.test.ts, src/features/calibration/calibrationApi.ts, src-tauri/src/commands/calibration.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Test 1: Test pattern acikken display degisirse once `close_display_overlay(oldDisplayId)` sonra `open_display_overlay(newDisplayId)` cagrilir.
    - Test 2: `open_display_overlay` hata dondururse state `blocked` olur ve pattern restart denemesi yapilmaz.
    - Test 3: Ayni anda tek aktif overlay kalir; parallel open durumuna izin verilmez.
  </behavior>
  <action>`display.ts` icinde display id ve overlay command kontratlarini tanimla. `displayTargetState.ts` icinde atomik `switchActiveDisplay` akisini uygula: close old -> open new -> failse `blocked` + `OVERLAY_OPEN_FAILED`. `calibrationApi.ts` ve Rust command tarafinda `list_displays`, `open_display_overlay`, `close_display_overlay` wrapper/handlerlarini ekle. Rust tarafinda open komutunda explicit error payload'i dondur ki UI neden metni gosterebilsin.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/displayTargetState.test.ts -t "single-active|OVERLAY_OPEN_FAILED"</automated>
  </verify>
  <done>Display switch lifecycle deterministic olur; overlay-open failure fallback'e dusmeden bloke edilir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overlay mini-kart secici ve blocked-state UX'i bagla</name>
  <files>src/features/calibration/ui/CalibrationOverlay.tsx, src/features/device/useDeviceConnection.ts, src/features/calibration/state/displayTargetState.ts</files>
  <behavior>
    - Test 1: 1+ display listesi mini kartlarla render edilir; secili kart aktif hedefi belirler.
    - Test 2: Test pattern acik durumdayken kart secimi `switchActiveDisplay` akisina girer.
    - Test 3: `blocked` durumda toggle disabled olur ve acik neden metni gorunur.
  </behavior>
  <action>`CalibrationOverlay.tsx` icine display selector kartlarini ekle (Display no + cozunurluk/konum). Veri kaynagi olarak `useDeviceConnection` veya mevcut display query patternini kullan. Test pattern acik durumda secim degisiminde `displayTargetState.switchActiveDisplay` cagir. `blocked` durumda toggle butonunu disabled yap ve fallback olmadigini belirten acik metni goster.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/displayTargetState.test.ts -t "switchActiveDisplay"</automated>
  </verify>
  <done>Kullanici hedef display'i secerek test pattern'i tek aktif overlay kuraliyla calistirir; open-fail durumunda blok nedenini gorur.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/calibration/state/displayTargetState.test.ts -t "single-active|OVERLAY_OPEN_FAILED"`
- `yarn vitest run src/features/calibration/state/displayTargetState.test.ts -t "switchActiveDisplay"`
</verification>

<success_criteria>
- Hedef display secimi mini kartlarla calisir.
- Display degisiminde tek aktif overlay kurali deterministic kalir.
- Overlay open hatasinda test pattern fallback olmadan bloke edilir ve neden gorunur.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-10-SUMMARY.md`
</output>
