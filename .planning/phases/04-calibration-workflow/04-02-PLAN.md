---
phase: 04-calibration-workflow
plan: "02"
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/features/calibration/state/calibrationEditorState.ts
  - src/features/calibration/state/calibrationEditorState.test.ts
  - src/features/calibration/ui/CalibrationOverlay.tsx
  - src/features/calibration/ui/CalibrationTemplateStep.tsx
  - src/features/calibration/ui/CalibrationEditorCanvas.tsx
  - src/features/persistence/shellStore.ts
autonomous: true
requirements: [UX-02, CAL-01, CAL-02, CAL-03]
must_haves:
  truths:
    - "Kullanici Calibration editor overlay'ini actiginda template secip ayni ekranda detaylari duzenleyebilir."
    - "Kullanici template secip edge count + start anchor + direction alanlarini ayarlayabilir."
    - "Kullanici editor icinden Sifirla / Sablon sec ile template secim adimina geri donebilir."
    - "Kaydedilmemis degisiklikle cikista onay akisi devreye girer."
  artifacts:
    - path: "src/features/calibration/ui/CalibrationOverlay.tsx"
      provides: "Template step + editor step gecislerini yoneten ortak tam ekran editor yuzeyi"
    - path: "src/features/calibration/state/calibrationEditorState.ts"
      provides: "Template/editor dirty-state ve save-onay state orkestrasyonu"
  key_links:
    - from: "src/features/calibration/ui/CalibrationOverlay.tsx"
      to: "src/features/persistence/shellStore.ts"
      via: "explicit Save ile ledCalibration persistence"
      pattern: "shellStore.save"
    - from: "src/features/calibration/ui/CalibrationOverlay.tsx"
      to: "src/features/calibration/ui/CalibrationTemplateStep.tsx"
      via: "Sifirla / Sablon sec ile step-state geri gecisi"
      pattern: "onResetToTemplateStep"
---

<objective>
Calibration editor overlay'inin template->editor akisina odaklan: explicit kayit, dirty-state onayi ve "Sifirla / Sablon sec" geri donusunu tek state modeliyle bagla.

Purpose: 04-02 kapsaminda editor davranisini izole ederek kaliteyi korumak; giris noktasi wiring'i ayri mini planda tamamlanacak.
Output: Ortak editor state katmani + overlay step gecisleri + explicit save/cancel davranisi.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-CONTEXT.md
@.planning/phases/04-calibration-workflow/04-RESEARCH.md
@.planning/phases/01-app-shell-and-baseline-defaults/01-03-SUMMARY.md
@src/App.tsx
@src/features/settings/SettingsLayout.tsx
@src/features/persistence/shellStore.ts

<interfaces>
From src/App.tsx:
```ts
function App() {
  const [activeSection, setActiveSection] = useState<SectionId>(SECTION_IDS.GENERAL);
  const handleSectionChange = useCallback(async (sectionId: SectionId) => {
    setActiveSection(sectionId);
    await saveShellState({ lastSection: sectionId });
  }, []);
}
```

From src/features/settings/SettingsLayout.tsx:
```ts
interface SettingsLayoutProps {
  activeSection: SectionId;
  onSectionChange: (sectionId: SectionId) => void;
}
```

From src/features/persistence/shellStore.ts:
```ts
export const shellStore = {
  async load(): Promise<ShellState>,
  async save(partial: Partial<ShellState>): Promise<void>,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calibration editor state motorunu testle ve uygula</name>
  <files>src/features/calibration/state/calibrationEditorState.ts, src/features/calibration/state/calibrationEditorState.test.ts</files>
  <behavior>
    - Test 1: Yeni acilista baseline = mevcut kayit olur; degisiklik yokken `isDirty=false`.
    - Test 2: Count/startAnchor/direction/template degisince `isDirty=true`, baseline'a geri donunce `isDirty=false`.
    - Test 3: Save basarisinda baseline guncellenir; cancel/close akisi dirty ise confirm gerektirir.
  </behavior>
  <action>Reducer veya saf state helper yapisiyla template secimi, manuel reset, alan guncelleme, save snapshot ve unsaved-confirm flag davranisini tek yerde topla. Kaydedilmemis degisiklik tespiti icin normalize edilmis model string karsilastirmasi kullan (referans esitligine guvenme).</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/calibrationEditorState.test.ts -t "isDirty"</automated>
  </verify>
  <done>Editor state degisiklikleri deterministic olur ve overlay UI sadece bu state API'sini kullanir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: overlay template/editor adimlarini ortak editor state'iyle bagla</name>
  <files>src/features/calibration/ui/CalibrationOverlay.tsx, src/features/calibration/ui/CalibrationTemplateStep.tsx, src/features/calibration/ui/CalibrationEditorCanvas.tsx</files>
  <behavior>
    - Test 1: Overlay ilk acilista template step'i render eder; secimden sonra editor step'ine gecer.
    - Test 2: Editor icindeki "Sifirla / Sablon sec" aksiyonu kullaniciyi template secim step'ine geri goturur ve secilen template'i editor'e yeniden uygular.
    - Test 3: Cikis sirasinda dirty-state varsa onay modal'i, dirty yoksa direkt kapanis olur.
  </behavior>
  <action>Overlay'i tam ekran ve seffaf arka planla tasarla; ilk giriste template step (opsiyonel), tekrar duzenlemede dogrudan editor step ac. Editor toolbar/ust aksiyon alanina "Sifirla / Sablon sec" butonu ekle: bu aksiyon mevcut editor state'ini template secim adimina geri tasisin, gerekiyorsa `resetToManual` ile degerleri boslatip step-state'i template ekranina dondursun, ardindan kullanici yeni template secerek tekrar editor adimina gecsin. Editorde top/left/right/bottom-left/bottom-right count alanlari, start marker secimi ve CW/CCW secimi bulunmali. Save explicit butonla shell state'e yazilsin; auto-save olmasin. Not: App-level ilk baglanti auto-open ve Settings section wiring'i 04-12 mini planinda tamamlanacak.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/entryFlow.test.ts -t "returns to template step"</automated>
  </verify>
  <done>Overlay icinde template->editor gecisi ve "Sifirla / Sablon sec" geri donusu stabil calisir; explicit save/cancel + dirty-confirm davranisi tutarlidir.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/calibration/state/calibrationEditorState.test.ts -t "isDirty"`
- `yarn vitest run src/features/calibration/state/entryFlow.test.ts -t "returns to template step"`
</verification>

<success_criteria>
- Kullanici ilk baglantidan sonra wizard ile kalibrasyon ekranina otomatik yonlendirilir.
- Kullanici Settings > Calibration'dan kaydi tekrar acip duzenleyebilir.
- Explicit Save disinda kalibrasyon verisi kalici degismez.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-02-SUMMARY.md`
</output>
