---
phase: 04-calibration-workflow
plan: "09"
type: execute
wave: 4
depends_on: [04-02, 04-12]
files_modified:
  - src/features/mode/state/modeGuard.ts
  - src/features/mode/state/modeGuard.test.ts
  - src/features/settings/sections/GeneralSection.tsx
  - src/App.tsx
  - src/features/calibration/state/entryFlow.test.ts
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "Kalibrasyon kaydi yokken kullanici LED modunu etkinlestiremez."
    - "Mode alani disabled nedenini acik metinle gosterir ve kullaniciyi Calibration ekranina yonlendirir."
    - "Kalibrasyon kaydi olusunca LED mode enable guard otomatik olarak kalkar."
  artifacts:
    - path: "src/features/mode/state/modeGuard.ts"
      provides: "ledCalibration var/yok durumuna gore canEnable + reason donduren saf guard"
      exports: ["canEnableLedMode"]
    - path: "src/features/settings/sections/GeneralSection.tsx"
      provides: "disabled UX + calibration CTA"
    - path: "src/App.tsx"
      provides: "mode enable aksiyonunda guard enforcement"
  key_links:
    - from: "src/App.tsx"
      to: "src/features/mode/state/modeGuard.ts"
      via: "mode toggle oncesi canEnableLedMode kontrolu"
      pattern: "canEnableLedMode"
    - from: "src/features/settings/sections/GeneralSection.tsx"
      to: "src/App.tsx"
      via: "disabled CTA ile calibration overlay acma callback'i"
      pattern: "openCalibrationOverlay"
---

<objective>
Kalibrasyon tamamlanmadan LED mode enable edilmesini teknik ve gorsel olarak bloke eden guard katmanini ayri bir plan olarak uygula.

Purpose: 04-02'nin kapsam yogunlugunu azaltirken kilit kullanici kararini (mode lock) net ve testlenebilir sekilde garanti etmek.
Output: Saf mode guard helper'i, App-level enforcement ve General section disabled UX.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-CONTEXT.md
@.planning/phases/04-calibration-workflow/04-02-PLAN.md
@.planning/phases/04-calibration-workflow/04-12-PLAN.md
@src/App.tsx
@src/features/settings/sections/GeneralSection.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: modeGuard saf kontratini TDD ile olustur</name>
  <files>src/features/mode/state/modeGuard.ts, src/features/mode/state/modeGuard.test.ts</files>
  <behavior>
    - Test 1: `ledCalibration` yoksa `canEnable=false` ve `reason="CALIBRATION_REQUIRED"` doner.
    - Test 2: `ledCalibration` varsa `canEnable=true` ve reason bos doner.
  </behavior>
  <action>`canEnableLedMode(ledCalibration)` helper'ini saf ve framework-agnostic yaz. Donus tipinde hem boolean hem de reason code bulunsun; UI ve App ayni helper'i kullansin. Discrete reason code kullan, magic string dagitma.</action>
  <verify>
    <automated>yarn vitest run src/features/mode/state/modeGuard.test.ts -t "CALIBRATION_REQUIRED|canEnable"</automated>
  </verify>
  <done>Guard behavior'i unit testle sabitlenir; kalibrasyon yokken mode lock deterministic calisir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App ve General section'da mode lock UX'i bagla</name>
  <files>src/App.tsx, src/features/settings/sections/GeneralSection.tsx, src/features/calibration/state/entryFlow.test.ts</files>
  <behavior>
    - Test 1: Guard `CALIBRATION_REQUIRED` dondugunde mode toggle state degistirmez.
    - Test 2: Disabled UI acik neden metni ve Calibration CTA'si gosterir.
    - Test 3: CTA tetiklenince calibration overlay acma callback'i cagrilir.
  </behavior>
  <action>`App.tsx` icindeki mode enable aksiyonunu guard ile sar: `canEnable=false` ise state mutasyonu yapma, calibration ekranina yonlendiren callback tetikle. `GeneralSection.tsx` icinde disabled durumda kilit-aciklamasi + CTA ekle. Mevcut localization keylerini kullan; yeni key gerekiyorsa ayni naming pattern ile sinirli ekle.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/entryFlow.test.ts -t "CALIBRATION_REQUIRED gate|open calibration"</automated>
  </verify>
  <done>Kullanici kalibrasyon tamamlanmadan LED mode'u acamaz; UI neden ve sonraki adimi net sekilde gosterir.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/mode/state/modeGuard.test.ts -t "CALIBRATION_REQUIRED|canEnable"`
- `yarn vitest run src/features/calibration/state/entryFlow.test.ts -t "CALIBRATION_REQUIRED gate|open calibration"`
</verification>

<success_criteria>
- Kalibrasyon kaydi olmadan mode enable aksiyonu teknik olarak bloke edilir.
- General section disabled UX ile neden ve cozum aksiyonu gorunur.
- Kalibrasyon kaydi olunca ayni guard otomatik olarak mode enable'e izin verir.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-09-SUMMARY.md`
</output>
