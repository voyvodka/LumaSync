---
phase: 04-calibration-workflow
plan: "13"
type: execute
wave: 6
depends_on: [04-10, 04-12]
files_modified:
  - src-tauri/src/commands/calibration.rs
  - src-tauri/src/lib.rs
  - src/features/calibration/state/displayTargetState.ts
  - src/features/calibration/state/displayTargetState.test.ts
  - src/features/calibration/ui/CalibrationOverlay.tsx
autonomous: true
gap_closure: true
requirements: [CAL-04, UX-02]
must_haves:
  truths:
    - "Kullanici Settings > Calibration > Duzenle ile editoru actiginda hedef ekranda gorunur OS-level overlay gorebilir."
    - "Kullanici test pattern acikken display hedefi degistirdiginde eski overlay kapanir ve yeni hedefte tek aktif overlay acilir."
    - "Kullanici overlay open fail durumunda toggle'un bloke oldugunu ve acik reason metnini gorebilir."
  artifacts:
    - path: "src-tauri/src/commands/calibration.rs"
      provides: "gercek OS-level display overlay open/close lifecycle ve kodlu fail reason"
      exports: ["open_display_overlay", "close_display_overlay", "OverlayState"]
    - path: "src/features/calibration/state/displayTargetState.ts"
      provides: "close-old/open-new ve blocked snapshot state orkestrasyonu"
      exports: ["createDisplayTargetState"]
    - path: "src/features/calibration/ui/CalibrationOverlay.tsx"
      provides: "display switchte blocked reason + test pattern kapatma guvencesi"
  key_links:
    - from: "src/features/calibration/state/displayTargetState.ts"
      to: "src-tauri/src/commands/calibration.rs"
      via: "open_display_overlay / close_display_overlay command sonucu"
      pattern: "OVERLAY_OPENED|OVERLAY_OPEN_FAILED|OVERLAY_CLOSED"
    - from: "src/features/calibration/ui/CalibrationOverlay.tsx"
      to: "src/features/calibration/state/displayTargetState.ts"
      via: "switchActiveDisplay sonucu blocked kontrolu"
      pattern: "switchActiveDisplay"
---

<objective>
UAT Test 2/7/8'de kalan ana gap'i kapat: state-only kalan display overlay komutunu gercek OS-level overlay lifecycle'a tasiyip, fail durumunda UI blokaj metnini sahada gozlenebilir hale getir.

Purpose: CAL-04 dogrulamasinin kodda degil gercek calisma aninda da gozlenebilmesi; UX-02 Edit yeniden girisinde "overlay gorunmuyor" semptomunun kaldirilmasi.
Output: Rust tarafinda gercek overlay open/close akisi, TS state tarafinda buna uyumlu blokaj gecisleri, overlay UI'da net hata davranisi.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-UAT.md
@.planning/debug/calibration-edit-overlay-not-opening.md
@.planning/debug/test-pattern-overlay-missing-on-display-switch.md
@.planning/debug/overlay-open-fail-blocked-reason-not-visible.md
@src-tauri/src/commands/calibration.rs
@src/features/calibration/state/displayTargetState.ts
@src/features/calibration/ui/CalibrationOverlay.tsx

<interfaces>
From src-tauri/src/commands/calibration.rs:
```rust
pub struct OverlayState {
  pub active_display_id: std::sync::Mutex<Option<String>>,
}

#[tauri::command]
pub fn open_display_overlay<R: Runtime>(... , display_id: String) -> Result<DisplayOverlayCommandResult, String>;

#[tauri::command]
pub fn close_display_overlay(... , display_id: String) -> Result<DisplayOverlayCommandResult, String>;
```

From src/features/calibration/state/displayTargetState.ts:
```typescript
export interface DisplayTargetState {
  switchActiveDisplay: (displayId?: DisplayId | null) => Promise<DisplayTargetSnapshot>;
  closeActiveDisplay: () => Promise<DisplayTargetSnapshot>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rust tarafinda gercek display overlay lifecycle'ini uygula</name>
  <files>src-tauri/src/commands/calibration.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Test 1: Gecerli display id ile open cagrisi state guncellemekle kalmaz; gorunur bir overlay window acilir ve `OVERLAY_OPENED` doner.
    - Test 2: Yeni display'e geciste onceki aktif overlay kapanir, yeni overlay acilir; ayni anda tek overlay aktif kalir.
    - Test 3: Overlay olusturma basarisizsa `OVERLAY_OPEN_FAILED` + reason doner ve state aktif overlay'e gecmez.
  </behavior>
  <action>`open_display_overlay` ve `close_display_overlay` komutlarini state-only davranistan cikar. Gecerli monitor bilgisi uzerinden Tauri window/webview overlay olustur: fullscreen/undecorated/always-on-top ve hedef monitor sinirlarina konumlanmis olacak. `OverlayState` icinde aktif display ile birlikte aktif overlay window label bilgisini de tutarak close-old/open-new zincirini garanti et. Open/close adimlarinin her birinde kodlu sonuc (`OVERLAY_OPENED`, `OVERLAY_CLOSED`, `OVERLAY_OPEN_FAILED`) ve reason detayini dondur. `lib.rs` setup/manage kayitlarini yeni overlay state ihtiyacina gore guncelle. Preview fallback ekleme; user karari geregi open fail durumunda blokaj korunacak.</action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>Backend komutu artik sahada gorunur overlay olusturur; basarisiz open denemeleri reason ile deterministik raporlanir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Display state/UI akislarini gercek overlay lifecycle ile hizala</name>
  <files>src/features/calibration/state/displayTargetState.ts, src/features/calibration/state/displayTargetState.test.ts, src/features/calibration/ui/CalibrationOverlay.tsx</files>
  <behavior>
    - Test 1: `switchActiveDisplay` old-close/new-open sirasini korur ve open fail olunca `blocked` snapshot doner.
    - Test 2: Overlay open fail sonrasi UI test pattern'i guvenli sekilde kapatir ve blocked reason metnini gosterir.
    - Test 3: Test pattern acikken display secimi sonrasi yalniz bir aktif display etiketi kalir.
  </behavior>
  <action>`displayTargetState.test.ts` icinde UAT gap'lerini modelleyen testleri once RED yaz: open fail reason propagation, switch sirasinda tek aktif overlay, fail sonrasi blocked state. Sonra `displayTargetState.ts` ve `CalibrationOverlay.tsx` icinde open/switch/close akislarini harden et: blocked durumda toggle-on denemesini sonlandir, gerekiyorsa pattern'i kapat, stale active id birakma. Mesajlari mevcut i18n zinciriyle uyumlu tut; yeni UX ekleme yapma (deferred alanlara girme).</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/displayTargetState.test.ts -t "switchActiveDisplay|OVERLAY_OPEN_FAILED|single-active"</automated>
  </verify>
  <done>Display switch ve open-fail davranisi UI'da deterministic ve gozlenebilir olur; Test 2/7/8 icin kod tabani dogrulanmis hale gelir.</done>
</task>

</tasks>

<verification>
- `cargo check --manifest-path src-tauri/Cargo.toml`
- `yarn vitest run src/features/calibration/state/displayTargetState.test.ts -t "switchActiveDisplay|OVERLAY_OPEN_FAILED|single-active"`
</verification>

<success_criteria>
- Settings > Calibration > Duzenle akisinda test pattern acildiginda hedef ekranda gorunur overlay olusur.
- Display switchte close-old/open-new zinciri ile ayni anda tek aktif overlay kalir.
- Overlay open fail durumunda blocked reason UI'da gorunur ve test pattern toggle bloke kalir.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-13-SUMMARY.md`
</output>
