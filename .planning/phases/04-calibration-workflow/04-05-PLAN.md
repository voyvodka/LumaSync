---
phase: 04-calibration-workflow
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/calibration/ui/CalibrationOverlay.tsx
  - src/features/calibration/state/testPatternFlow.ts
  - src/features/calibration/state/testPatternFlow.test.ts
autonomous: true
gap_closure: true
requirements: [CAL-04]

must_haves:
  truths:
    - "Test pattern toggle acildiginda buildLedSequence(currentConfig) sonucu fiziksel payload'a (ledIndexes) yazilir"
    - "Sadece aktif markerIndex'e karsilik gelen tek LED indeksi o an gonderilir (tam dizi degil)"
    - "Config (anchor/direction) degisince bir sonraki toggle acilisinda yeni siraya gore gonderim yapilir"
  artifacts:
    - path: "src/features/calibration/state/testPatternFlow.ts"
      provides: "createTestPatternFlow config param kabul eder, startPhysicalPattern aktif LED indeksini config'den turetir"
  key_links:
    - from: "src/features/calibration/model/indexMapping.ts"
      to: "src/features/calibration/state/testPatternFlow.ts"
      via: "buildLedSequence(config)[markerIndex].index -> ledIndexes payload"
      pattern: "buildLedSequence"
    - from: "src/features/calibration/ui/CalibrationOverlay.tsx"
      to: "src/features/calibration/state/testPatternFlow.ts"
      via: "flowRef ile currentConfig gecilmesi"
      pattern: "setConfig\\|updateConfig"
---

<objective>
CAL-04 gereksinimi: test pattern, gercek LED mapping sirasini (buildLedSequence uretimi) kullanarak fiziksel cihaza dogru indeks gondermelidir.

Purpose: Verification gap 3'u kapatir. testPatternFlow.ts'deki `startPhysicalPattern` sabit `[0]` gonderiyor; `buildLedSequence(config)` ile dinamik yapilmasi gerekiyor.
Output: testPatternFlow, mevcut kalibrasyon config'ini alip her frame'de aktif marker indeksine karsili gelen LED fiziksel indeksini hesaplar ve startCalibrationTestPattern payload'una yazar.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/features/calibration/model/indexMapping.ts:
```typescript
export interface LedSequenceItem {
  index: number;     // fiziksel LED indeksi (0-based, strip uzerindeki konum)
  segment: LedSegmentKey;
  localIndex: number;
}

// config'den tam LED sirasini uretir (startAnchor + direction uygulanmis)
export function buildLedSequence(config: LedCalibrationConfig): LedSequenceItem[];
```

From src/features/calibration/model/contracts.ts:
```typescript
export interface LedCalibrationConfig {
  counts: LedSegmentCounts;
  bottomGapPx: number;
  startAnchor: LedStartAnchor;
  direction: LedDirection;
  totalLeds: number;
  templateId?: string;
}
```

From src/features/calibration/calibrationApi.ts:
```typescript
export interface CalibrationTestPatternStartPayload {
  ledIndexes: number[];  // gonderiilecek LED fiziksel indeksleri
  frameMs: number;
  brightness: number;
}
export async function startCalibrationTestPattern(payload: CalibrationTestPatternStartPayload): Promise<...>
```

Mevcut testPatternFlow createDefaultTestPatternFlow:
```typescript
// SIMDI (sabit):
startPhysicalPattern: async () => {
  await startCalibrationTestPattern({ ledIndexes: [0], frameMs: MARKER_ADVANCE_MS, brightness: 64 });
},
```

TestPatternFlow arayuzu (degismeyecek):
```typescript
export interface TestPatternFlow {
  getSnapshot: () => TestPatternSnapshot;
  setTotalLeds: (totalLeds: number) => TestPatternSnapshot;
  toggle: (enabled: boolean) => Promise<TestPatternSnapshot>;
  dispose: () => Promise<void>;
  // EKLENECEK:
  setConfig: (config: LedCalibrationConfig) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: testPatternFlow'a config-driven LED indeksi hesabi ekle (TDD)</name>
  <files>src/features/calibration/ui/CalibrationOverlay.tsx, src/features/calibration/state/testPatternFlow.ts, src/features/calibration/state/testPatternFlow.test.ts</files>
  <behavior>
    - `TestPatternFlow` arayuzune `setConfig(config: LedCalibrationConfig): void` metodu eklenir
    - `createDefaultTestPatternFlow(getConnectionStatus, initialConfig)` initialConfig parametresi alir
    - toggle(true) cagrildigi an `buildLedSequence(currentConfig)[markerIndex].index` hesaplanir ve `ledIndexes: [physicalIndex]` olarak gonderilir
    - markerIndex 0 oldugunda ilk LED'in fiziksel indeksi gonderilir (config.startAnchor ile belirli)
    - setConfig cagrisi yapildiginda bir sonraki toggle acilisinda yeni config kullanilir
    - `buildLedSequence` sonucu bos ise fallback: `ledIndexes: [0]`
    - Mevcut testler (toggle lifecycle, preview-only fallback, raf marker loop) kirilmaz
  </behavior>
  <action>
    **RED: Once test yaz, sonra implement et.**

    **Yeni testler (testPatternFlow.test.ts):**
    1. `"uses buildLedSequence result for physical ledIndexes on toggle"`:
       - `top: 5, right: 3, ...` config ile flow yarat
       - `startPhysicalPattern` spy'i capture et: cagrildigi payload'u kaydet
       - `flow.toggle(true)` cagir
       - `startPhysicalPattern`'a gecilen `payload.ledIndexes` == `[buildLedSequence(config)[0].index]` olmali
    2. `"setConfig updates ledIndexes on next toggle"`:
       - Ilk toggle ile default config ile ac, kapat
       - `flow.setConfig(newConfig)` cagir
       - Tekrar ac, yeni config'in mapping'ine gore ledIndexes gonderilmis olmali

    **SONRA implement et (testPatternFlow.ts):**
    1. `CreateTestPatternFlowDeps`'e `initialConfig?: LedCalibrationConfig` ekle.
    2. Internal `let currentConfig: LedCalibrationConfig | null = deps.initialConfig ?? null` tut.
    3. `startPhysicalPattern` closure icinde `ledIndexes` hesapla:
       ```typescript
       const ledIndex = (() => {
         if (!currentConfig) return 0;
         const sequence = buildLedSequence(currentConfig);
         return sequence.length > 0 ? sequence[snapshot.markerIndex % sequence.length].index : 0;
       })();
       await deps.startPhysicalPattern({ ledIndexes: [ledIndex], frameMs: MARKER_ADVANCE_MS, brightness: 64 });
       ```
       NOT: `startPhysicalPattern` deps callback'ini dogrudan payload alan forma degistir — mevcut signature `() => Promise<void>` yeterli, fakat simdi `createDefaultTestPatternFlow`'da iceride buildLedSequence cagrisini yapar.
    4. `TestPatternFlow` arayuzune `setConfig(config: LedCalibrationConfig): void` ekle.
    5. `createTestPatternFlow` return objesine `setConfig: (config) => { currentConfig = config; }` ekle.
    6. `createDefaultTestPatternFlow` imzasini guncelle: `(getConnectionStatus, initialConfig?: LedCalibrationConfig)` parametresi alsin ve `createTestPatternFlow({..., initialConfig})` gecsin.
    7. `CalibrationOverlay.tsx` icindeki `createDefaultTestPatternFlow` cagrisi: ilk `initialConfig`'i gecebilir (optional — pas gecmek de gecerli, config sonradan setConfig ile verilecek).
    8. `CalibrationOverlay.tsx` icinde `editorState.current` degisince `flowRef.current.setConfig(editorState.current)` cagir (useEffect ile track et).

    **DIKKAT:** `startPhysicalPattern` imzasi `createTestPatternFlowDeps`'de `() => Promise<void>` olarak kalmali — ledIndexes hesabi `createDefaultTestPatternFlow` closure'inda yapilacak. `createTestPatternFlow` generic birakilir; deps kendi callback'ini tanımlar. Bu sekilde mevcut testler kirilmaz.
  </action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/testPatternFlow.test.ts</automated>
  </verify>
  <done>Tum testler gecti; yeni testler buildLedSequence entegrasyonunu dogruluyor; TypeScript derleme hatasi yok (yarn tsc --noEmit).</done>
</task>

</tasks>

<verification>
yarn vitest run src/features/calibration/state/testPatternFlow.test.ts
yarn tsc --noEmit
</verification>

<success_criteria>
- testPatternFlow.ts artik buildLedSequence() ile dinamik LED indeksi hesapliyor
- Sabit [0] gonderimi kaldiruldi
- Mevcut testler kirilmadi, yeni testler geciyor
- CAL-04 gereksinimi tam karsilaniyor
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-05-SUMMARY.md`
</output>
