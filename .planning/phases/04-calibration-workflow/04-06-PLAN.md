---
phase: 04-calibration-workflow
plan: "06"
type: execute
wave: 2
depends_on: [04-05]
files_modified:
  - src/features/calibration/model/indexMapping.ts
  - src/features/calibration/state/testPatternFlow.ts
  - src/features/calibration/state/testPatternFlow.test.ts
  - src/features/calibration/ui/CalibrationOverlay.tsx
autonomous: true
gap_closure: true
requirements: [CAL-01, CAL-02, CAL-03, CAL-04, UX-02]

must_haves:
  truths:
    - "Kullanici start anchor/direction degistirdiginde overlay'deki segment gostergesi ile fiziksel LED marker ayni sirayi isaret eder."
    - "Kullanici test pattern'i kapatip yeniden actiginda marker yeni kalibrasyon sirasindan devam eder; sabit ilk LED'e donmez."
    - "Kullanici farkli orientation kombinasyonlarinda da fiziksel LED ilerleyisini tutarli gorur."
  artifacts:
    - path: "src/features/calibration/model/indexMapping.ts"
      provides: "LedSequenceItem icin fiziksel indexi koruyan deterministic mapping"
    - path: "src/features/calibration/state/testPatternFlow.ts"
      provides: "markerIndex tabanli physical payload secimi"
    - path: "src/features/calibration/ui/CalibrationOverlay.tsx"
      provides: "preview segment gostergesinin buildLedSequence sirasindan turetilmesi"
    - path: "src/features/calibration/state/testPatternFlow.test.ts"
      provides: "mapping-order parity regresyon kapsamasi"
  key_links:
    - from: "src/features/calibration/model/indexMapping.ts"
      to: "src/features/calibration/state/testPatternFlow.ts"
      via: "buildLedSequence(config)[markerIndex].index -> startCalibrationTestPattern.ledIndexes"
      pattern: "buildLedSequence\(.*\)\[.*markerIndex"
    - from: "src/features/calibration/state/testPatternFlow.ts"
      to: "src/features/calibration/ui/CalibrationOverlay.tsx"
      via: "flow snapshot.markerIndex + sequence-derived segment label"
      pattern: "markerIndex"
---

<objective>
Phase 4 dogrulama gap'ini kapat: test pattern mapping-order parity, start anchor + direction degisikliklerinde hem UI preview'da hem fiziksel payload'da ayni sequence kaynagindan uretilmeli.

Purpose: 04-VERIFICATION.md icindeki kalan tek blocker'i (CAL-04) kapatirken mevcut wizard/advanced overlay akisinin (UX-02) ve segment/gap editor davranisinin (CAL-01/CAL-03) regresyona girmemesini saglamak.
Output: Fiziksel index semantigini koruyan mapping motoru, markerIndex bazli payload secimi, ve preview segment gostergesinde sequence uyumu.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-VERIFICATION.md
@.planning/phases/04-calibration-workflow/04-05-SUMMARY.md

<interfaces>
From src/features/calibration/model/indexMapping.ts:
```typescript
export interface LedSequenceItem {
  index: number;
  segment: LedSegmentKey;
  localIndex: number;
}

export function buildLedSequence(config: LedCalibrationConfig): LedSequenceItem[];
```

From src/features/calibration/state/testPatternFlow.ts:
```typescript
export interface TestPatternSnapshot {
  isEnabled: boolean;
  mode: "sending" | "preview-only";
  markerIndex: number;
  totalLeds: number;
  isBlockingSave: boolean;
}

export interface TestPatternFlow {
  getSnapshot: () => TestPatternSnapshot;
  setTotalLeds: (totalLeds: number) => TestPatternSnapshot;
  setConfig: (config: LedCalibrationConfig) => void;
  toggle: (enabled: boolean) => Promise<TestPatternSnapshot>;
  dispose: () => Promise<void>;
}
```

From src/features/calibration/ui/CalibrationOverlay.tsx:
```typescript
const markerSegment = resolveMarkerSegment(testPattern.markerIndex, editorState.current.counts);
```

Gap source (04-VERIFICATION):
- `buildLedSequence` cikisinda `index` alaninin yeniden numaralandirilmasi physical index semantigini zayiflatiyor.
- `startPhysicalPattern` payload'i markerIndex yerine sabit ilk elemani kullaniyor.
- `resolveMarkerSegment` sabit segment order ile hesaplandigi icin orientation etkisini gostermuyor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mapping-order parity'yi model, flow ve preview katmaninda sabitle</name>
  <files>src/features/calibration/model/indexMapping.ts, src/features/calibration/state/testPatternFlow.ts, src/features/calibration/state/testPatternFlow.test.ts, src/features/calibration/ui/CalibrationOverlay.tsx</files>
  <behavior>
    - Test 1: `buildLedSequence` cikisindaki `index` degeri canonical physical index semantigini korur; orientation degisse de yeniden 0..N-1 yazilmaz
    - Test 2: `createDefaultTestPatternFlow` toggle(true) aninda `ledIndexes` degerini `buildLedSequence(currentConfig)[snapshot.markerIndex]` kaynagindan turetir
    - Test 3: config degisimi (`setConfig`) sonrasi sonraki toggle acilisinda yeni mapping sirasina gore payload degisir
    - Test 4: Overlay preview segment etiketi sabit order helper'i yerine ayni sequence kaynagindan hesaplanir
  </behavior>
  <action>
    Once RED asamasinda `testPatternFlow.test.ts` icine markerIndex + setConfig parity testlerini ekle/guncelle; gerekirse model odakli bir ek assertion ile `buildLedSequence` index semantigini dogrula.

    Sonra GREEN asamasinda su duzeltmeleri uygula:
    1. `indexMapping.ts`: `buildLedSequence` donusunde `index` alanini yeniden atama; canonical sequence'ten gelen fiziksel index bilgisini koru.
    2. `testPatternFlow.ts`: default flow'da payload LED secimini sabit ilk eleman yerine aktif snapshot marker indeksine bagla. `sequence.length === 0` veya config yok durumunda fallback `[0]` kalabilir.
    3. `CalibrationOverlay.tsx`: `resolveMarkerSegment` sabit count-order helper'ini kaldir; segment etiketini `buildLedSequence(editorState.current)` ve `testPattern.markerIndex` uzerinden derive et ki preview metni ayni orientation mapping'i gostersin.
    4. Mevcut lifecycle kurallarini koru: disconnected preview-only ve dispose cleanup davranisi degismeyecek.

    Not: Kullanici karari geregi ayni editor (wizard + advanced) korunacak; yeni ayri preview modeli olusturma.
  </action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/testPatternFlow.test.ts</automated>
  </verify>
  <done>`04-VERIFICATION.md` gap maddesindeki uc eksik davranis (physical index semantigi, markerIndex payload, preview parity) kod seviyesinde kapanmis olur.</done>
</task>

</tasks>

<verification>
yarn vitest run src/features/calibration/state/testPatternFlow.test.ts
</verification>

<success_criteria>
- CAL-04 gap maddesinde belirtilen tum missing item'lar kod seviyesinde kapanir
- Preview ve physical payload ayni sequence kaynagina bagli calisir
- Wizard/advanced ayni overlay akisi korunur; save/dispose davranisinda regresyon olmaz
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-06-SUMMARY.md`
</output>
