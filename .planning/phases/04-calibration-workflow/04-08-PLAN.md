---
phase: 04-calibration-workflow
plan: "08"
type: execute
wave: 4
depends_on: [04-07]
files_modified:
  - src/features/calibration/model/indexMapping.ts
  - src/features/calibration/model/indexMapping.test.ts
  - src/features/calibration/state/testPatternFlow.ts
  - src/features/calibration/state/testPatternFlow.test.ts
  - src/features/calibration/ui/CalibrationOverlay.tsx
autonomous: true
gap_closure: true
requirements: [CAL-04]

must_haves:
  truths:
    - "Kullanici start anchor ve direction degistirdiginde test pattern fiziksel LED sirasini dogru yansitir."
    - "Kullanici preview uzerindeki aktif segment etiketinin fiziksel marker akisiyla ayni oldugunu gorur."
    - "Kullanici test pattern'i tekrar acip kapattiginda ayni konfigde deterministik parity davranisi alir."
  artifacts:
    - path: "src/features/calibration/model/indexMapping.ts"
      provides: "fiziksel index semantigini koruyan ve marker normalization sunan mapping contract"
    - path: "src/features/calibration/state/testPatternFlow.ts"
      provides: "markerIndex -> ledIndexes payload baglantisi"
    - path: "src/features/calibration/ui/CalibrationOverlay.tsx"
      provides: "preview segment ve siranin mapping sequence'ten turetilmesi"
    - path: "src/features/calibration/model/indexMapping.test.ts"
      provides: "orientation + physical-index parity regression kapsami"
    - path: "src/features/calibration/state/testPatternFlow.test.ts"
      provides: "markerIndex payload parity regression kapsami"
  key_links:
    - from: "src/features/calibration/model/indexMapping.ts"
      to: "src/features/calibration/state/testPatternFlow.ts"
      via: "resolveLedSequenceItem ile marker-index tabanli physical index lookup"
      pattern: "buildLedSequence|resolveLedSequenceItem"
    - from: "src/features/calibration/model/indexMapping.ts"
      to: "src/features/calibration/ui/CalibrationOverlay.tsx"
      via: "aynı sequence kaynagindan marker segment/order turetimi"
      pattern: "buildLedSequence|resolveLedSequenceItem"
---

<objective>
04-VERIFICATION.md'de acik kalan CAL-04 parity gap'ini model, flow ve overlay katmanlarinda tek mapping contract etrafinda kalici olarak kapat.

Purpose: Test pattern dogrulamasinda preview/fiziksel sira ayrismasini kalici olarak engelleyip kalibrasyon dogrulugunu sahada guvenilir hale getirmek.
Output: CAL-04 parity bug'ini bloke eden regression kapsami + model/flow/overlay parity wiring.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-CONTEXT.md
@.planning/phases/04-calibration-workflow/04-RESEARCH.md
@.planning/phases/04-calibration-workflow/04-VERIFICATION.md
@.planning/phases/04-calibration-workflow/04-06-SUMMARY.md

<interfaces>
From src/features/calibration/model/indexMapping.ts:
```typescript
export interface LedSequenceItem {
  index: number;
  segment: LedSegmentKey;
  localIndex: number;
}

export function resolveLedSequenceItem(
  sequence: LedSequenceItem[],
  markerIndex: number,
): LedSequenceItem | null;

export function buildLedSequence(config: LedCalibrationConfig): LedSequenceItem[];
```

From src/features/calibration/state/testPatternFlow.ts:
```typescript
export interface TestPatternFlow {
  getSnapshot: () => TestPatternSnapshot;
  setTotalLeds: (totalLeds: number) => TestPatternSnapshot;
  setConfig: (config: LedCalibrationConfig) => void;
  toggle: (enabled: boolean) => Promise<TestPatternSnapshot>;
  dispose: () => Promise<void>;
}

export function createDefaultTestPatternFlow(
  getConnectionStatus: () => Promise<{ connected: boolean }>,
  initialConfig?: LedCalibrationConfig,
): TestPatternFlow;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mapping contract'inda physical index semantigini ve marker normalization'ı regressiona-karsi kilitle</name>
  <files>src/features/calibration/model/indexMapping.ts, src/features/calibration/model/indexMapping.test.ts</files>
  <behavior>
    - Test 1: `buildLedSequence` farkli startAnchor/direction kombinasyonlarinda physical index kumesini kayipsiz korur
    - Test 2: `resolveLedSequenceItem` negatif ve tasan markerIndex degerlerini sequence uzunluguna gore normalize eder
    - Test 3: Orientation degisimi sadece traversal order'u etkiler, physical index semantigini bozmaz
  </behavior>
  <action>04-VERIFICATION gap reason'indaki physical index yeniden-numaralandirma riskini engelleyecek sekilde `buildLedSequence`/`resolveLedSequenceItem` davranisini testlerle acikca sabitle. User decision olarak kilitlenen canonical traversal (top -> right -> bottomRight -> bottomLeft -> left) korunacak; alternatif traversal deneme. Gerekiyorsa model kodunu testleri gecirecek minimum degisiklikle duzelt.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/model/indexMapping.test.ts</automated>
  </verify>
  <done>Model seviyesinde marker normalization + physical index semantigi deterministic ve regression-korumali hale gelir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test pattern physical payload'i aktif markerIndex'e bagla ve config degisimlerinde parity'yi koru</name>
  <files>src/features/calibration/state/testPatternFlow.ts, src/features/calibration/state/testPatternFlow.test.ts</files>
  <behavior>
    - Test 1: `toggle(true)` payload `ledIndexes` degerini `buildLedSequence(config)[markerIndex]` esdegerinden turetir
    - Test 2: `setConfig` sonrasi bir sonraki `toggle(true)` yeni sequence'e gore farkli physical index gonderebilir
    - Test 3: Baglanti yokken flow preview-only kalir ama marker hesap semantigi degismez
  </behavior>
  <action>`createDefaultTestPatternFlow` icindeki physical index cozumlemesini shared mapping contract'a bagla; hardcoded/tek-sabit index davranisi birakma. `startPhysicalPattern` payload'i marker-index tabanli ve config-duyarlı kalacak. Mevcut callback imza desenini bozma (research + onceki kararla uyumlu kal).</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/testPatternFlow.test.ts</automated>
  </verify>
  <done>Flow katmani, aktif marker ve guncel config'e gore fiziksel payload parity'sini deterministik sekilde uretir.</done>
</task>

<task type="auto">
  <name>Task 3: Overlay preview segment/order gostergesini ayni mapping kaynagindan turet</name>
  <files>src/features/calibration/ui/CalibrationOverlay.tsx</files>
  <action>Overlay icindeki aktif segment etiketi ve segment pill sirasi hesaplamalarini `buildLedSequence` + `resolveLedSequenceItem` sonucundan turet; sabit segment-order varsayimina geri donme. Bu task, user'in kilitledigi "preview ile fiziksel mapping parity" beklentisini UI katmaninda garanti eder. Localization keylerini degistirme; sadece mevcut key kullanimiyla parity wiring'i duzelt.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/testPatternFlow.test.ts -t "markerIndex|setConfig"</automated>
  </verify>
  <done>Preview'deki aktif segment ve sira gostergesi orientation degisimlerinde mapping-order ile tutarli kalir.</done>
</task>

</tasks>

<verification>
yarn vitest run src/features/calibration/model/indexMapping.test.ts -t "physical index|normalize"
yarn vitest run src/features/calibration/state/testPatternFlow.test.ts -t "markerIndex|setConfig"
</verification>

<success_criteria>
- CAL-04 icin markerIndex -> physical payload -> preview segment zinciri tek mapping contract etrafinda deterministic kalir
- Start anchor + direction kombinasyon degisimleri fiziksel payload ve preview metninde ayni anda gozlenebilir hale gelir
- 04-VERIFICATION'da listelenen kalan parity gap maddeleri kod ve regression seviyesinde kapanabilir duruma gelir
- Fiziksel parity checkpoint'i 04-11 kapanis planinda tek noktadan yurur
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-08-SUMMARY.md`
</output>
