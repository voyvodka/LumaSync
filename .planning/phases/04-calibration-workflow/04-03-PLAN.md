---
phase: 04-calibration-workflow
plan: "03"
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/features/calibration/calibrationApi.ts
  - src/features/calibration/state/testPatternFlow.ts
  - src/features/calibration/state/testPatternFlow.test.ts
  - src/features/calibration/ui/CalibrationOverlay.tsx
  - src-tauri/src/commands/calibration.rs
  - src-tauri/src/lib.rs
  - src/shared/contracts/device.ts
autonomous: true
requirements: [CAL-04]
must_haves:
  truths:
    - "Kullanici test pattern modunu V1 setinden secebilir: Gradient Flow, Gradient Static, Single Color, Segment Colors."
    - "Kullanici mod kontrollerini (mode, hiz, parlaklik, renk) degistirdiginde preview ve fiziksel payload ayni kontrol degerleriyle guncellenir."
    - "OS-level overlay acilamazsa test pattern baslatilmaz ve kullanici bloklanma nedenini gorur."
    - "Cihaz bagli degilken editor kaydetme akisi devam eder; test pattern durumu preview-only olarak acikca gorunur."
    - "Editor kapatilinca aktif test pattern/overlay temiz sekilde durdurulur."
  artifacts:
    - path: "src/features/calibration/calibrationApi.ts"
      provides: "start/stop calibration test pattern invoke wrapperlari ve mode/kontrol payload kontrati"
      exports: ["startCalibrationTestPattern", "stopCalibrationTestPattern", "CalibrationPatternMode", "CalibrationPatternControls"]
    - path: "src-tauri/src/commands/calibration.rs"
      provides: "test pattern baslat/durdur command handlerlari (mode/speed/brightness/color parse + guard)"
      exports: ["start_calibration_test_pattern", "stop_calibration_test_pattern"]
    - path: "src/features/calibration/state/testPatternFlow.ts"
      provides: "UI toggle + mode/kontrol + baglanti durumuna gore command orkestrasyonu"
      exports: ["createTestPatternFlow"]
    - path: "src/features/calibration/ui/CalibrationOverlay.tsx"
      provides: "V1 mode secici ve hiz/parlaklik/renk kontrol UI'si"
  key_links:
    - from: "src/features/calibration/ui/CalibrationOverlay.tsx"
      to: "src/features/calibration/state/testPatternFlow.ts"
      via: "toggle + mode/kontrol degisimlerinde flow.setControls"
      pattern: "setControls|toggle"
    - from: "src/features/calibration/calibrationApi.ts"
      to: "src-tauri/src/commands/calibration.rs"
      via: "invoke command adi ve payload (mode/speed/brightness/color) eslesmesi"
      pattern: "start_calibration_test_pattern"
    - from: "src-tauri/src/lib.rs"
      to: "tauri::generate_handler!"
      via: "command registration"
      pattern: "calibration"
---

<objective>
Canli dogrulama gereksinimini tamamlamak icin V1 test pattern modlarini ve kontrollerini overlay + flow + Rust command bridge katmanlarinda ayni payload zinciriyle bagla.

Purpose: CAL-04 kapsaminda kullanicinin mod/secenek degisikliginin hem preview'da hem fiziksel cihaza giden payload'da birebir eslesmesini saglamak.
Output: V1 mode/control payload kontrati, test-pattern flow control state'i ve Rust tarafinda bu payload'i dogrulayan command handlerlar.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-CONTEXT.md
@.planning/phases/04-calibration-workflow/04-RESEARCH.md
@src/shared/contracts/device.ts
@src/features/device/deviceConnectionApi.ts
@src-tauri/src/lib.rs

<interfaces>
From src/shared/contracts/device.ts:
```ts
export const DEVICE_COMMANDS = {
  LIST_PORTS: "list_serial_ports",
  CONNECT_PORT: "connect_serial_port",
  GET_CONNECTION_STATUS: "get_serial_connection_status",
  RUN_HEALTH_CHECK: "run_serial_health_check",
} as const;
```

From src/features/calibration/calibrationApi.ts:
```ts
import { invoke } from "@tauri-apps/api/core";
export interface CalibrationTestPatternStartPayload {
  mode: "gradient-flow" | "gradient-static" | "single-color" | "segment-colors";
  speed: number;
  brightness: number;
  color: { r: number; g: number; b: number };
  ledIndexes: number[];
}

export async function startCalibrationTestPattern(payload: CalibrationTestPatternStartPayload): Promise<void>;
export async function stopCalibrationTestPattern(): Promise<void>;
```

From src-tauri/src/lib.rs:
```rust
.invoke_handler(tauri::generate_handler![
  list_serial_ports,
  connect_serial_port,
  get_serial_connection_status,
  run_serial_health_check
])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: V1 mode/kontrol payload kontratini flow + command bridge katmaninda TDD ile kur</name>
  <files>src/shared/contracts/device.ts, src/features/calibration/calibrationApi.ts, src/features/calibration/state/testPatternFlow.ts, src/features/calibration/state/testPatternFlow.test.ts, src-tauri/src/commands/calibration.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Test 1: Toggle ON oldugunda `start_calibration_test_pattern` payload'inda mode degeri yalnizca su dortluden biri olur: `gradient-flow`, `gradient-static`, `single-color`, `segment-colors`.
    - Test 2: Hiz/parlaklik/renk kontrolleri degistiginde bir sonraki start payload'i ayni degerlerle guncellenir.
    - Test 3: Toggle OFF veya overlay unmount oldugunda stop command her durumda cagrilir.
    - Test 4: Cihaz bagli degilse flow `preview-only` durumunu set eder; save akisini engellemez.
  </behavior>
  <action>Kilit kullanici kararini birebir uygula: V1 test pattern mode seti tam olarak `Gradient Flow / Gradient Static / Single Color / Segment Colors` olacak, alternatif mod ekleme. `DEVICE_COMMANDS` kontratina calibration start/stop command ID'lerini ekle. `calibrationApi.ts` icinde mode/speed/brightness/color + ledIndexes alanlarini tasiyan typed payload kontratini tanimla. `testPatternFlow.ts` icinde `setControls` benzeri bir state API ile mode ve kontrolleri sakla; toggle/start aninda guncel control snapshot'ini payload'a yaz. Rust `commands/calibration.rs` tarafinda mode enum parse et, speed/brightness/color araligini dogrula (gecersizde explicit error dondur), baglanti durumu kontrolunu koru; `lib.rs` command kaydini guncelle.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/testPatternFlow.test.ts -t "mode payload|preview-only|cleanup"</automated>
  </verify>
  <done>Flow ve Rust command katmanlari V1 mode setini ve mode/hiz/parlaklik/renk kontrollerini ayni payload kontratiyla tasir; lifecycle kurallari korunur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CalibrationOverlay icinde V1 mode/kontrol UI'sini flow state'ine bagla</name>
  <files>src/features/calibration/ui/CalibrationOverlay.tsx, src/features/calibration/state/testPatternFlow.ts, src/features/calibration/state/testPatternFlow.test.ts</files>
  <behavior>
    - Test 1: Overlay'de V1 mode secici 4 secenekle render edilir ve secim `flow.setControls({ mode })` ile state'e yazilir.
    - Test 2: Hiz ve parlaklik kontrolleri degisince degerler normalize edilip (`min/max`) flow state'ine aktarilir.
    - Test 3: Single Color/Segment Colors modunda renk secimi yapildiginda sonraki toggle-start payload'inda ayni RGB degeri gorunur.
    - Test 4: Save butonu test pattern durumundan bagimsiz kalir.
    - Test 5: `OVERLAY_OPEN_FAILED` benzeri blocker durumunda toggle kapali/disabled kalir ve acik neden metni gorunur.
  </behavior>
  <action>`CalibrationOverlay.tsx` icinde mode dropdown/radio ve kontrol alanlarini acikca ekle: mode, hiz, parlaklik, renk. Renk kontrolu tek renk picker ile baslar; `segment-colors` icin ayni picker degeri payload'a tek source olarak aktarilir (v1 scope). UI state'i local tutulabilir ama test-pattern start oncesi mutlaka `testPatternFlow` kontrol snapshot'ina yazilmis olmali. Flow snapshot `blocked` oldugunda toggle disabled + acik hata metni goster; preview-only mod metnini koru. Not: Bu plan display switch/lifecycle implementasyonunu degil mode/control davranisini kapsar; display lifecycle ayri planda ele alinacak.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/testPatternFlow.test.ts -t "mode switch|control update|OVERLAY_OPEN_FAILED"</automated>
  </verify>
  <done>Overlay, V1 mode/kontrol setini acikca sunar; kullanici secimlerinin start payload'a yansidigi testlerle dogrulanir; blocker/preview-only UX net kalir.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/calibration/state/testPatternFlow.test.ts -t "mode payload|preview-only|cleanup"`
- `yarn vitest run src/features/calibration/state/testPatternFlow.test.ts -t "mode switch|control update|OVERLAY_OPEN_FAILED"`
</verification>

<success_criteria>
- V1 mode seti (`Gradient Flow`, `Gradient Static`, `Single Color`, `Segment Colors`) UI, flow ve Rust payload katmanlarinda birebir ayni adlarla kullanilir.
- Mode/hiz/parlaklik/renk kontrolleri degistiginde bir sonraki start payload'i ayni degerleri tasir.
- OS overlay acma hatasi oldugunda test pattern fallback preview'e dusmeden bloke edilir ve neden metni gorunur.
- Editor kapatma/sayfa gecisi durumunda donanima acik test pattern kalmaz.
- Cihaz bagli olmasa bile kaydetme ve editor akisi bloklanmaz.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-03-SUMMARY.md`
</output>
