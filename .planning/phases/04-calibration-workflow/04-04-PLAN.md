---
phase: 04-calibration-workflow
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/calibration/ui/CalibrationEditorCanvas.tsx
  - src/features/calibration/ui/CalibrationOverlay.tsx
  - src/locales/en/common.json
  - src/locales/tr/common.json
autonomous: true
gap_closure: true
requirements: [CAL-03]

must_haves:
  truths:
    - "Kullanici bottomGapPx degerini editor uzerinden ayarlayabilir"
    - "Gecersiz konfigurasyonla (sifir/negatif segment count veya negatif gap) kayit islemi bloke edilir"
    - "Validation hatasi mesaji kullaniciya gosterilir ve kayit butonu disabled kalir"
  artifacts:
    - path: "src/features/calibration/ui/CalibrationEditorCanvas.tsx"
      provides: "bottomGapPx number input ve onBottomGapChange callback"
    - path: "src/features/calibration/ui/CalibrationOverlay.tsx"
      provides: "Save oncesi validateCalibrationConfig cagrisi ve hata gosterimi"
  key_links:
    - from: "src/features/calibration/ui/CalibrationEditorCanvas.tsx"
      to: "src/features/calibration/ui/CalibrationOverlay.tsx"
      via: "onBottomGapChange callback ile updateEditorConfig({ bottomGapPx })"
      pattern: "onBottomGapChange"
    - from: "src/features/calibration/ui/CalibrationOverlay.tsx"
      to: "src/features/calibration/model/validation.ts"
      via: "save handler icinde validateCalibrationConfig(editorState.current)"
      pattern: "validateCalibrationConfig"
---

<objective>
CAL-03 gereksinimi: kullanici alt bosluk (bottomGapPx) alanini duzenleyebilmeli; gecersiz konfigurasyonlar kaydedilememeli.

Purpose: Verification gaps 1 ve 2'yi kapatir. Validation motoru (validation.ts) zaten mevcut ama save akisina baglanmamis; bottomGapPx icin editor UI kontrolu yok.
Output: bottomGapPx input alani CalibrationEditorCanvas'a eklenir; CalibrationOverlay save handler validateCalibrationConfig kullanarak gecersiz konfigurasyonlari bloke eder.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/features/calibration/model/contracts.ts:
```typescript
export interface LedCalibrationConfig {
  templateId?: string;
  counts: LedSegmentCounts;
  bottomGapPx: number;
  startAnchor: LedStartAnchor;
  direction: LedDirection;
  totalLeds: number;
}
```

From src/features/calibration/model/validation.ts:
```typescript
export type CalibrationValidationCode =
  | "COUNTS_REQUIRED"
  | "SEGMENT_NON_POSITIVE"
  | "TOTAL_MISMATCH"
  | "BOTTOM_GAP_NEGATIVE";

export interface CalibrationValidationResult {
  ok: boolean;
  errors: CalibrationValidationError[];
}

export function validateCalibrationConfig(config: LedCalibrationConfig): CalibrationValidationResult;
```

From src/features/calibration/state/calibrationEditorState.ts:
```typescript
export function updateEditorConfig(
  state: CalibrationEditorState,
  patch: EditorConfigPatch,  // EditorConfigPatch: { bottomGapPx?: number; ... }
): CalibrationEditorState;
```

CalibrationEditorCanvas mevcut props (genisletilecek):
```typescript
interface CalibrationEditorCanvasProps {
  config: LedCalibrationConfig;
  isDirty: boolean;
  onCountChange: (segment: ..., value: number) => void;
  onStartAnchorChange: (anchor: LedStartAnchor) => void;
  onDirectionChange: (direction: LedDirection) => void;
  onResetTemplate: () => void;
  // EKLENECEK:
  onBottomGapChange: (px: number) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CalibrationEditorCanvas'a bottomGapPx input ekle</name>
  <files>src/features/calibration/ui/CalibrationEditorCanvas.tsx, src/locales/en/common.json, src/locales/tr/common.json</files>
  <behavior>
    - CalibrationEditorCanvas `onBottomGapChange: (px: number) => void` prop kabul eder
    - `calibration.editor.bottomGap` i18n key'i ile etiketlenmi&#351; number input render edilir
    - Input `type="number" min={0}` ve `value={config.bottomGapPx}` ile baglanir
    - onChange: `onBottomGapChange(Number.isFinite(val) ? Math.max(0, val) : 0)` cagrisi yapilir
    - EN: "Bottom gap (px)", TR: "Alt bosluk (px)"
  </behavior>
  <action>
    1. `CalibrationEditorCanvasProps` arayuzune `onBottomGapChange: (px: number) => void` ekle.
    2. Mevcut count fields gridi altina (startAnchor/direction row'undan once) bottomGapPx icin ayri bir `label` blogu ekle. Mevcut `sm:grid-cols-2` layout'unu kullan, input style: `type="number" min={0}` ve `value={config.bottomGapPx}`.
    3. `onChange`: `onBottomGapChange(Number.isFinite(Number(event.target.value)) ? Math.max(0, Number(event.target.value)) : 0)`.
    4. EN `src/locales/en/common.json` dosyasindaki `calibration.editor` altina `"bottomGap": "Bottom gap (px)"` ekle.
    5. TR `src/locales/tr/common.json` dosyasindaki `calibration.editor` altina `"bottomGap": "Alt bosluk (px)"` ekle.
    6. `CalibrationOverlay.tsx` icindeki `CalibrationEditorCanvas` kullanimi genislet: `onBottomGapChange={(px) => setEditorState((prev) => updateEditorConfig(prev, { bottomGapPx: px }))}` prop'unu ekle.
  </action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>CalibrationEditorCanvas bottomGapPx inputu render ediyor, prop tipi check gecti, i18n keyleri her iki locale'de mevcut, Overlay bottomGapPx degerini state'e yaziyor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overlay save handler'ina validateCalibrationConfig baglantisi</name>
  <files>src/features/calibration/ui/CalibrationOverlay.tsx</files>
  <behavior>
    - Save butonu tiklandiginda once `validateCalibrationConfig(editorState.current)` cagrisi yapilir
    - `result.ok === false` ise shellStore.save cagrilmaz, `isSaving` false kalir
    - Validation hatasi local state olarak tutulur (`validationErrors: CalibrationValidationError[] | null`)
    - Hatalar UI'da kullaniciya gosterilir (kirmizi metin, save butonunun ustunde)
    - Kayit basarili olursa validationErrors temizlenir
    - Editor config degistiginde (setEditorState cagrisi) validationErrors sifirlanir
  </behavior>
  <action>
    1. `CalibrationOverlay` icine `const [validationErrors, setValidationErrors] = useState<CalibrationValidationError[] | null>(null)` ekle.
    2. `validateCalibrationConfig`'i `src/features/calibration/model/validation.ts`'den import et.
    3. Save button onClick handler'ini guncelle:
       ```typescript
       const result = validateCalibrationConfig(editorState.current);
       if (!result.ok) {
         setValidationErrors(result.errors);
         setIsSaving(false);
         return;
       }
       setValidationErrors(null);
       // ...mevcut shellStore.save akisi...
       ```
    4. `updateEditorConfig` cagrisi yapilan her `setEditorState` satirindan sonra `setValidationErrors(null)` ekle (onCountChange, onStartAnchorChange, onDirectionChange, onBottomGapChange callback'leri icinde).
    5. Footer div icinde, save butonu ONCESINDE validation hata listesi render et:
       ```tsx
       {validationErrors && validationErrors.length > 0 ? (
         <ul className="mr-auto flex flex-col gap-1 text-xs text-rose-400">
           {validationErrors.map((e) => (
             <li key={e.field}>{e.code}: {e.field}</li>
           ))}
         </ul>
       ) : null}
       ```
    **NOT:** Hata mesajlari validation.ts'den gelen `code` + `field` ikili ile gosterilir (i18n'e sarmalanmaz — bu gap closure icin yeterli). Gelecekte i18n'e alinabilir.
  </action>
  <verify>
    <automated>yarn tsc --noEmit</automated>
  </verify>
  <done>TypeScript derleme hatasi yok; save handler validateCalibrationConfig cagiriyor; gecersiz config ile kayit engelleniyor; hata kodu+alan bilgisi UI'da gorunuyor.</done>
</task>

</tasks>

<verification>
yarn tsc --noEmit
</verification>

<success_criteria>
- CalibrationEditorCanvas bottomGapPx input render ediyor ve Overlay state'e yaziyor
- Overlay save handler validateCalibrationConfig() cagiriyor
- Gecersiz config (ornegin segment count 0 veya negatif bottomGapPx) kayit engelliyor ve hata gosteriyor
- CAL-03 gereksinimi tam olarak karsilaniyor
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-04-SUMMARY.md`
</output>
