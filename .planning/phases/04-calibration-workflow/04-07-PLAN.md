---
phase: 04-calibration-workflow
plan: "07"
type: execute
wave: 3
depends_on: [04-06]
files_modified:
  - src/features/calibration/model/indexMapping.test.ts
  - src/features/calibration/state/testPatternFlow.test.ts
autonomous: true
gap_closure: true
requirements: [CAL-04]

must_haves:
  truths:
    - "Kullanici parity davranisinin (preview + fiziksel marker) yeni degisikliklerden sonra da bozulmadigini test raporuyla gorebilir."
    - "Kullanici farkli start anchor/direction kombinasyonlarinda test pattern'in tutarli kaldigini sahada dogrulayabilir."
    - "Kullanici ayni cihazda tekrar denediginde onceki parity sonucunu tekrar alir (regresyon yok)."
  artifacts:
    - path: "src/features/calibration/model/indexMapping.test.ts"
      provides: "orientation degisiminde physical index korunumu regresyon testi"
    - path: "src/features/calibration/state/testPatternFlow.test.ts"
      provides: "markerIndex->payload parity regresyon testi"
  key_links:
    - from: "src/features/calibration/model/indexMapping.test.ts"
      to: "src/features/calibration/model/indexMapping.ts"
      via: "physical index semantics assertion"
      pattern: "physical index|orientation"
    - from: "src/features/calibration/state/testPatternFlow.test.ts"
      to: "src/features/calibration/state/testPatternFlow.ts"
      via: "markerIndex -> ledIndexes parity assertion"
      pattern: "markerIndex|ledIndexes"
---

<objective>
04-06'daki parity duzeltmelerini regresyon testleriyle kilitle ve fiziksel dogrulama kapisini final parity degisiklik planina tasi.

Purpose: Kapanan CAL-04 parity bug'inin yeniden acilmasini engellemek ve insan dogrulamasini son kod degisiklikten sonraya birakmak.
Output: Hardening odakli regression test seti.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-VERIFICATION.md
@.planning/phases/04-calibration-workflow/04-06-SUMMARY.md

<interfaces>
From src/features/calibration/model/indexMapping.ts:
```typescript
export interface LedSequenceItem {
  index: number;
  segment: LedSegmentKey;
  localIndex: number;
}

export function buildLedSequence(config: LedCalibrationConfig): LedSequenceItem[];
```

From src/features/calibration/state/testPatternFlow.ts:
```typescript
export interface TestPatternSnapshot {
  isEnabled: boolean;
  mode: "sending" | "preview-only";
  markerIndex: number;
  totalLeds: number;
  isBlockingSave: boolean;
}

export interface TestPatternFlow {
  getSnapshot: () => TestPatternSnapshot;
  setTotalLeds: (totalLeds: number) => TestPatternSnapshot;
  setConfig: (config: LedCalibrationConfig) => void;
  toggle: (enabled: boolean) => Promise<TestPatternSnapshot>;
  dispose: () => Promise<void>;
}
```

From src/features/calibration/ui/CalibrationOverlay.tsx:
```typescript
function resolveMarkerSegment(markerIndex: number, config: LedCalibrationConfig) {
  const sequence = buildLedSequence(config);
  const normalizedMarkerIndex = ((markerIndex % sequence.length) + sequence.length) % sequence.length;
  return sequence[normalizedMarkerIndex]?.segment ?? "top";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CAL-04 parity davranisini regression-hardening testleriyle kilitle</name>
  <files>src/features/calibration/model/indexMapping.test.ts, src/features/calibration/state/testPatternFlow.test.ts</files>
  <behavior>
    - Test 1: `buildLedSequence` orientation degisse bile physical index kumesini (0..N-1) kayipsiz korur
    - Test 2: `buildLedSequence` icin ayni startAnchor'da CW ve CCW ilk marker physical indexleri farkli kombinasyonlarda deterministic kalir
    - Test 3: `createDefaultTestPatternFlow.toggle(true)` `ledIndexes` degerini aktif `markerIndex` uzerinden sequence'ten uretir
    - Test 4: `setConfig` sonrasi sonraki toggle acilisinda payload yeni sequence'e gore degisir
  </behavior>
  <action>Bu plan implementation degil hardening planidir: production kodu degistirme. Var olan test dosyalarini genisletip parity bug'ini tekrar acacak degisiklikleri yakalayacak assertion'lar ekle. `indexMapping.test.ts` tarafinda physical index semantigi ve orientation determinism'i, `testPatternFlow.test.ts` tarafinda markerIndex->payload parity ve setConfig sonrasi yeniden-acilis davranisini net senaryolarla kilitle.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/model/indexMapping.test.ts -t "physical index|orientation" && yarn vitest run src/features/calibration/state/testPatternFlow.test.ts -t "markerIndex|setConfig"</automated>
  </verify>
  <done>CAL-04 parity davranisini bozan degisiklikler test seviyesinde aninda yakalanir.</done>
</task>

</tasks>

<verification>
yarn vitest run src/features/calibration/model/indexMapping.test.ts -t "physical index|orientation"
yarn vitest run src/features/calibration/state/testPatternFlow.test.ts -t "markerIndex|setConfig"
</verification>

<success_criteria>
- 04-06'da kapanan parity davranislari test seviyesinde kalici regresyon guvencesi kazanir
- CAL-04 davranisi startAnchor/direction kombinasyonlarinda tekrarli denemelerde deterministic kalir
- Fiziksel parity checkpoint'i final parity degisiklik planinda (04-08) calistirilir
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-07-SUMMARY.md`
</output>
