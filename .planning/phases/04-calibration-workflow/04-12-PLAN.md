---
phase: 04-calibration-workflow
plan: "12"
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/App.tsx
  - src/features/settings/SettingsLayout.tsx
  - src/features/settings/sections/CalibrationSection.tsx
  - src/features/settings/sections/CalibrationSection.test.tsx
  - src/features/calibration/state/entryFlow.test.ts
  - src/locales/en/common.json
  - src/locales/tr/common.json
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "Kullanici ilk cihaz baglantisinda kalibrasyon kaydi yoksa wizard overlay'i otomatik gorur."
    - "Kullanici kalibrasyon kaydi olusturduktan sonra uygulama yeniden acildiginda wizard tekrar zorla acilmaz."
    - "Overlay acilis karari cihaz baglanti durumu + kayitli ledCalibration varligi birlikte kontrol edilerek verilir."
    - "Kullanici Settings sidebar'da Calibration bolumunde ozeti gorur ve Duzenle butonuyla ayni overlay editoru acar."
  artifacts:
    - path: "src/App.tsx"
      provides: "ilk baglanti + hasCalibration guard ile openCalibrationOverlay tetigi"
    - path: "src/features/settings/sections/CalibrationSection.tsx"
      provides: "Calibration ozet karti ve Duzenle tetigi"
    - path: "src/features/settings/SettingsLayout.tsx"
      provides: "Settings sidebar/content icinde Calibration section wiring"
    - path: "src/features/calibration/state/entryFlow.test.ts"
      provides: "first-connection auto-open ve hasCalibration bypass regresyon kapsami"
    - path: "src/features/settings/sections/CalibrationSection.test.tsx"
      provides: "ozet render + Duzenle callback davranisi regression kapsamasi"
  key_links:
    - from: "src/App.tsx"
      to: "src/features/calibration/state/entryFlow.test.ts"
      via: "entry-flow guard senaryolari"
      pattern: "openCalibrationOverlay|hasCalibration"
    - from: "src/App.tsx"
      to: "src/features/device/useDeviceConnection.ts"
      via: "ilk baglanti trigger kaynagi"
      pattern: "connected"
    - from: "src/features/settings/SettingsLayout.tsx"
      to: "src/features/settings/sections/CalibrationSection.tsx"
      via: "activeSection=CALIBRATION iken ozet + Duzenle UI render"
      pattern: "SECTION_IDS.CALIBRATION"
    - from: "src/features/settings/sections/CalibrationSection.tsx"
      to: "src/App.tsx"
      via: "Duzenle tiklamasi -> openCalibrationOverlay"
      pattern: "onEditCalibration"
---

<objective>
04-02 kapsamindan ayrilan giris-wiring mini dilimini tamamla: ilk baglanti auto-open davranisini ve Settings > Calibration (ozet + Duzenle) giris noktasini netlestir.

Purpose: 04-02 planini daraltip kalite riskini azaltirken UX-02'nin iki kilit davranisini (ilk baglanti auto-open + settings'ten yeniden acilis) eksiksiz tutmak.
Output: App-level auto-open guard, Settings Calibration section wiring ve hedefli regresyon testleri.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calibration-workflow/04-CONTEXT.md
@.planning/phases/04-calibration-workflow/04-02-PLAN.md
@src/App.tsx
@src/features/settings/SettingsLayout.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: App-level first-connection auto-open guard'ini TDD ile bagla</name>
  <files>src/App.tsx, src/features/calibration/state/entryFlow.test.ts</files>
  <behavior>
    - Test 1: `connected=true` ve `ledCalibration` yoksa `openCalibrationOverlay` bir kez tetiklenir.
    - Test 2: `ledCalibration` varsa ayni baglanti senaryosunda overlay auto-open tetiklenmez.
    - Test 3: Baglanti false->true gecisinde guard bir kez calisir; rerender'da tekrar acilmaz.
  </behavior>
  <action>`App.tsx` icinde baglanti ve shell state'i birlestiren hedefli entry-flow guard uygula. Guard, sadece ilk uygun baglanti aninda ve kalibrasyon kaydi yokken overlay acsin. Karari tek yerde topla (daginik if bloklari olusturma) ve mevcut Settings akisini bozmadan callback ile acilis yap. `entryFlow.test.ts` dosyasina yukaridaki uc davranisi kapsayan net regression testleri ekle.</action>
  <verify>
    <automated>yarn vitest run src/features/calibration/state/entryFlow.test.ts -t "auto-open|hasCalibration|first connection"</automated>
  </verify>
  <done>UX-02 zorunlu wizard tetigi App seviyesinde deterministic olur ve hedefli testlerle korunur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings sidebar'a Calibration ozet + Duzenle giris noktasini bagla</name>
  <files>src/App.tsx, src/features/settings/SettingsLayout.tsx, src/features/settings/sections/CalibrationSection.tsx, src/features/settings/sections/CalibrationSection.test.tsx, src/locales/en/common.json, src/locales/tr/common.json</files>
  <behavior>
    - Test 1: `activeSection=SECTION_IDS.CALIBRATION` iken Settings content'te CalibrationSection render olur.
    - Test 2: CalibrationSection kayitli `ledCalibration` varsa toplam LED + template ozetini, yoksa "not calibrated" durumunu gosterir.
    - Test 3: `Duzenle` butonu tiklaninca `onEditCalibration` callback'i cagrilir ve App tarafinda ayni calibration overlay acilir.
  </behavior>
  <action>Kilit kullanici kararini birebir uygula: Settings sidebar'a `Calibration` bolumu eklenecek ve bu bolumde ozet + `Duzenle` butonu olacak. `SettingsLayout.tsx` icindeki section switch/menu wiring'ini `SECTION_IDS.CALIBRATION` ile genislet, yeni `CalibrationSection.tsx` olustur (summary: toplam LED, secili template adi veya manuel etiketi). `App.tsx` tarafindan `onEditCalibration` callback'i gecir ve bu callback mevcut overlay acma mekanizmasini cagirarak ayni editoru acsin (yeni farkli modal olusturma). Gerekli copy icin `common.json` EN/TR locale dosyalarina yalnizca calibration summary/edit keylerini ekle. Deferred scope'a girmeden (profile, multi-monitor calibration) sadece v1 ozet + acma davranisini tamamla.</action>
  <verify>
    <automated>yarn vitest run src/features/settings/sections/CalibrationSection.test.tsx</automated>
  </verify>
  <done>Settings > Calibration bolumu ozet + Duzenle ile calisir; Duzenle aksiyonu mevcut calibration overlay'ini acarak ayni editora geri giris saglar.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/calibration/state/entryFlow.test.ts -t "auto-open|hasCalibration|first connection"`
- `yarn vitest run src/features/settings/sections/CalibrationSection.test.tsx`
</verification>

<success_criteria>
- Ilk baglanti auto-open davranisi yalnizca kalibrasyon yoksa calisir.
- Kalibrasyon kaydi olan kullanicida yeniden acilista gereksiz wizard acilmaz.
- Settings > Calibration bolumu ozet + Duzenle aksiyonunu sunar ve ayni overlay editorunu acar.
- Entry-flow ve Settings giris davranislari regression testleriyle hizli geri bildirimde dogrulanir.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calibration-workflow/04-12-SUMMARY.md`
</output>
