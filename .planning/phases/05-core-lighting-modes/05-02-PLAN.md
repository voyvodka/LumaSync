---
phase: 05-core-lighting-modes
plan: "02"
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src-tauri/src/commands/lighting_mode.rs
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
  - src-tauri/Cargo.toml
autonomous: true
requirements: [MODE-01, MODE-02]
must_haves:
  truths:
    - "Kullanici Ambilight modunu actiginda backend tek aktif runtime session baslatir."
    - "Kullanici Solid mode'a gectiginde onceki mode runtime'i durur ve sabit renk cikisi uygulanir."
    - "Ayni anda birden fazla mode runtime'i calismaz."
  artifacts:
    - path: "src-tauri/src/commands/lighting_mode.rs"
      provides: "mode set/stop/status command'lari ve runtime owner state"
      exports: ["set_lighting_mode", "stop_lighting", "get_lighting_mode_status"]
    - path: "src-tauri/src/lib.rs"
      provides: "lighting mode command registration + managed state wiring"
    - path: "src-tauri/capabilities/default.json"
      provides: "frontend invoke permissionlari"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/commands/lighting_mode.rs"
      via: "tauri::generate_handler command registration"
      pattern: "set_lighting_mode|stop_lighting|get_lighting_mode_status"
    - from: "src-tauri/src/commands/lighting_mode.rs"
      to: "src-tauri/src/commands/device_connection.rs"
      via: "serial connection state kontrolu"
      pattern: "SerialConnectionState"
---

<objective>
Mode secimlerini gercek runtime davranisina donusturen Rust command katmanini uygula.

Purpose: MODE-01 ve MODE-02 gereksinimlerinde gereken runtime owner, lifecycle cancellation ve backend source-of-truth desenini saglamak.
Output: `lighting_mode` command modulu, command registration/capability wiring ve command-level test kapsami.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-lighting-modes/05-RESEARCH.md
@.planning/phases/05-core-lighting-modes/05-01-PLAN.md
@src-tauri/src/lib.rs
@src-tauri/src/commands/device_connection.rs

<interfaces>
From src-tauri/src/commands/device_connection.rs:
```rust
pub struct SerialConnectionState {
  pub(crate) last_status: Mutex<SerialConnectionStatus>,
}
```

From src/features/mode/model/contracts.ts:
```typescript
export type LightingModeKind = "off" | "ambilight" | "solid";
export interface LightingModeConfig { kind: LightingModeKind; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rust tarafinda transactional lighting mode runtime'i uygula</name>
  <files>src-tauri/src/commands/lighting_mode.rs, src-tauri/Cargo.toml</files>
  <behavior>
    - Test 1: `set_lighting_mode(ambilight)` cagrisi once onceki mode'u durdurup yeni runtime state'i aktif eder.
    - Test 2: `set_lighting_mode(solid)` cagrisi tek-seferlik solid payload uygulayip active mode'u `solid` yapar.
    - Test 3: Ardarda mode degisimlerinde paralel worker sarkmasi olmaz (tek aktif runtime).
  </behavior>
  <action>`lighting_mode.rs` olustur: `LightingRuntimeState` (aktif mode + cancel token/handle) ve `set_lighting_mode`, `stop_lighting`, `get_lighting_mode_status` command'larini ekle. Gecis yolu zorunlu olarak `stop_previous -> start_next -> commit_active` sirasini izlesin. MODE-01 icin Windows-first capture worker'i tanimla (research karari: `windows-capture`), MODE-02 icin solid payload yolunu ayri branch olarak uygula. Calisma durumu code/message/details yapisinda donsun; bagli cihaz yoksa deterministic hata kodu donup state'i degistirme.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml lighting_mode</automated>
  </verify>
  <done>Mode runtime tek sahipli ve cancellable hale gelir; Ambilight ve Solid komutlari backend tarafinda calisir.</done>
</task>

<task type="auto">
  <name>Task 2: Command registration ve capability surface'i tamamla</name>
  <files>src-tauri/src/lib.rs, src-tauri/capabilities/default.json</files>
  <action>`lib.rs` icinde `commands::lighting_mode` modulunu ekle, gerekli managed state'i register et ve `generate_handler` listesine uc mode command'ini dahil et. `default.json` capability dosyasini yeni command invoke surface'i ile guncelle. Mevcut calibration/device command izinlerini bozma; append-only ilerle.</action>
  <verify>
    <automated>cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <done>Frontend yeni mode command'larini invoke edebilir ve Tauri build capability/registration acisindan green kalir.</done>
</task>

</tasks>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml lighting_mode`
- `cargo check --manifest-path src-tauri/Cargo.toml`
</verification>

<success_criteria>
- Backend mode source-of-truth tek runtime owner ile calisir.
- Mode degisimi sirasinda stop-before-start kurali testle sabitlenir.
- Ambilight/Solid command surface frontend tarafindan cagirilabilir ve derleme temizdir.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-lighting-modes/05-02-SUMMARY.md`
</output>
