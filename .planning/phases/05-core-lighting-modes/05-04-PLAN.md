---
phase: 05-core-lighting-modes
plan: "04"
type: execute
wave: 4
depends_on: [05-02, 05-03]
files_modified:
  - src-tauri/src/commands/led_output.rs
  - src-tauri/src/commands/lighting_mode.rs
  - src-tauri/src/lib.rs
  - .planning/phases/05-core-lighting-modes/05-HARDWARE-UAT.md
  - .planning/phases/05-core-lighting-modes/05-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: [MODE-01, MODE-02]
must_haves:
  truths:
    - "Kullanici Ambilight moduna gectiginde LED seridi ekran hareketlerini gercek zamanli takip eder."
    - "Kullanici Solid moduna gectiginde secilen renk/parlaklik fiziksel cikisa uygulanir."
    - "Kullanici modlar arasinda gecis yaparken kalibrasyon kaydi korunur."
  artifacts:
    - path: "src-tauri/src/commands/led_output.rs"
      provides: "fiziksel cihaza frame/payload yazan output bridge ve paketleme kurallari"
      exports: ["LedOutputBridge", "apply_solid_payload", "send_ambilight_frame"]
    - path: "src-tauri/src/commands/lighting_mode.rs"
      provides: "mode transaction akisi icinde gercek output uygulamasi ve ambilight worker entegrasyonu"
      exports: ["set_lighting_mode", "stop_lighting", "get_lighting_mode_status"]
    - path: ".planning/phases/05-core-lighting-modes/05-VERIFICATION.md"
      provides: "MODE-01/MODE-02 gap closure sonrasi guncel verifier kaydi"
  key_links:
    - from: "src-tauri/src/commands/lighting_mode.rs"
      to: "src-tauri/src/commands/led_output.rs"
      via: "solid apply ve ambilight frame gonderimi"
      pattern: "apply_solid_payload|send_ambilight_frame"
    - from: "src-tauri/src/commands/lighting_mode.rs"
      to: "src-tauri/src/commands/device_connection.rs"
      via: "connected port guard ve baglanti durum kontrolu"
      pattern: "SerialConnectionState|DEVICE_NOT_CONNECTED"
    - from: "src/App.tsx"
      to: "src/features/shell/windowLifecycle.ts"
      via: "lightingMode partial persistence"
      pattern: "saveShellState\(\{ lightingMode"
---

<objective>
05-VERIFICATION raporunda acik kalan MODE-01/MODE-02 kod seviyesi blocker'larini kapat: runtime'i no-op/stub durumundan cikarip fiziksel LED output yoluna bagla ve UAT/verification kanitlarini teknik davranisla hizala.

Purpose: Phase 5'in kapanisini sadece UI/command wiring ile degil, gercek cihaz output davranisiyla da dogrulanabilir hale getirmek.
Output: Rust output bridge implementasyonu, lighting_mode runtime entegrasyonu, guncellenmis hardware UAT ve verification kapanis kaydi.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-lighting-modes/05-RESEARCH.md
@.planning/phases/05-core-lighting-modes/05-VERIFICATION.md
@.planning/phases/05-core-lighting-modes/05-HARDWARE-UAT.md
@.planning/phases/05-core-lighting-modes/05-02-SUMMARY.md
@.planning/phases/05-core-lighting-modes/05-03-SUMMARY.md
@src-tauri/src/commands/lighting_mode.rs
@src-tauri/src/commands/device_connection.rs

<interfaces>
From src-tauri/src/commands/lighting_mode.rs:
```rust
#[derive(Clone, Deserialize, Serialize, PartialEq, Debug)]
#[serde(rename_all = "camelCase")]
pub struct LightingModeConfig {
    pub kind: LightingModeKind,
    pub solid: Option<SolidColorPayload>,
    pub ambilight: Option<AmbilightPayload>,
}

#[tauri::command]
pub fn set_lighting_mode(
    payload: LightingModeConfig,
    runtime_state: State<'_, LightingRuntimeState>,
    connection_state: State<'_, SerialConnectionState>,
) -> Result<LightingModeCommandResult, String>;
```

From src-tauri/src/commands/device_connection.rs:
```rust
pub struct SerialConnectionStatus {
    pub port_name: Option<String>,
    pub connected: bool,
    pub status: CommandStatus,
    pub updated_at_unix_ms: u128,
}

pub struct SerialConnectionState {
    pub(crate) last_status: Mutex<SerialConnectionStatus>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Solid/Ambilight icin fiziksel LED output bridge'ini test-first kur</name>
  <files>src-tauri/src/commands/led_output.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Test 1: Solid payload (r,g,b,brightness) fiziksel cikisa gidecek byte dizisine deterministic cevrilir.
    - Test 2: Output bridge bagli port bilgisini kullanarak payload'i cihaza yazar; yazma hatasinda acik hata kodu doner.
    - Test 3: Ambilight frame gonderim helper'i tek frame icin ayni packet kurallarini kullanir.
  </behavior>
  <action>Yeni `led_output.rs` modulu olustur ve no-op `apply_solid_payload` yerine kullanilacak gercek output yolunu burada topla. `SerialConnectionState` icinden son bagli port bilgisini okuyan bir bridge yaz; port yok/kapali/permission durumlarini kodlanmis hata sebebiyle dondur. Paketleme kurali tek yerde kalsin (solid ve ambilight ayni encoder'i kullansin). Bu modulu `lib.rs` commands altina ekle; mevcut command registration'i bozma.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml led_output</automated>
  </verify>
  <done>Solid ve frame gonderim artik no-op degildir; output bridge testleri packet ve hata semantigini kilitler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lighting_mode runtime'ini gercek output pipeline'ina bagla</name>
  <files>src-tauri/src/commands/lighting_mode.rs</files>
  <behavior>
    - Test 1: `set_lighting_mode(solid)` cagrisi bridge'e gercek payload yazar; basarisizsa `SOLID_MODE_APPLY_FAILED` doner.
    - Test 2: `set_lighting_mode(ambilight)` worker'i capture->sample->send akisini baslatir ve en az bir frame gonderimini kanitlayan test gecer.
    - Test 3: Ambilight -> Solid/Off gecisinde eski worker durur, yeni cikis uygulanir, tek aktif runtime kurali korunur.
  </behavior>
  <action>`lighting_mode.rs` icinde `apply_solid_payload` ve ambilight worker'i `led_output` bridge'i ile bagla. Ambilight branch'inde `thread::sleep` no-op dongusunu kaldir; research kararina uygun sekilde ekran frame kaynagi + LED frame uretimi + cihaz gonderimini ayni worker akisina yerlestir. Device disconnected guard, stop-before-start transaction, ve mevcut status code kontratlarini koru. Testlerde dis kaynaga bagimli olmayan bir fake sender/capture adapter kullanarak entegrasyon seviyesinde "payload uygulandi" kaniti uret.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml lighting_mode</automated>
  </verify>
  <done>MODE-01 ve MODE-02 backend yolunda stub kalmaz; runtime output davranisi kod ve testle kanitlanir.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fiziksel UAT'i yeniden calistir ve verification kapanisini senkronize et</name>
  <files>.planning/phases/05-core-lighting-modes/05-HARDWARE-UAT.md, .planning/phases/05-core-lighting-modes/05-VERIFICATION.md</files>
  <action>Task 1-2 sonrasi donanim testini yeniden uygula ve onceki PASS kaydi ile kod davranisi arasindaki celiskiyi kapat. `05-HARDWARE-UAT.md` icinde test ortami/sonuc tablosunu yeni kosuya gore guncelle; ardindan `05-VERIFICATION.md` dosyasinda gap durumunu tekrar degerlendirip MODE-01 ve MODE-02 icin blocker satirlarini kapat veya kalan farki net yaz.</action>
  <what-built>No-op/stub backend davranisi output bridge + worker pipeline ile degistirildi; bu adim fiziksel cihaz sonucunu teknik implementasyonla hizalar.</what-built>
  <how-to-verify>
    1. Cihaz bagli ve kalibrasyon kayitli durumda app'i ac.
    2. `Ambilight` sec, kontrastli ekran hareketleri olustur; LED'in anlik takip ettigini gozlemle.
    3. `Solid` sec, birden fazla renk/parlaklik degeri dene; tum seritte uniform cikisi teyit et.
    4. `Ambilight <-> Solid` gecisini en az 5 kez tekrarla, sonra `Off` sec ve fiziksel cikisin durdugunu dogrula.
    5. Sonuclari `05-HARDWARE-UAT.md` tablosuna isle; `05-VERIFICATION.md` status ve gaps bolumlerini yeni sonuca gore guncelle.
  </how-to-verify>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml led_output lighting_mode</automated>
  </verify>
  <done>UAT PASS/FAIL sonucu kod gercegiyle tutarlidir ve verification raporu MODE-01/MODE-02 gap durumunu dogru yansitir.</done>
  <resume-signal>"approved" yaz veya fail olan test adimi + gozlenen davranisi paylas.</resume-signal>
</task>

</tasks>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml led_output lighting_mode`
- `05-HARDWARE-UAT.md` sonuc tablosu yeni kosuyla doldurulmus olmali
- `05-VERIFICATION.md` status/gaps bolumu yeni teknik kanitla uyumlu olmali
</verification>

<success_criteria>
- Ambilight branch'i artik sleep-loop degil, frame tabanli fiziksel output yoluna baglidir.
- Solid mode secimi fiziksel cihaza payload yazar; no-op `Ok(())` davranisi kaldirilmistir.
- MODE-01 ve MODE-02 icin verification blocker'lari kapanir veya kalan teknik fark acikca raporlanir.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-lighting-modes/05-04-SUMMARY.md`
</output>
