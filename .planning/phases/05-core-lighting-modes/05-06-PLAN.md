---
phase: 05-core-lighting-modes
plan: "06"
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - src-tauri/src/commands/ambilight_capture.rs
  - src-tauri/src/commands/lighting_mode.rs
  - .planning/phases/05-core-lighting-modes/05-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [MODE-01, MODE-02]
must_haves:
  truths:
    - "Kullanici Ambilight modunu actiginda runtime static/sentetik frame degil canli monitor frame kaynagini kullanir (MODE-01)."
    - "Canli capture baslatilamazsa Ambilight baslangici kodlu ve acik hata nedeni ile fail olur; sessiz fallback calismaz."
    - "Solid mode fiziksel cikisi ve kalibrasyon korunumu regresyona ugramaz (MODE-02 + persistence)."
  artifacts:
    - path: "src-tauri/src/commands/ambilight_capture.rs"
      provides: "Platform live frame source factory + capture hata kodlari"
      exports: ["AmbilightFrameSource", "AmbilightCaptureError", "sample_led_frame", "create_live_frame_source"]
    - path: "src-tauri/src/commands/lighting_mode.rs"
      provides: "Varsayilan frame_source_factory icin live source baglantisi ve start-failure propagation"
      exports: ["set_lighting_mode", "stop_lighting", "get_lighting_mode_status"]
    - path: ".planning/phases/05-core-lighting-modes/05-VERIFICATION.md"
      provides: "MODE-01 gap closure sonrasi dogrulama sonucu ve kanit guncellemesi"
  key_links:
    - from: "src-tauri/src/commands/lighting_mode.rs"
      to: "src-tauri/src/commands/ambilight_capture.rs"
      via: "runtime default frame source factory"
      pattern: "create_live_frame_source|frame_source_factory"
    - from: "src-tauri/src/commands/ambilight_capture.rs"
      to: "windows-capture crate"
      via: "platform monitor frame capture implementation"
      pattern: "windows_capture|capture_frame"
    - from: "src-tauri/src/commands/lighting_mode.rs"
      to: "src-tauri/src/commands/led_output.rs"
      via: "capture->sample->send zincirinde fiziksel frame gonderimi"
      pattern: "send_ambilight_frame_to_port"
---

<objective>
05-VERIFICATION dosyasinda acik kalan MODE-01 blocker'ini kapatmak icin Ambilight runtime varsayilan frame kaynagini static implementasyondan gercek canli monitor capture kaynagina tasimak.

Purpose: Phase 5 hedefinin "real-time screen mirroring" boyutunu kod seviyesinde gerceklemek ve verification/UAT kanitlarini ayni gercege senkronlamak.
Output: Live frame source implementasyonu + runtime default wiring + guncel verification raporu.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-lighting-modes/05-RESEARCH.md
@.planning/phases/05-core-lighting-modes/05-VERIFICATION.md
@.planning/phases/05-core-lighting-modes/05-HARDWARE-UAT.md
@.planning/phases/05-core-lighting-modes/05-05-SUMMARY.md
@src-tauri/src/commands/ambilight_capture.rs
@src-tauri/src/commands/lighting_mode.rs

<interfaces>
From src-tauri/src/commands/ambilight_capture.rs:
```rust
pub trait AmbilightFrameSource: Send {
    fn capture_frame(&mut self) -> Result<CapturedFrame, AmbilightCaptureError>;
}

pub enum AmbilightCaptureError {
    FrameUnavailable,
    InvalidFrame(&'static str),
}

pub fn sample_led_frame(
    frame: &CapturedFrame,
    calibration: &SamplingCalibration,
) -> Result<SampledLedFrame, AmbilightCaptureError>;
```

From src-tauri/src/commands/lighting_mode.rs:
```rust
type AmbilightFrameSourceFactory =
    dyn Fn() -> Result<Box<dyn AmbilightFrameSource>, AmbilightCaptureError> + Send + Sync;

impl Default for LightingRuntimeOwner {
    fn default() -> Self {
        Self {
            frame_source_factory: Arc::new(|| Ok(Box::new(StaticFrameSource::default()))),
            // ...
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ambilight capture kontratini platform live source ile tamamla</name>
  <files>src-tauri/src/commands/ambilight_capture.rs</files>
  <behavior>
    - Test 1: Live source factory basarili oldugunda `AmbilightFrameSource` uzerinden gercek monitor frame'i alinabilir ve `sample_led_frame` ile islenebilir.
    - Test 2: Capture baslatma/frame alma hatalari `AmbilightCaptureError` icinde kodlu reason olarak doner; static fallback'e otomatik donus yoktur.
    - Test 3: Mevcut sampler dogrulamalari (frame dimensions/pixel count/led count) green kalir.
  </behavior>
  <action>`ambilight_capture.rs` icine production path olarak `create_live_frame_source()` ekle. Windows hedefinde (`cfg(target_os = "windows")`) `windows-capture` kullanarak birincil monitorden frame uretebilen source implemente et; hata durumlarini `AMBILIGHT_CAPTURE_*` kodlarina map et. Non-Windows path'te deterministic unsupported error donerek sessiz success veya static fallback uretme. `StaticFrameSource` sadece test/sandbox yardimci olarak kalsin; production default olmasin.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml ambilight_capture</automated>
  </verify>
  <done>Ambilight capture katmani live source factory sunar; production canli capture dogrudan bu factory uzerinden erisilebilir ve hata semantigi kodludur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lighting_mode varsayilan frame source wiring'ini live capture'a gecir</name>
  <files>src-tauri/src/commands/lighting_mode.rs, src-tauri/src/commands/ambilight_capture.rs</files>
  <behavior>
    - Test 1: `LightingRuntimeOwner::default()` static source yerine live source factory kullanir.
    - Test 2: Live source olusturma/capture hatasi `AMBILIGHT_MODE_START_FAILED` + detail reason olarak sonuclanir.
    - Test 3: Solid mode apply, stop-before-start transaction ve single-worker kurallari green kalir.
  </behavior>
  <action>`lighting_mode.rs` icindeki varsayilan `frame_source_factory` tanimini `create_live_frame_source()` cagiracak sekilde degistir. Runtime'in start akisinda hata propagation mantigini koru; details alaninda capture reason kodunu sakla. `send_ambilight_frame_to_port`, solid apply yolu ve persistence ile ilgili davranislara dokunma (MODE-02 regressions yasatma).</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml lighting_mode ambilight_capture led_output</automated>
  </verify>
  <done>Ambilight runtime default path canli capture source'a baglanir; capture kaynakli baslangic hatalari kodlu sekilde raporlanir; MODE-02 davranislari korunur.</done>
</task>

<task type="auto">
  <name>Task 3: Verification raporunu MODE-01 blocker kapanisina gore senkronize et</name>
  <files>.planning/phases/05-core-lighting-modes/05-VERIFICATION.md</files>
  <action>Task 1-2 sonrasi `05-VERIFICATION.md` dosyasini yeniden degerlendir: MODE-01 truth satirini live source implementation kanitlariyla guncelle, `gaps` bolumundeki mevcut blocker'i kapat ve requirements coverage tablosunu yeni duruma gore duzelt. `05-HARDWARE-UAT.md` ile teknik verification sonucunu tutarli hale getir; yeni celiski birakma.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml ambilight_capture lighting_mode led_output</automated>
  </verify>
  <done>Verification raporu status/gap/coverage bolumleri MODE-01 kapanisiyla tutarli ve kanita dayali hale gelir.</done>
</task>

</tasks>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml ambilight_capture`
- `cargo test --manifest-path src-tauri/Cargo.toml lighting_mode ambilight_capture led_output`
- `.planning/phases/05-core-lighting-modes/05-VERIFICATION.md` dosyasinda `status` ve `gaps` bolumleri live capture wiring sonucunu yansitir
</verification>

<success_criteria>
- MODE-01 icin runtime varsayilan frame kaynagi artik static kaynak degil, live monitor capture kaynagidir.
- Ambilight baslatma akisinda capture sorunu olursa `AMBILIGHT_MODE_START_FAILED` ve kodlu detail reason dondurulur.
- MODE-02 (solid output) ve kalibrasyon korunumu davranislari regress etmeden devam eder.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-lighting-modes/05-06-SUMMARY.md`
</output>
