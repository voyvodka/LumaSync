---
phase: 05-core-lighting-modes
plan: "05"
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - src-tauri/src/commands/ambilight_capture.rs
  - src-tauri/src/commands/lighting_mode.rs
  - src-tauri/src/lib.rs
  - .planning/phases/05-core-lighting-modes/05-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [MODE-01, MODE-02]
must_haves:
  truths:
    - "Kullanici Ambilight modunu actiginda LED cikisi sentetik degil, gercek ekran frame verisine gore uretilir."
    - "Kullanici Solid moduna gectiginde secilen renk/parlaklik fiziksel cikisa uygulanmaya devam eder."
    - "Kullanici Ambilight <-> Solid gecislerinde tek aktif runtime kuralini korur ve kalibrasyon kaydi bozulmaz."
  artifacts:
    - path: "src-tauri/src/commands/ambilight_capture.rs"
      provides: "ekran frame kaynagi ve frame->LED ornekleme kontrati"
      exports: ["AmbilightFrameSource", "SampledLedFrame", "sample_led_frame"]
    - path: "src-tauri/src/commands/lighting_mode.rs"
      provides: "capture->sample->send worker akisi ve sentetik frame yolunun kaldirilmasi"
      exports: ["set_lighting_mode", "stop_lighting", "get_lighting_mode_status"]
    - path: ".planning/phases/05-core-lighting-modes/05-VERIFICATION.md"
      provides: "MODE-01 gap closure sonrasi guncel teknik dogrulama kaydi"
  key_links:
    - from: "src-tauri/src/commands/lighting_mode.rs"
      to: "src-tauri/src/commands/ambilight_capture.rs"
      via: "ambilight worker icinde frame alimi ve ornekleme"
      pattern: "AmbilightFrameSource|sample_led_frame"
    - from: "src-tauri/src/commands/lighting_mode.rs"
      to: "src-tauri/src/commands/led_output.rs"
      via: "sample edilen frame'in send_ambilight_frame_to_port ile cihaza yazilmasi"
      pattern: "send_ambilight_frame_to_port"
    - from: "src/App.tsx"
      to: "src/features/shell/windowLifecycle.ts"
      via: "saveShellState({ lightingMode }) partial merge"
      pattern: "saveShellState\\(\\{ lightingMode"
---

<objective>
05-VERIFICATION'da acik kalan MODE-01 blocker'ini kapatmak icin Ambilight runtime'i sentetik frame uretiminden cikarip gercek ekran capture->sample->send pipeline'ina gecir.

Purpose: Phase 5 kapanisinin teknik olarak dogru olmasini saglamak; MODE-01'i gercek ekran mirroring davranisiyla kanitlarken MODE-02 ve persistence davranisini regress etmemek.
Output: Capture/sampling kontrati + runtime entegrasyonu + guncel verification raporu.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-lighting-modes/05-RESEARCH.md
@.planning/phases/05-core-lighting-modes/05-VERIFICATION.md
@.planning/phases/05-core-lighting-modes/05-04-SUMMARY.md
@src-tauri/src/commands/lighting_mode.rs
@src-tauri/src/commands/led_output.rs
@src-tauri/src/commands/device_connection.rs

<interfaces>
From src-tauri/src/commands/lighting_mode.rs:
```rust
#[derive(Clone, Deserialize, Serialize, PartialEq, Debug)]
#[serde(rename_all = "camelCase")]
pub struct LightingModeConfig {
    pub kind: LightingModeKind,
    pub solid: Option<SolidColorPayload>,
    pub ambilight: Option<AmbilightPayload>,
}

#[tauri::command]
pub fn set_lighting_mode(
    payload: LightingModeConfig,
    runtime_state: State<'_, LightingRuntimeState>,
    connection_state: State<'_, SerialConnectionState>,
) -> Result<LightingModeCommandResult, String>;
```

From src-tauri/src/commands/led_output.rs:
```rust
pub fn send_ambilight_frame_to_port(
    bridge: &LedOutputBridge,
    port_name: &str,
    frame: &[[u8; 3]],
    brightness: f32,
) -> Result<(), LedOutputError>;
```

From src-tauri/src/commands/device_connection.rs:
```rust
pub struct SerialConnectionState {
    pub(crate) last_status: Mutex<SerialConnectionStatus>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ambilight capture/sampling kontratini test-first tanimla</name>
  <files>src-tauri/src/commands/ambilight_capture.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - Test 1: Capture kaynagi bir frame verdiginde sampler deterministic LED RGB listesi uretir.
    - Test 2: Frame bulunamazsa/invalid olursa kodlanmis hata sonucu doner ve worker bu sonucu ele alabilir.
    - Test 3: Sampler, calibration verisine gore LED sayisi kadar cikis uretir; magic-length fallback kullanmaz.
  </behavior>
  <action>Yeni `ambilight_capture.rs` modulunu ekle ve Ambilight frame kaynagi icin trait-temelli kontrat yaz (`AmbilightFrameSource`). Sampling fonksiyonunu ayri pure helper olarak tanimla (`sample_led_frame`) ve testleri bu helper etrafinda yaz. Bu task'in sonunda `lighting_mode.rs` hala mevcut davranista kalabilir; sadece downstream task'in baglanacagi capture/sampling API'si netlesmis olsun. Bu adimda sentetik `build_ambilight_frame` fonksiyonunu degistirme.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml ambilight_capture</automated>
  </verify>
  <done>Capture/sampling kontrati ve test-backed davranislari diske eklenmis, runtime'in baglanacagi API sabitlenmis olur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lighting_mode worker'ini capture->sample->send akisina bagla ve sentetik frame yolunu kaldir</name>
  <files>src-tauri/src/commands/lighting_mode.rs, src-tauri/src/commands/ambilight_capture.rs</files>
  <behavior>
    - Test 1: Ambilight branch'i `build_ambilight_frame` yerine capture kaynagindan frame alip `send_ambilight_frame_to_port` ile gonderir.
    - Test 2: Capture/ sampling hatasi oldugunda status kodu `AMBILIGHT_MODE_START_FAILED` veya runtime-safe coded reason ile raporlanir.
    - Test 3: Solid mode ve stop-before-start transaction testleri green kalir (MODE-02 regresyon korumasi).
  </behavior>
  <action>`lighting_mode.rs` icinde Ambilight worker'i `AmbilightFrameSource` + `sample_led_frame` kullanacak sekilde refactor et. Sentetik `build_ambilight_frame` ve tick-temelli frame ureti mini tamamen kaldir. Worker dongusu: capture frame al -> LED renklerini sample et -> `send_ambilight_frame_to_port` ile yaz -> cancel kontrolu. `DEVICE_NOT_CONNECTED`, `SOLID_MODE_APPLY_FAILED`, stop-before-start ve tek aktif worker kurallarini degistirme. MODE-02 kapsamini korumak icin mevcut solid testlerini guncelleyip tekrar gecir; yeni degisiklik solid yolu etkilememeli.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml lighting_mode led_output ambilight_capture</automated>
  </verify>
  <done>Ambilight runtime artik gercek capture/sampling pipeline'i kullanir; sentetik frame uretimi koddan cikar ve MODE-02 davranislari regress etmez.</done>
</task>

<task type="auto">
  <name>Task 3: Verification raporunu MODE-01 gap closure durumuna gore guncelle</name>
  <files>.planning/phases/05-core-lighting-modes/05-VERIFICATION.md</files>
  <action>Task 1-2 tamamlandiktan sonra `05-VERIFICATION.md` dosyasini yeniden yaz: `gaps_found` durumunu yeni kod kanitina gore guncelle, failed truth satirinda artik capture->sample->send baglantisinin mevcut oldugunu dosya/line referanslariyla belirt, ve MODE-02'nin regress etmedigini ilgili test/akislara dayandir. `05-HARDWARE-UAT.md` mevcut approved kanit olarak kalabilir; sadece verification raporu teknik gercekle senkronize olsun.</action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml lighting_mode led_output ambilight_capture</automated>
  </verify>
  <done>Verification raporu, MODE-01 blocker'inin kapandigini (veya kalan net farki) acik, tutarli ve kanita dayali bicimde yansitir.</done>
</task>

</tasks>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml ambilight_capture`
- `cargo test --manifest-path src-tauri/Cargo.toml lighting_mode led_output ambilight_capture`
- `05-VERIFICATION.md` status/gaps bolumu yeni kod kaniti ile senkron
</verification>

<success_criteria>
- Ambilight runtime kodunda sentetik `build_ambilight_frame` yolu kalmaz; capture->sample->send zinciri aktif olur.
- MODE-01 truth'u kod seviyesinde karsilanir ve verification raporunda blocker kapanir.
- MODE-02 solid output ve persistence davranislari degisiklikten etkilenmeden dogrulanir.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-lighting-modes/05-05-SUMMARY.md`
</output>
