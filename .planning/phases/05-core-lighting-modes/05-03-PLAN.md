---
phase: 05-core-lighting-modes
plan: "03"
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - src/App.tsx
  - src/features/settings/SettingsLayout.tsx
  - src/features/settings/sections/GeneralSection.tsx
  - src/features/settings/sections/GeneralSection.test.tsx
  - src/locales/en/common.json
  - src/locales/tr/common.json
autonomous: false
requirements: [MODE-01, MODE-02]
must_haves:
  truths:
    - "Kullanici General ekranindan Ambilight ve Solid modlari arasinda gecis yapabilir."
    - "Kullanici Solid modda renk degistirdiginde cikis rengi aninda guncellenir."
    - "Mode degisimi sonrasinda kayitli kalibrasyon verisi korunur."
  artifacts:
    - path: "src/features/settings/sections/GeneralSection.tsx"
      provides: "mode secici UI + solid color kontrolu + lock-state davranisi"
    - path: "src/App.tsx"
      provides: "mode command orchestration ve persisted mode restore"
    - path: "src/features/settings/sections/GeneralSection.test.tsx"
      provides: "mode secim davranisini kilitleyen UI testleri"
  key_links:
    - from: "src/App.tsx"
      to: "src/features/mode/modeApi.ts"
      via: "mode degisiminde set/stop command cagrisi"
      pattern: "setLightingMode|stopLighting"
    - from: "src/App.tsx"
      to: "src/features/shell/windowLifecycle.ts"
      via: "saveShellState ile mode alanini kismi persist etme"
      pattern: "saveShellState\\(\\{ lightingMode"
---

<objective>
Mode engine'i kullanicinin gorecegi ayar akisina bagla ve fiziksel UAT ile phase davranisini kapat.

Purpose: MODE-01/MODE-02'nin kullanici perspektifinden ulasilabilir olmasini ve mode degisimlerinde kalibrasyonun bozulmamasini dogrulamak.
Output: General section mode UI, App-level mode orchestration/persistence ve hardware UAT onayi.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-lighting-modes/05-RESEARCH.md
@.planning/phases/05-core-lighting-modes/05-VALIDATION.md
@src/App.tsx
@src/features/settings/sections/GeneralSection.tsx
@src/features/settings/SettingsLayout.tsx

<interfaces>
From src/App.tsx:
```typescript
const [savedCalibration, setSavedCalibration] = useState<LedCalibrationConfig | undefined>(undefined);
const [ledModeEnabled, setLedModeEnabled] = useState(false);
```

From src/features/settings/sections/GeneralSection.tsx:
```typescript
interface GeneralSectionProps {
  ledModeEnabled: boolean;
  modeLockReason: ModeGuardReason | null;
  onLedModeChange: (nextEnabled: boolean) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: General section mode secimini Ambilight/Solid odakli UI'a donustur</name>
  <files>src/features/settings/sections/GeneralSection.tsx, src/features/settings/sections/GeneralSection.test.tsx, src/features/settings/SettingsLayout.tsx, src/locales/en/common.json, src/locales/tr/common.json</files>
  <behavior>
    - Test 1: Kullanici Ambilight secenegini sectiginde `onModeChange` callback'i `ambilight` payload'i ile cagrilir.
    - Test 2: Kullanici Solid seceneginde renk degistirdiginde `solid` payload guncellenir.
    - Test 3: `CALIBRATION_REQUIRED` lock davranisi yeni UI'da da disabled + CTA olarak korunur.
  </behavior>
  <action>Mevcut tek toggle UI'yi mode seciciye cevir: `Off`, `Ambilight`, `Solid` secenekleri gorunsun; Solid seciliyken hex/rgb tabanli renk kontrolu ve brightness alanini goster. `SettingsLayout` prop kontratini bool yerine mode config callback'ine guncelle. EN/TR locale keylerini parity ile genislet. Mevcut lock-state CTA davranisini (calibration ac) bozmadan yeni seciciye tasiyip testle kilitle.</action>
  <verify>
    <automated>yarn vitest run src/features/settings/sections/GeneralSection.test.tsx</automated>
  </verify>
  <done>Kullanici UI'da Ambilight/Solid secimini yapabilir; lock-state davranisi korunur; i18n parity bozulmaz.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App-level mode orchestration ve persistence baglantisini tamamla</name>
  <files>src/App.tsx</files>
  <behavior>
    - Test 1: App bootstrapi persisted `lightingMode` alanini yukler ve UI state'ine yansitir.
    - Test 2: Mode degisiminde `setLightingMode/stopLighting` command'lari dogru payload ile cagrilir.
    - Test 3: Mode kaydederken `ledCalibration` korunur; sadece `lightingMode` partial save edilir.
  </behavior>
  <action>`App.tsx` icindeki `ledModeEnabled` bool state'ini `LightingModeConfig` state'ine tasiyip `modeGuard` ile birlikte calistir. Bootstraptan gelen shell state'te `lightingMode` varsa restore et; degisimlerde backend modeApi cagir, basarili sonucu shell'e partial save et. Off'a geciste `stopLighting` cagir. Command hata durumlarini mevcut status-card/console patternine uygun sekilde kullaniciya gorunur tut (silent swallow yapma).</action>
  <verify>
    <automated>yarn vitest run src/features/mode/state/modeRuntimeFlow.test.ts src/features/mode/state/modePersistence.test.ts src/features/settings/sections/GeneralSection.test.tsx</automated>
  </verify>
  <done>Mode secimi runtime ve persistence katmanina baglanir; mode degisimi kalibrasyon kaydini bozmadan calisir.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fiziksel LED uzerinde MODE-01 ve MODE-02 final UAT</name>
  <files>src/features/settings/sections/GeneralSection.tsx, src/App.tsx, src-tauri/src/commands/lighting_mode.rs</files>
  <action>Task 1-2 ile tamamlanan mode secim akislarini gercek donanimda dogrula; Ambilight takip davranisi, Solid uniform renk cikisi ve mode gecislerinde kalibrasyon korunumu son kullanici gozlemiyle onaylanana kadar checklist adimlarini uygula.</action>
  <what-built>Ambilight/Solid mode secimi UI ve backend runtime'a baglandi; bu adim fiziksel cihaz davranisini son kez dogrular.</what-built>
  <how-to-verify>
    1. Uygulamayi ac, cihaz bagli ve kalibrasyon kayitli durumda General > Lighting moduna git.
    2. `Ambilight` sec, ekranda hizli kontrastli pencere hareketleri yap; LED cikisinin ekran renkleriyle eslestigini gozlemle.
    3. `Solid` sec, sirayla kirmizi/yesil/mavi renkleri ve en az iki farkli brightness degeri uygula; tum stripin tek renk oldugunu dogrula.
    4. Ambilight <-> Solid gecislerini en az 5 kez tekrarla; kalibrasyon ekranina gidip kayitli geometri verisinin aynen korundugunu dogrula.
    5. `Off` sec ve fiziksel cikisin durdugunu teyit et.
  </how-to-verify>
  <verify>
    <automated>yarn vitest run src/features/mode/state/modeRuntimeFlow.test.ts src/features/mode/state/modePersistence.test.ts src/features/settings/sections/GeneralSection.test.tsx</automated>
  </verify>
  <done>Insan dogrulamasinda MODE-01 ve MODE-02 davranislari fiziksel donanimda PASS olur veya fail adimlari net raporlanir.</done>
  <resume-signal>"approved" yaz veya fail adim numarasi + beklenen/gercek sonucu paylas.</resume-signal>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/mode/state/modeRuntimeFlow.test.ts src/features/mode/state/modePersistence.test.ts`
- `yarn vitest run src/features/settings/sections/GeneralSection.test.tsx`
- Hardware UAT: Ambilight tracking + Solid uniform color + calibration persistence
</verification>

<success_criteria>
- Kullanici General ekranindan Ambilight ve Solid modlarini secip degistirebilir.
- Solid mod renk degisimleri fiziksel stripte uniform uygulanir.
- Mode gecisleri kalibrasyon kaydini bozmadan devam eder ve UAT sonucu onaylanir.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-lighting-modes/05-03-SUMMARY.md`
</output>
