---
phase: 05-core-lighting-modes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/mode/model/contracts.ts
  - src/features/mode/model/contracts.test.ts
  - src/shared/contracts/shell.ts
  - src/shared/contracts/device.ts
  - src/features/mode/state/modeRuntimeFlow.ts
  - src/features/mode/state/modeRuntimeFlow.test.ts
  - src/features/mode/state/modePersistence.test.ts
  - src/features/mode/modeApi.ts
  - src/features/mode/modeApi.test.ts
autonomous: true
requirements: [MODE-01, MODE-02]
must_haves:
  truths:
    - "Kullanici modu Ambilight veya Solid olarak sectiginde sistem tek bir aktif moda iner."
    - "Mod secimi kalibrasyon kaydini bozmaz; mode data ayri ve kismi update ile saklanir."
    - "Frontend komut payload'i backend komut ID kontratiyla birebir eslesir."
  artifacts:
    - path: "src/features/mode/model/contracts.ts"
      provides: "Lighting mode domain modelleri ve persistable payload tipleri"
      exports: ["LIGHTING_MODE_KIND", "LightingModeConfig"]
    - path: "src/features/mode/state/modeRuntimeFlow.ts"
      provides: "stop->start transactional gecis kurallari"
      exports: ["resolveModeTransition"]
    - path: "src/features/mode/modeApi.ts"
      provides: "set/stop/status invoke wrapper'lari"
      exports: ["setLightingMode", "stopLighting", "getLightingModeStatus"]
  key_links:
    - from: "src/features/mode/modeApi.ts"
      to: "src/shared/contracts/device.ts"
      via: "DEVICE_COMMANDS uzerinden invoke"
      pattern: "DEVICE_COMMANDS\\.(SET_LIGHTING_MODE|STOP_LIGHTING|GET_LIGHTING_MODE_STATUS)"
    - from: "src/features/mode/state/modeRuntimeFlow.ts"
      to: "src/features/mode/model/contracts.ts"
      via: "mode kind bazli gecis karari"
      pattern: "mode\\.kind"
---

<objective>
Phase 5 icin mode motorunun sozlesmelerini (kontrat, komut, gecis kurali) test-backed sekilde kilitle.

Purpose: Backend ve UI implementasyonlarinin ayni mode modeline dayanmasini saglayip MODE-01/MODE-02 icin yorum gerektirmeyen teknik temel olusturmak.
Output: Lighting mode domain tipleri, shell persist alani, command wrapper'lari ve lifecycle/persistence unit testleri.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-lighting-modes/05-RESEARCH.md
@.planning/phases/05-core-lighting-modes/05-VALIDATION.md
@.planning/phases/04-calibration-workflow/04-09-SUMMARY.md
@src/shared/contracts/shell.ts
@src/shared/contracts/device.ts
@src/features/mode/state/modeGuard.ts

<interfaces>
From src/shared/contracts/shell.ts:
```typescript
export interface ShellState {
  ledCalibration?: LedCalibrationConfig;
}
export const DEFAULT_SHELL_STATE: ShellState;
```

From src/shared/contracts/device.ts:
```typescript
export const DEVICE_COMMANDS: {
  START_CALIBRATION_TEST_PATTERN: string;
  STOP_CALIBRATION_TEST_PATTERN: string;
};
```

From src/features/mode/state/modeGuard.ts:
```typescript
export function canEnableLedMode(calibration?: LedCalibrationConfig): LedModeGuardResult;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lighting mode kontratlarini ve persisted shell alanini tanimla</name>
  <files>src/features/mode/model/contracts.ts, src/features/mode/model/contracts.test.ts, src/shared/contracts/shell.ts, src/shared/contracts/device.ts</files>
  <behavior>
    - Test 1: Domain model yalnizca `off | ambilight | solid` mode kind degerlerini kabul eder.
    - Test 2: Solid mode payload'i `r,g,b,brightness` alanlarini normalize ederek tutar.
    - Test 3: Shell state'e mode alanlari eklenince `ledCalibration` kontrati degismez.
  </behavior>
  <action>`contracts.ts` altinda mode domain tiplerini tanimla (`LightingModeKind`, `LightingModeConfig`, `SolidColorPayload`, `AmbilightPayload`, normalize helper'lari). `shell.ts` icinde persisted `lightingMode` alanini ekle ve `DEFAULT_SHELL_STATE` ile geriye-donuk uyumlulugu koru. `device.ts` icine sadece yeni mode command ID'lerini append et (`SET_LIGHTING_MODE`, `STOP_LIGHTING`, `GET_LIGHTING_MODE_STATUS`); mevcut command kimliklerini degistirme.</action>
  <verify>
    <automated>yarn vitest run src/features/mode/model/contracts.test.ts</automated>
  </verify>
  <done>Mode modeli type-level ve unit-test seviyesinde kilitlenir; shell persist yapisi kalibrasyonla cakismadan genisler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runtime gecis/persistence kurallarini ve mode API bridge'ini uygula</name>
  <files>src/features/mode/state/modeRuntimeFlow.ts, src/features/mode/state/modeRuntimeFlow.test.ts, src/features/mode/state/modePersistence.test.ts, src/features/mode/modeApi.ts, src/features/mode/modeApi.test.ts</files>
  <behavior>
    - Test 1: Ambilight'a gecis `stopPrevious=true` ve `startNext=ambilight` transaction adimlarini uretir.
    - Test 2: Ambilight'tan Solid'e gecis once onceki runtime'i durdurur sonra solid baslatir.
    - Test 3: Mode persistence merge'i sadece mode alanlarini yazar, `ledCalibration` alanini korur.
    - Test 4: modeApi invoke payload'i yeni command ID'leri ile birebir eslesir.
  </behavior>
  <action>`resolveModeTransition` gibi saf bir lifecycle helper'i yaz ve MODE-01/MODE-02 gecis kurallarini deterministic hale getir. Ayrica shell merge davranisini testte dogrulayacak persistence helper'larini ekle (full overwrite yasak). `modeApi.ts` icinde `setLightingMode`, `stopLighting`, `getLightingModeStatus` wrapper'larini yaz; hata kodlarini mevcut device API deseniyle (`code/message/details`) map et.</action>
  <verify>
    <automated>yarn vitest run src/features/mode/state/modeRuntimeFlow.test.ts src/features/mode/state/modePersistence.test.ts src/features/mode/modeApi.test.ts</automated>
  </verify>
  <done>Mode lifecycle ve persistence kurallari testlerle ispatlanir; backend entegrasyonu icin hazir typed API surface olusur.</done>
</task>

</tasks>

<verification>
- `yarn vitest run src/features/mode/model/contracts.test.ts`
- `yarn vitest run src/features/mode/state/modeRuntimeFlow.test.ts src/features/mode/state/modePersistence.test.ts src/features/mode/modeApi.test.ts`
</verification>

<success_criteria>
- Phase 5 implementasyonu tek bir canonical mode domain modelini kullanir.
- MODE-01/MODE-02 transition kurallari test-backed olarak deterministic hale gelir.
- Shell persistence mode update'lerinde kalibrasyon kaybi olusmadigini testle kanitlar.
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-lighting-modes/05-01-SUMMARY.md`
</output>
